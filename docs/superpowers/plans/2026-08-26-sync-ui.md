# Plan 4d — UI de synchronisation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendre la synchronisation utilisable : une section « Synchronisation » dans les réglages (test de connexion, inscription, connexion, état, appareils, déconnexion), le dialogue de première synchronisation avec sauvegarde JSON automatique, et le déverrouillage du coffre après redémarrage — le trou de conception laissé entre 4c et 4d.

**Architecture:** Le moteur 4c est headless et complet ; 4d le pilote. Quatre tâches moteur d'abord (statut `locked` et garde de verrouillage, `signOut` complet, déverrouillage après inscription, validation d'URL), puis l'UI React qui les consomme. Toute l'UI vit dans `src/components/sync/` en fichiers focalisés — `SettingsDialog.tsx` fait déjà 1247 lignes et ne gagne qu'un onglet et une ligne de rendu. Un module `src/sync/runtime.ts` détient le moteur vivant pour que la connexion depuis les réglages démarre la sync sans redémarrer l'app.

**Tech Stack:** React 19 / Tauri 2, `@base-ui/react` + Tailwind v4, zustand, i18next (EN + FR), vitest + `@testing-library/react` (happy-dom) pour l'UI, vitest + better-sqlite3 (`// @vitest-environment node`) pour le moteur.

**Spec (autorité) :** [docs/superpowers/specs/2026-08-20-sync-offline-first-design.md](../specs/2026-08-20-sync-offline-first-design.md) — §2.1, §6.2, §6.3, §6.4, §6.5, §7.
**Contrat headless (contraignant) :** [docs/superpowers/plans/2026-08-25-sync-engine.md](./2026-08-25-sync-engine.md).

---

## État réel des dépôts (vérifié le 2026-08-26, copié du code)

Lire cette section avant toute chose : elle corrige trois points de la consigne d'origine.

1. **Les deux prérequis bloquants sont corrigés**, mais **pas dans `develop`**. Ils sont sur `fix/sync-tag-collision-mid-batch`, **9 commits devant `develop`** :
   - Atomicité (§9.5) : [`src-tauri/src/db.rs:44`](../../../src-tauri/src/db.rs) pool `max_connections(1)` + `idle_timeout(None)` + `max_lifetime(None)`, [`src/db/connection-lock.ts`](../../../src/db/connection-lock.ts) verrou lecteurs/rédacteur, et un test Rust qui **démontre** le bug d'origine sur un pool à 2 connexions ([`db.rs:109`](../../../src-tauri/src/db.rs)).
   - Ordre FK inter-pages : report en `sync_extra._pendingRef` ([`apply.ts:119-142`](../../../src/sync/apply.ts)), relink de fin de cycle ([`apply.ts:473`](../../../src/sync/apply.ts)), test multi-pages ([`engine.pull.test.ts:515`](../../../src/sync/engine.pull.test.ts)). Le report est **persisté en base**, donc il survit aux pages **et** aux redémarrages.
2. **Les FK sont bien actives en production.** `sqlx-sqlite` 0.8.6 pose `foreign_keys=ON` par défaut dans les pragmas de `SqliteConnectOptions::new()` (`options/mod.rs:185`). Aucun `PRAGMA` applicatif n'est nécessaire, et le report FK ci-dessus est donc porteur.
3. **695 tests** passent sur cette branche (pas 675). C'est la ligne de base.
4. **L'avertissement d'import §9.4 est déjà livré** : [`ImportConfirmDialog.tsx`](../../../src/components/layout/ImportConfirmDialog.tsx) existe, avec choix de mode puis confirmation séparée, et la copie dit explicitement « *including on your other synced devices* » (`en.ts:270-275`). **Retiré du périmètre 4d** contrairement à ce qu'annonçait le « hors périmètre » de 4c.

### Les trois trous que 4d doit combler

Ils sont invisibles tant que la sync est inerte, et chacun casse en production dès qu'on pose `server_url`.

- **Le coffre est verrouillé au redémarrage.** [`initSync`](../../../src/sync/init.ts) appelle `scheduler.start()` **sans condition** dès que `server_url` et `refresh_token` existent. La DEK ne vit qu'en mémoire Rust ([`state.rs:13-19`](../../../src-tauri/src/crypto/state.rs)) et disparaît à la fermeture. Conséquences mesurées dans le code :
  - **Pull** : `decrypt` lève `CryptoError::Locked` ; ce n'est pas une `SyntaxError`, donc [`engine.ts:322-328`](../../../src/sync/engine.ts) classe `decrypt-failed` → **toute la page part en quarantaine et le curseur avance quand même**. `retryQuarantine` rattrape après déverrouillage, mais chaque démarrage à froid déverse une page entière en quarantaine.
  - **Push** : `cipher.encrypt` n'est protégé par aucun `try` dans `pushPhase` ([`engine.ts`](../../../src/sync/engine.ts)) ; l'erreur remonte à `syncNow`, qui **relance** tout ce qui n'est ni `SyncHttpError` ni `SyncNetworkError` → le scheduler fait `void syncNow()` → **rejet de promesse non géré**.
  - **Aucune UI de déverrouillage n'existe** : seuls `auth.ts` et `crypto/index.ts` référencent le coffre.
- **`register` laisse le coffre verrouillé.** `crypto_prepare_registration` ([`state.rs:119-124`](../../../src-tauri/src/crypto/state.rs)) ne prend **pas** de `state` : c'est une fonction pure qui fabrique le matériel et le retourne, sans jamais charger la DEK. Après [`register()`](../../../src/sync/auth.ts) un compte tout neuf ne peut donc **rien chiffrer**.
- **`signOut` n'efface que `refresh_token`** ([`auth.ts:181`](../../../src/sync/auth.ts)). Le §6.5 exige aussi le curseur, l'outbox, l'identité et l'URL.

Et **aucune validation d'URL n'existe** nulle part : `baseUrl` est concaténé tel quel (`auth.ts:52, 94, 103, 116, 139, 172`).

---

## Décisions tranchées (2026-08-26, avec Loan)

Ne pas les rouvrir pendant l'exécution.

1. **Le déverrouillage porte sur la sync seule, pas sur l'app.** L'app s'ouvre normalement en local. Le moteur ne travaille pas tant que le coffre est fermé ; un bandeau et le panneau de réglages proposent de déverrouiller. Raison : le SQLite local **n'est pas chiffré** — barrer toute l'app n'ajouterait aucune confidentialité et coûterait une saisie à chaque lancement.
2. **Garde moteur : statut `locked` + sortie immédiate de `syncNow`.** Aucun pull, aucun push, aucune quarantaine, aucun rejet non géré tant que `isUnlocked()` est faux. C'est une modification du moteur, portée par les tâches 1 de ce plan.
3. **Un seul plan.** Les blockers étant corrigés, il ne reste que la conscience du verrouillage : tâches 1-4 moteur, 5+ UI.
4. **Nouvel onglet « Synchronisation », panneau dans ses propres fichiers** (`src/components/sync/`). `SettingsDialog.tsx` ne gagne qu'un identifiant d'onglet et une ligne de rendu.
5. **Confirmation de la clé de 24 mots par ressaisie de 3 mots tirés au hasard.** Standard des portefeuilles crypto : prouve réellement la sauvegarde sans imposer 24 champs.
6. **État affiché : statut + dernière sync + « Synchroniser maintenant » + liste des appareils avec révocation.** Le compteur de quarantaine est **volontairement différé** (voir « Hors périmètre »).
7. **`signOut` étendu dans `src/sync/auth.ts`**, pas de seconde fonction : un seul point d'entrée, tout le nettoyage dans une transaction.
8. **Changement de serveur interdit à chaud** : tant qu'un compte est connecté, le champ URL est en lecture seule ; on passe obligatoirement par « Déconnecter ». Rend le « un seul serveur à la fois » structurel.

### Raffinement de la décision 2 (à appliquer tel quel)

La formulation d'origine disait « `initSync` n'arme le scheduler qu'une fois déverrouillé ». **L'implémentation retenue est plus simple et strictement plus sûre** : `initSync` arme **toujours** le scheduler, et c'est `syncNow` qui garde.

Pourquoi : `SyncScheduler.start()` appelle `syncNow()` immédiatement ([`scheduler.ts`](../../../src/sync/scheduler.ts)). Avec la garde dans `syncNow`, ce premier appel constate le verrouillage, pose le statut `locked` et sort — sans réseau. On y gagne trois choses qu'un scheduler non armé n'offre pas :

- **Le statut affiché est juste dès le démarrage.** Un moteur construit mais jamais appelé resterait à `idle` et le panneau mentirait.
- **La reprise est automatique.** Le tick suivant reprend tout seul dès que le coffre est ouvert ; aucun code de « réarmement » à écrire ni à tester.
- **La garde couvre le bouton « Synchroniser maintenant ».** Un scheduler non armé laisserait `syncNow` nu sur le chemin manuel — exactement là où la garde compte.

---

## Contrat 4c consommé (signatures copiées du code, ne pas les dériver)

```ts
// src/sync/auth.ts
export interface VaultPort {
	beginUnlock(password: string, authSalt: string): Promise<string>;
	completeUnlock(wrappedDek: string, userId: string): Promise<void>;
}
export const tauriVault: VaultPort;
export function getServerInfo(fetchImpl: FetchLike, baseUrl: string): Promise<ServerInfo>;
export function signIn(
	deps: { db: DbDriver; fetchImpl: FetchLike; baseUrl: string; vault: VaultPort },
	input: { email: string; password: string; deviceName: string; devicePlatform: string },
): Promise<{ accessToken: string }>;
export function register(
	deps: { db: DbDriver; fetchImpl: FetchLike; baseUrl: string },   // Omit<AuthDeps, "vault">
	input: { email: string; password: string; deviceName: string; devicePlatform: string; inviteToken?: string },
): Promise<{ accessToken: string; recoveryPhrase: string }>;
export function signOut(deps: { db: DbDriver; fetchImpl: FetchLike; baseUrl: string }): Promise<void>;
export class AuthorizedHttp {
	constructor(deps: { db: DbDriver; fetchImpl: FetchLike; baseUrl: string; seedAccessToken?: string });
	request<T>(method: "GET" | "POST" | "PUT" | "DELETE", path: string, body?: unknown): Promise<T>;
}

// src/sync/types.ts
export type SyncStatus = "idle" | "syncing" | "awaiting-first-sync" | "reauth-required" | "protocol-mismatch";
export interface ServerInfo { name: string; version: string; protocolVersion: number; registrationEnabled: boolean; minClientVersion: string; }
export const CLIENT_PROTOCOL_VERSION = 1;
export class CursorOutOfRangeError extends Error {}
export class ReauthRequiredError extends Error {}
export class ProtocolMismatchError extends Error { constructor(public readonly server: ServerInfo); }

// src/sync/engine.ts
export interface SyncEngineDeps { db: DbDriver; transport: SyncTransport; cipher: RecordCipher; getServerInfo: () => Promise<ServerInfo>; }
export class SyncEngine {
	constructor(deps: SyncEngineDeps);
	getStatus(): SyncStatus;
	onStatus(listener: (status: SyncStatus) => void): () => void;   // retourne le désabonnement
	syncNow(): Promise<void>;
	resolveFirstSync(choice: "merge" | "replace"): Promise<void>;
	retryQuarantine(): Promise<void>;
}

// src/sync/state.ts
export type SyncStateKey =
	| "server_url" | "cursor" | "clock_offset_ms" | "refresh_token"
	| "user_id" | "account_email" | "first_sync_resolved" | "last_sync_at";
export function getSyncState(db: DbDriver, key: SyncStateKey): Promise<string | null>;
export function setSyncState(db: DbDriver, key: SyncStateKey, value: string): Promise<void>;
export function deleteSyncState(db: DbDriver, key: SyncStateKey): Promise<void>;

// src/sync/init.ts
export interface SyncRuntime { engine: SyncEngine; scheduler: SyncScheduler; repository: TodoRepository; }
export function initSync(db: DbDriver, repository: TodoRepository, deps: { fetchImpl: FetchLike; cipher?: RecordCipher }): Promise<SyncRuntime | null>;

// src/sync/http.ts
export type FetchLike = typeof fetch;
export class SyncHttpError extends Error { constructor(status: number, code: string | null, retryAfterMs: number | null, message: string); }
export class SyncNetworkError extends Error {}

// src/crypto/index.ts  (tous via invoke Tauri)
export function beginUnlock(password: string, authSalt: string): Promise<string>;
export function completeUnlock(wrappedDek: string, userId: string): Promise<void>;
export function unlockWithRecovery(recoveryPhrase: string, wrappedDekRecovery: string, userId: string): Promise<void>;
export function lock(): Promise<void>;
export function isUnlocked(): Promise<boolean>;

// src/lib/dataTransfer.ts
export interface ExportOptions { activeTasks: boolean; completedTasks: boolean; archivedTasks: boolean; projects: boolean; tags: boolean; projectIds?: string[] | null; }
export function exportData(repo: TodoRepository, options: ExportOptions): Promise<ExportData>;
```

### Contrat serveur pour les appareils (vérifié, `usagi-server` develop `74e5f2c`)

```
GET    /v1/devices        Bearer → 200 [{ id, name, platform, lastSeenAt, createdAt, current: boolean }]
DELETE /v1/devices/:id    Bearer → 204 ; 404 si l'id n'appartient pas au compte (jamais 403 : un 403 confirmerait l'existence)
```
`:id` est validé par `ParseUUIDPipe`. Les deux routes sont sous `JwtAuthGuard` et `@SkipThrottle({ login: true })`.

---

## Global Constraints

- **Dépôt :** `/home/loan/Projects/perso/usagi`. **Base : `fix/sync-tag-collision-mid-batch` (HEAD `296118f`)**, PAS `develop` — les correctifs des deux prérequis n'y sont pas encore. Branche de travail : `feat/sync-ui`. Si `fix/sync-tag-collision-mid-batch` a été mergée dans `develop` entre-temps, partir de `develop` après avoir vérifié que `34774e1`, `9ec3c5c` et `b9da4c3` en sont ancêtres (`git merge-base --is-ancestor <sha> develop`).
- **pnpm**, jamais npm.
- **La définition de « vert » dans CHAQUE tâche, sans exception :** `pnpm test:run` **ET** `pnpm exec tsc --noEmit` **ET** `pnpm build`. Biome ne typecheck pas — un test vert avec un `tsc` rouge n'est pas une tâche finie.
- **Ligne de base : 695 tests passants.** Toute tâche doit finir à ≥ ce nombre, sans régression.
- **Commits :** `type: :gitmoji: message impératif en anglais`, **sans trailer Co-Authored-By**.
- **ES2020.** Pas de `replaceAll`, `Array.prototype.at`, `Object.hasOwn`, `findLast`, opérateurs logiques d'affectation récents. Utiliser `replace(/…/g, …)`, `arr[arr.length - 1]`.
- **Commentaires en anglais**, uniquement pour un *pourquoi* non évident. **Types dans leur propre fichier**, sauf props locales d'un composant.
- **Alias `@/` = `src/`.** Tests colocalisés `*.test.ts` / `*.test.tsx`.
- **Tests moteur/DB :** en-tête `// @vitest-environment node` obligatoire (better-sqlite3). **Tests de composants :** environnement par défaut (happy-dom, configuré dans `vite.config.ts`), et `import "@/i18n";` en tête sinon `t()` retourne la clé brute.
- **Les triggers SQL horodatent à l'horloge réelle** : les fake timers de vitest ne les atteignent pas. Aucun test touchant l'outbox ne doit dépendre de `vi.useFakeTimers()` pour ses horodatages.
- **Toutes les chaînes UI passent par i18next**, en **EN et FR**, dans `src/i18n/locales/en.ts` et `fr.ts`. Zéro chaîne en dur dans le JSX.
- **Pas de bibliothèque de toasts, pas de `react-hook-form`, pas de `zod`** dans ce dépôt. Erreurs affichées en ligne : `<p className="text-xs text-destructive">`. Validation par état local et `disabled={…}`, comme [`ProjectForm.tsx`](../../../src/components/projects/ProjectForm.tsx).
- **Changelog DÛ** ([src/assets/changelog.json](../../../src/assets/changelog.json)) : la sync devient visible. Entrée bilingue `{ en, fr }` en section `"version": "Unreleased"`. Tâche 15.
- **Fin de tâche CLAUDE.md** (react-doctor sous `nvm use 22.22.2` + `rm -rf ~/.npm/_npx`, puis `pnpm run lint:fix`) : appliquée **en clôture** (tâche 15), pas à chaque tâche.
- **Ne jamais persister `recoveryPhrase`** : ni store, ni localStorage, ni base, ni log. Affichée une fois, retirée de l'état React dès confirmation.

### Protocole anti-test-creux (obligatoire)

Tout test marqué **[SABOTAGE]** passe par ce cycle, **après le commit de la tâche** (`git checkout --` ne restaure que du committé) :

1. **Saboter** le code de production exactement comme décrit.
2. **Vérifier que le sabotage s'est appliqué** — toujours les deux : `git diff --stat -- <fichier>` liste le fichier (diff vide = STOP), et le `grep` donné produit exactement la sortie attendue.
3. **Exécuter le test : il DOIT échouer.** S'il passe, le test est creux — corriger le test, pas le sabotage.
4. **Restaurer** : `git checkout -- <fichier>`.
5. **Ré-exécuter : il DOIT passer.**

**Amendement appris sur 4c (tâche 13) :** avant d'écrire un sabotage, vérifier que son mode d'échec est **réellement observable**. Un sabotage qu'une défense d'une autre couche neutralise est infalsifiable — il donne un faux vert. Si en le posant on constate que le test passe quand même *parce qu'autre chose rattrape*, changer de sabotage pour viser la seule couche testée.

---

## Structure de fichiers

| Fichier | Rôle | Tâche |
|---|---|---|
| `src/sync/types.ts` | ajout de `"locked"` à `SyncStatus` | 1 |
| `src/sync/engine.ts` (+ `engine.locked.test.ts`) | port `isUnlocked` requis, garde en tête de `syncNow` | 1 |
| `src/sync/init.ts`, `src/test-harness/engine.ts`, `src/sync/engine.pull.test.ts` | branchement du port sur les 5 sites de construction | 1 |
| `src/sync/auth.ts` (+ `auth.test.ts`) | `signOut` complet §6.5 | 2 |
| `src/sync/auth.ts` (+ `auth.test.ts`) | déverrouillage du coffre après `register` | 3 |
| `src/sync/server-url.ts` (+ test) | `normalizeServerUrl` : https imposé, exceptions localhost/`.local` | 4 |
| `src/sync/devices.ts`, `src/sync/devices-types.ts` (+ test) | `listDevices` / `revokeDevice` | 5 |
| `src/sync/runtime.ts` (+ test) | moteur vivant : `startSync` / `stopSync` / `getSyncRuntime` | 6 |
| `src/store/sync.ts` (+ test) | store zustand : statut, dernière sync, abonnement `onStatus` | 6 |
| `src/i18n/locales/en.ts`, `fr.ts` | toutes les clés `sync.*` | 7 |
| `src/components/sync/ServerUrlForm.tsx` (+ test) | saisie URL + « Tester la connexion » (§6.2) | 8 |
| `src/components/sync/SignInForm.tsx` (+ test) | connexion | 9 |
| `src/components/sync/recovery-confirmation.ts` (+ test) | tirage des 3 positions à ressaisir | 10 |
| `src/components/sync/RegisterForm.tsx`, `RecoveryPhraseStep.tsx` (+ tests) | inscription §2.1 | 10 |
| `src/components/sync/DeviceList.tsx` (+ test) | appareils + révocation (§6.3) | 11 |
| `src/components/sync/ConnectedPanel.tsx` (+ test) | état, dernière sync, sync manuelle, déconnexion | 11 |
| `src/components/sync/UnlockDialog.tsx` (+ test) | déverrouillage à la demande | 12 |
| `src/components/sync/SyncStatusBanner.tsx` (+ test) | bandeau §7 (verrouillé / reconnexion / protocole) | 12 |
| `src/components/sync/FirstSyncDialog.tsx` (+ test) | §6.4 + sauvegarde JSON automatique avant « Remplacer » | 13 |
| `src/components/sync/SyncPanel.tsx` (+ test) | orchestration des écrans du panneau | 14 |
| `src/components/layout/SettingsDialog.tsx`, `AppShell.tsx`, `src/App.tsx` | branchement | 14 |
| `src/assets/changelog.json` | entrée bilingue | 15 |

---

## Types partagés (référence pour toutes les tâches)

```ts
// src/sync/devices-types.ts  (tâche 5)
export interface SyncDevice {
	id: string;
	name: string;
	platform: string;
	lastSeenAt: string | null;
	createdAt: string;
	current: boolean;
}

// src/sync/server-url.ts  (tâche 4)
export type ServerUrlError = "empty" | "malformed" | "insecure";
export interface ServerUrlResult {
	url: string | null;          // normalisée, sans slash final ; null si invalide
	error: ServerUrlError | null;
	insecureWarning: boolean;    // true quand http:// est toléré (hôte local)
}
```

---

# PARTIE A — Prérequis moteur (tâches 1 à 4)

Aucune UI n'est branchée avant que ces quatre tâches soient vertes.

---

### Tâche 1 : Statut `locked` et garde de verrouillage

Le trou central. Sans elle, un simple redémarrage envoie une page entière en quarantaine et lève un rejet non géré au push.

**Files:**
- Modify: `src/sync/types.ts`
- Modify: `src/sync/engine.ts`
- Modify: `src/sync/init.ts`
- Modify: `src/test-harness/engine.ts`
- Modify: `src/sync/engine.pull.test.ts` (3 sites de construction : lignes ~263, ~770, ~794)
- Test: `src/sync/engine.locked.test.ts` (créer)

**Interfaces:**
- Consomme : `SyncEngine`, `SyncEngineDeps`, `SyncStatus` (4c) ; `isUnlocked()` de `@/crypto`.
- Produit : `SyncStatus` gagne `"locked"` ; `SyncEngineDeps` gagne `isUnlocked: () => Promise<boolean>` **requis**. Les tâches 6, 11, 12 et 14 en dépendent.

> **Pourquoi requis et non optionnel avec défaut permissif.** Il n'y a que **5 sites de construction** dans tout le dépôt (`init.ts`, `test-harness/engine.ts`, 3 dans `engine.pull.test.ts`) : le coût est nul. Un défaut `() => true` rendrait un futur oubli de branchement silencieux en production — exactement la classe de bug que cette tâche existe pour fermer.

- [ ] **Step 1: Écrire le test d'échec**

Créer `src/sync/engine.locked.test.ts` :

```ts
// @vitest-environment node
import { beforeEach, describe, expect, it } from "vitest";
import { FakeSyncServer } from "@/test-harness/FakeSyncServer";
import { makeDevice, syncMerging } from "@/test-harness/engine";

describe("engine — verrouillage du coffre", () => {
	let server: FakeSyncServer;

	beforeEach(() => {
		server = new FakeSyncServer();
	});

	it("ne pull rien, ne quarantaine rien et n'avance pas le curseur quand le coffre est fermé", async () => {
		// Un appareil source pousse un enregistrement réel sur le serveur.
		const source = await makeDevice(server);
		await source.repo.createProject({ name: "Projet distant" });
		await syncMerging(source);

		// L'appareil cible démarre coffre fermé, comme après un redémarrage.
		let unlocked = false;
		const target = await makeDevice(server, { isUnlocked: async () => unlocked });

		await target.engine.syncNow();

		expect(target.engine.getStatus()).toBe("locked");
		expect(await target.repo.getProjects()).toHaveLength(0);
		const quarantined = await target.driver.select<{ n: number }>(
			"SELECT COUNT(*) AS n FROM sync_quarantine",
		);
		expect(quarantined[0].n).toBe(0);
		const cursor = await target.driver.select<{ value: string }>(
			"SELECT value FROM sync_state WHERE key = 'cursor'",
		);
		expect(cursor).toHaveLength(0);

		// Déverrouillage : le cycle suivant rattrape tout, sans intervention.
		unlocked = true;
		await syncMerging(target);
		expect(target.engine.getStatus()).toBe("idle");
		expect(await target.repo.getProjects()).toHaveLength(1);
	});

	it("ne pousse rien et ne lève pas quand le coffre est fermé", async () => {
		let unlocked = false;
		const device = await makeDevice(server, { isUnlocked: async () => unlocked });
		await device.repo.createProject({ name: "Écrit hors ligne" });

		// Le mode d'échec avant correction : cipher.encrypt lève Locked dans
		// pushPhase, l'erreur remonte et syncNow la relance.
		await expect(device.engine.syncNow()).resolves.toBeUndefined();
		expect(device.engine.getStatus()).toBe("locked");
		expect(server.recordCount()).toBe(0);

		// L'outbox est intacte : rien n'a été consommé ni perdu.
		const outbox = await device.driver.select<{ n: number }>(
			"SELECT COUNT(*) AS n FROM sync_outbox",
		);
		expect(outbox[0].n).toBeGreaterThan(0);

		unlocked = true;
		await syncMerging(device);
		expect(server.recordCount()).toBe(1);
	});
});
```

> **Note pour l'implémenteur.** `server.recordCount()` doit exister sur `FakeSyncServer`. Vérifier la méthode réelle avec `grep -n "recordCount\|^\t[a-z].*(" src/test-harness/FakeSyncServer.ts` ; si elle porte un autre nom, **adapter l'appel du test à l'API réelle**, jamais l'inverse. Idem pour `createProject` / `getProjects` : confirmer les signatures dans [`src/db/repository.ts`](../../../src/db/repository.ts) (rappel 4c : c'est `tagIds`, pas `tags`).

- [ ] **Step 2: Lancer le test et constater le rouge**

```bash
pnpm test:run src/sync/engine.locked.test.ts
```
Attendu : ÉCHEC. `makeDevice` n'accepte pas encore `isUnlocked` (erreur TypeScript / option ignorée), et le statut vaut `idle` ou `syncing`, pas `locked`.

- [ ] **Step 3: Ajouter `"locked"` au type de statut**

Dans `src/sync/types.ts`, remplacer la définition de `SyncStatus` :

```ts
export type SyncStatus =
	| "idle"
	| "syncing"
	| "locked"
	| "awaiting-first-sync"
	| "reauth-required"
	| "protocol-mismatch";
```

- [ ] **Step 4: Ajouter le port au moteur et la garde dans `syncNow`**

Dans `src/sync/engine.ts`, étendre `SyncEngineDeps` :

```ts
export interface SyncEngineDeps {
	db: DbDriver;
	transport: SyncTransport;
	cipher: RecordCipher;
	getServerInfo: () => Promise<ServerInfo>;
	/**
	 * The DEK lives only in Rust memory, so a restart leaves the vault shut
	 * while server_url and refresh_token survive on disk. Decrypting then
	 * fails as `decrypt-failed` and quarantines a whole pull page while the
	 * cursor moves on; encrypting throws straight out of pushPhase. The
	 * engine has to know, so this port is required, not defaulted.
	 */
	isUnlocked: () => Promise<boolean>;
}
```

Puis, dans `syncNow`, **avant** `ensureProtocol` — donc avant toute requête réseau :

```ts
	async syncNow(): Promise<void> {
		if (this.running) {
			this.rerunRequested = true;
			return;
		}
		this.running = true;
		try {
			if (!(await this.deps.isUnlocked())) {
				this.setStatus("locked");
				return;
			}
			if (!(await this.ensureProtocol())) return;
			// … inchangé
```

> Le `finally` existant relance `syncNow` si `rerunRequested` : c'est correct ici aussi — la relance reconstatera le verrouillage et sortira, sans boucle chaude puisque chaque passage sort immédiatement.

- [ ] **Step 5: Brancher le port sur les cinq sites de construction**

Dans `src/sync/init.ts`, importer `isUnlocked` et le passer :

```ts
import { isUnlocked, lock } from "@/crypto";
```
```ts
		const engine = new SyncEngine({
			db,
			transport: new HttpSyncTransport(http, new RequestGate()),
			cipher: deps.cipher ?? new TauriRecordCipher(),
			getServerInfo: () => getServerInfo(deps.fetchImpl, serverUrl),
			isUnlocked: deps.isUnlocked ?? isUnlocked,
		});
```

et étendre la signature pour rester testable sous node (pas de runtime Tauri en vitest) :

```ts
export async function initSync(
	db: DbDriver,
	repository: TodoRepository,
	deps: {
		fetchImpl: FetchLike;
		cipher?: RecordCipher;
		isUnlocked?: () => Promise<boolean>;
	},
): Promise<SyncRuntime | null> {
```

Dans `src/test-harness/engine.ts`, ajouter l'option et la passer :

```ts
export async function makeDevice(
	server: FakeSyncServer,
	opts: {
		cipher?: FakeRecordCipher;
		serverInfo?: ServerInfo;
		isUnlocked?: () => Promise<boolean>;
	} = {},
): Promise<TestDevice> {
	const driver = new BetterSqliteDriver();
	await runMigrations(driver, ALL_MIGRATIONS);
	const cipher = opts.cipher ?? new FakeRecordCipher();
	const engine = new SyncEngine({
		db: driver,
		transport: server.transport(),
		cipher,
		getServerInfo: async () => opts.serverInfo ?? FAKE_SERVER_INFO,
		isUnlocked: opts.isUnlocked ?? (async () => true),
	});
	return { driver, repo: new SqliteRepository(driver), engine, cipher };
}
```

Dans `src/sync/engine.pull.test.ts`, ajouter `isUnlocked: async () => true,` aux trois littéraux `new SyncEngine({ … })` (≈ lignes 263, 770, 794). Les localiser par :

```bash
grep -n "new SyncEngine" src/sync/engine.pull.test.ts
```

- [ ] **Step 6: Vert complet**

```bash
pnpm test:run src/sync/engine.locked.test.ts
pnpm test:run
pnpm exec tsc --noEmit
pnpm build
```
Attendu : le nouveau fichier passe ; **≥ 697 tests** au total (695 + 2) ; `tsc` et `build` silencieux.

- [ ] **Step 7: Commit**

```bash
git add src/sync/types.ts src/sync/engine.ts src/sync/init.ts src/test-harness/engine.ts src/sync/engine.pull.test.ts src/sync/engine.locked.test.ts
git commit -m "feat: :lock: stop the sync engine from running on a locked vault"
```

- [ ] **Step 8: [SABOTAGE] Vérifier que la garde est réellement testée**

Retirer la garde dans `src/sync/engine.ts` — supprimer les quatre lignes :

```ts
			if (!(await this.deps.isUnlocked())) {
				this.setStatus("locked");
				return;
			}
```

Vérifier que le sabotage s'est appliqué, **les deux** :

```bash
git diff --stat -- src/sync/engine.ts
grep -c "isUnlocked" src/sync/engine.ts
```
Attendu : `engine.ts` listé avec des suppressions ; le `grep -c` retourne **`1`** (il ne reste que la déclaration dans `SyncEngineDeps`), contre `2` avant sabotage.

> **Observabilité du mode d'échec (amendement 4c) :** ce sabotage est falsifiable, car aucune autre couche ne rattrape. `FakeRecordCipher` chiffre sans se soucier d'un coffre, donc sans la garde le moteur pull et push normalement : le premier test verra 1 projet au lieu de 0 et le statut `idle` au lieu de `locked`. Si les deux tests passent malgré le sabotage, **c'est le test qu'il faut corriger**.

```bash
pnpm test:run src/sync/engine.locked.test.ts
```
Attendu : ÉCHEC des deux tests.

- [ ] **Step 9: Restaurer et re-vérifier**

```bash
git checkout -- src/sync/engine.ts
pnpm test:run src/sync/engine.locked.test.ts
```
Attendu : PASS.

---

### Tâche 2 : `signOut` efface tout l'état de sync (§6.5)

**Files:**
- Modify: `src/sync/auth.ts`
- Test: `src/sync/auth.test.ts` (existant — ajouter un `describe`)

**Interfaces:**
- Consomme : `deleteSyncState` (`src/sync/state.ts`), `DbDriver.transaction` (4a).
- Produit : `signOut` garde sa signature `(deps: { db; fetchImpl; baseUrl }) => Promise<void>` mais efface tout. La tâche 11 l'appelle.

Le §6.5 est explicite : la déconnexion efface **les clés en mémoire, le curseur, l'outbox et l'identité de l'appareil — mais ne touche pas au SQLite local** (entendre : pas aux tables métier). Aujourd'hui seul `refresh_token` part.

- [ ] **Step 1: Écrire le test d'échec**

Ajouter à `src/sync/auth.test.ts` (conserver l'en-tête `// @vitest-environment node` du fichier) :

```ts
describe("signOut", () => {
	it("efface tout l'état de sync et l'outbox, sans toucher aux données métier", async () => {
		const driver = new BetterSqliteDriver();
		await runMigrations(driver, ALL_MIGRATIONS);
		const repo = new SqliteRepository(driver);

		// Données métier + outbox alimentée par les triggers.
		await repo.createProject({ name: "À conserver" });

		for (const [key, value] of [
			["server_url", "https://sync.example.com"],
			["refresh_token", "rt-1"],
			["cursor", "42"],
			["user_id", "u-1"],
			["account_email", "a@example.com"],
			["first_sync_resolved", "1"],
			["last_sync_at", "2026-08-26T10:00:00.000Z"],
			["clock_offset_ms", "1500"],
		] as const) {
			await setSyncState(driver, key, value);
		}
		await driver.execute(
			"INSERT INTO sync_quarantine (entity_type, entity_id, direction, reason, quarantined_at) VALUES ('task', 'q1', 'pull', 'decrypt-failed', '2026-08-26T10:00:00.000Z')",
		);

		await signOut({
			db: driver,
			fetchImpl: async () => new Response(null, { status: 204 }),
			baseUrl: "https://sync.example.com",
		});

		for (const key of [
			"server_url", "refresh_token", "cursor", "user_id",
			"account_email", "first_sync_resolved", "last_sync_at", "clock_offset_ms",
		] as const) {
			expect(await getSyncState(driver, key)).toBeNull();
		}
		const outbox = await driver.select<{ n: number }>("SELECT COUNT(*) AS n FROM sync_outbox");
		expect(outbox[0].n).toBe(0);
		const quarantine = await driver.select<{ n: number }>("SELECT COUNT(*) AS n FROM sync_quarantine");
		expect(quarantine[0].n).toBe(0);

		// Le SQLite métier est intact : l'app locale doit survivre entière.
		expect(await repo.getProjects()).toHaveLength(1);
	});

	it("efface localement même si le serveur est injoignable", async () => {
		const driver = new BetterSqliteDriver();
		await runMigrations(driver, ALL_MIGRATIONS);
		await setSyncState(driver, "refresh_token", "rt-1");
		await setSyncState(driver, "server_url", "https://sync.example.com");

		await signOut({
			db: driver,
			fetchImpl: async () => {
				throw new TypeError("network down");
			},
			baseUrl: "https://sync.example.com",
		});

		expect(await getSyncState(driver, "refresh_token")).toBeNull();
		expect(await getSyncState(driver, "server_url")).toBeNull();
	});
});
```

> Vérifier les imports déjà présents en tête de `auth.test.ts` et compléter au besoin : `BetterSqliteDriver`, `runMigrations`, `ALL_MIGRATIONS`, `SqliteRepository`, `getSyncState`, `setSyncState`, `signOut`.

- [ ] **Step 2: Lancer et constater le rouge**

```bash
pnpm test:run src/sync/auth.test.ts
```
Attendu : ÉCHEC sur le premier `expect` d'une clé autre que `refresh_token` — `server_url` vaut encore `"https://sync.example.com"`.

- [ ] **Step 3: Implémenter**

Dans `src/sync/auth.ts`, remplacer le corps de `signOut` après le bloc `try/catch` réseau :

```ts
/** Every sync_state key signOut clears. Listed rather than "DELETE FROM
 * sync_state" so a key added later is a deliberate decision here, not a
 * silent inclusion. */
const SIGNED_OUT_KEYS: SyncStateKey[] = [
	"server_url",
	"cursor",
	"clock_offset_ms",
	"refresh_token",
	"user_id",
	"account_email",
	"first_sync_resolved",
	"last_sync_at",
];

export async function signOut(deps: {
	db: DbDriver;
	fetchImpl: FetchLike;
	baseUrl: string;
}): Promise<void> {
	const refreshToken = await getSyncState(deps.db, "refresh_token");
	if (refreshToken) {
		try {
			await requestJson(
				deps.fetchImpl,
				"POST",
				`${deps.baseUrl}/v1/auth/logout`,
				{ body: { refreshToken } },
			);
		} catch {
			// Best effort: the local wipe is what signs this device out; the
			// server-side revocation just also closes the session remotely.
		}
	}
	// One transaction: a partial wipe would leave a server_url without a
	// refresh token, which initSync reads as "configured" and retries forever.
	await deps.db.transaction(async (tx) => {
		for (const key of SIGNED_OUT_KEYS) await deleteSyncState(tx, key);
		await tx.execute("DELETE FROM sync_outbox");
		await tx.execute("DELETE FROM sync_quarantine");
	});
}
```

Ajouter `SyncStateKey` à l'import depuis `./state` :

```ts
import { deleteSyncState, getSyncState, setSyncState, type SyncStateKey } from "./state";
```

> **`device_id` n'est PAS effacé** — délibérément. Il vit dans `sync_state` sous la clé `device_id` gérée par [`getOrCreateDeviceId()`](../../../src/db/device-id.ts), et il estampille `field_updated_at` sur des lignes métier **qui restent en base**. L'effacer ferait repartir l'appareil avec une nouvelle identité alors que ses anciens stamps portent l'ancienne, et le départage LWW du §5 comparerait des identités incohérentes. `SyncStateKey` ne contient d'ailleurs pas `device_id`, ce qui rend l'oubli impossible par typage.

- [ ] **Step 4: Vert complet**

```bash
pnpm test:run src/sync/auth.test.ts
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 699 tests**.

- [ ] **Step 5: Commit**

```bash
git add src/sync/auth.ts src/sync/auth.test.ts
git commit -m "feat: :wastebasket: clear cursor, outbox and identity on sign-out"
```

- [ ] **Step 6: [SABOTAGE] Vérifier que le nettoyage est testé**

Dans `src/sync/auth.ts`, réduire la liste à la seule clé d'origine :

```ts
const SIGNED_OUT_KEYS: SyncStateKey[] = ["refresh_token"];
```

```bash
git diff --stat -- src/sync/auth.ts
grep -n "SIGNED_OUT_KEYS: SyncStateKey\[\]" -A 1 src/sync/auth.ts
```
Attendu : fichier listé ; le `grep` montre la liste réduite à `["refresh_token"]`.

```bash
pnpm test:run src/sync/auth.test.ts
```
Attendu : ÉCHEC — `server_url` n'est pas `null`.

- [ ] **Step 7: Restaurer et re-vérifier**

```bash
git checkout -- src/sync/auth.ts
pnpm test:run src/sync/auth.test.ts
```
Attendu : PASS.

---

### Tâche 3 : `register` déverrouille le coffre

**Files:**
- Modify: `src/sync/auth.ts`
- Test: `src/sync/auth.test.ts`

**Interfaces:**
- Consomme : `VaultPort` (déjà défini dans `auth.ts`), `RegistrationMaterial` (`src/crypto/types.ts`).
- Produit : `register` prend désormais `deps: AuthDeps` **complet** (avec `vault`), plus `Omit<…, "vault">`. La tâche 10 l'appelle avec `tauriVault`.

`crypto_prepare_registration` est une commande **sans état** : elle fabrique le matériel et le rend, sans charger la DEK dans `CryptoState`. Un compte fraîchement inscrit repart donc coffre fermé, et depuis la tâche 1 le moteur reste bloqué sur `locked` jusqu'à ce que l'utilisateur saisisse à nouveau son mot de passe — juste après l'avoir choisi. Absurde, et invisible sans test.

- [ ] **Step 1: Écrire le test d'échec**

Ajouter à `src/sync/auth.test.ts` :

```ts
describe("register", () => {
	it("laisse le coffre ouvert : le compte créé peut chiffrer immédiatement", async () => {
		const driver = new BetterSqliteDriver();
		await runMigrations(driver, ALL_MIGRATIONS);

		const unlockCalls: Array<{ salt: string; wrappedDek: string; userId: string }> = [];
		let pendingSalt: string | null = null;
		const vault: VaultPort = {
			async beginUnlock(_password, authSalt) {
				pendingSalt = authSalt;
				return "verifier-from-vault";
			},
			async completeUnlock(wrappedDek, userId) {
				unlockCalls.push({ salt: pendingSalt ?? "", wrappedDek, userId });
			},
		};

		const fetchImpl = fakeFetch({
			"/v1/server-info": {
				name: "usagi-server",
				version: "1.0.0",
				protocolVersion: 1,
				registrationEnabled: true,
				minClientVersion: "0.1.0",
			},
			"/v1/auth/register": {
				userId: "u-42",
				workspaceId: "w-1",
				deviceId: "d-1",
				accessToken: "at-1",
				refreshToken: "rt-1",
			},
		});

		const result = await register(
			{ db: driver, fetchImpl, baseUrl: "https://sync.example.com", vault },
			{
				email: "a@example.com",
				password: "correct horse battery staple",
				deviceName: "Poste",
				devicePlatform: "linux",
			},
		);

		expect(result.recoveryPhrase).toBeTruthy();
		// Le coffre a été ouvert avec le sel et la DEK que l'inscription vient
		// de produire, et sous l'identité que le serveur a attribuée.
		expect(unlockCalls).toHaveLength(1);
		expect(unlockCalls[0].userId).toBe("u-42");
		expect(unlockCalls[0].salt).toMatch(/^[0-9a-f]{32}$/);
		expect(unlockCalls[0].wrappedDek).toBeTruthy();
	});
});
```

> `fakeFetch` : réutiliser l'utilitaire déjà présent dans `auth.test.ts`. Le localiser avec `grep -n "function fakeFetch\|const fakeFetch" src/sync/auth.test.ts` et **adapter l'appel à sa signature réelle**. `prepareRegistration` étant une commande Tauri absente sous vitest, vérifier comment `auth.test.ts` neutralise déjà `@tauri-apps/api/core` (`grep -n "vi.mock" src/sync/auth.test.ts`) et suivre le même mécanisme : le mock doit rendre un `RegistrationMaterial` dont `authSalt` fait 32 caractères hexadécimaux minuscules (contrainte §2.1, validée par le serveur).

- [ ] **Step 2: Lancer et constater le rouge**

```bash
pnpm test:run src/sync/auth.test.ts
```
Attendu : ÉCHEC — `unlockCalls` est vide ; et une erreur TypeScript, `register` n'accepte pas encore `vault`.

- [ ] **Step 3: Implémenter**

Dans `src/sync/auth.ts`, changer la signature de `register` (prend `AuthDeps` complet) et déverrouiller après la réponse serveur :

```ts
export async function register(
	deps: AuthDeps,
	input: {
		email: string;
		password: string;
		deviceName: string;
		devicePlatform: string;
		inviteToken?: string;
	},
): Promise<{ accessToken: string; recoveryPhrase: string }> {
	await assertProtocol(deps.fetchImpl, deps.baseUrl);
	const material = await prepareRegistration(input.password);
	const login = await requestJson<LoginResponse>(
		deps.fetchImpl,
		"POST",
		`${deps.baseUrl}/v1/auth/register`,
		{
			body: {
				email: input.email,
				authVerifier: material.authVerifier,
				authSalt: material.authSalt,
				keys: toRegisterKeys(material),
				deviceName: input.deviceName,
				devicePlatform: input.devicePlatform,
				...(input.inviteToken ? { inviteToken: input.inviteToken } : {}),
			},
		},
	);
	// crypto_prepare_registration is stateless: it mints the material without
	// loading the DEK. Without this the brand-new account starts locked and the
	// engine sits on "locked" until the user retypes the password they just
	// chose. Only the userId the server assigns was missing to open the vault.
	await deps.vault.beginUnlock(input.password, material.authSalt);
	await deps.vault.completeUnlock(material.wrappedDek, login.userId);
	await persistSession(deps.db, deps.baseUrl, input.email, login);
	// recoveryPhrase is real key material: shown once by the caller (plan 4d),
	// never persisted, never logged.
	return {
		accessToken: login.accessToken,
		recoveryPhrase: material.recoveryPhrase,
	};
}
```

> `beginUnlock` refait un passage Argon2id (~2 s, 64 Mio) alors que `prepareRegistration` en a déjà fait un. C'est assumé : la seule alternative serait une commande Rust supplémentaire qui charge la DEK au moment de la fabrication, et le §2.3 veut que la crypto reste côté Rust sans élargir la surface d'IPC pour un gain de deux secondes payé une fois dans la vie du compte. L'UI de la tâche 10 affiche un état de chargement pendant ce temps.

- [ ] **Step 4: Vert complet**

```bash
pnpm test:run src/sync/auth.test.ts
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 700 tests**.

- [ ] **Step 5: Commit**

```bash
git add src/sync/auth.ts src/sync/auth.test.ts
git commit -m "feat: :unlock: open the vault right after registering an account"
```

- [ ] **Step 6: [SABOTAGE]**

Dans `src/sync/auth.ts`, commenter les deux appels ajoutés :

```ts
	// await deps.vault.beginUnlock(input.password, material.authSalt);
	// await deps.vault.completeUnlock(material.wrappedDek, login.userId);
```

```bash
git diff --stat -- src/sync/auth.ts
grep -c "// await deps.vault" src/sync/auth.ts
```
Attendu : fichier listé ; le `grep -c` retourne **`2`**.

```bash
pnpm test:run src/sync/auth.test.ts
```
Attendu : ÉCHEC — `unlockCalls` a une longueur de 0 au lieu de 1.

- [ ] **Step 7: Restaurer et re-vérifier**

```bash
git checkout -- src/sync/auth.ts
pnpm test:run src/sync/auth.test.ts
```
Attendu : PASS.

---

### Tâche 4 : Validation d'URL serveur (§6.2)

**Files:**
- Create: `src/sync/server-url.ts`
- Test: `src/sync/server-url.test.ts`

**Interfaces:**
- Consomme : rien.
- Produit : `normalizeServerUrl(raw: string): ServerUrlResult` et les types `ServerUrlError` / `ServerUrlResult`. Consommé par la tâche 8.

Règle du §6.2 : `https://` imposé, **exception explicite** pour `localhost`, `127.0.0.1` et les domaines `.local`, sans quoi l'auto-hébergement en réseau local et le développement deviennent impraticables. Avertissement visible en cas de connexion non chiffrée.

- [ ] **Step 1: Écrire les tests d'échec**

Créer `src/sync/server-url.test.ts` :

```ts
import { describe, expect, it } from "vitest";
import { normalizeServerUrl } from "./server-url";

describe("normalizeServerUrl", () => {
	it("accepte https et retire le slash final", () => {
		expect(normalizeServerUrl("https://sync.example.com/")).toEqual({
			url: "https://sync.example.com",
			error: null,
			insecureWarning: false,
		});
	});

	it("ajoute https:// quand le schéma est absent", () => {
		expect(normalizeServerUrl("sync.example.com")).toEqual({
			url: "https://sync.example.com",
			error: null,
			insecureWarning: false,
		});
	});

	it("conserve un port et un chemin de base", () => {
		expect(normalizeServerUrl("https://sync.example.com:8443/usagi")).toEqual({
			url: "https://sync.example.com:8443/usagi",
			error: null,
			insecureWarning: false,
		});
	});

	it("refuse http sur un hôte public", () => {
		expect(normalizeServerUrl("http://sync.example.com")).toEqual({
			url: null,
			error: "insecure",
			insecureWarning: false,
		});
	});

	it.each([
		"http://localhost:3000",
		"http://127.0.0.1:3000",
		"http://nas.local",
		"http://NAS.LOCAL",
	])("tolère http sur un hôte local (%s) en avertissant", (raw) => {
		const result = normalizeServerUrl(raw);
		expect(result.error).toBeNull();
		expect(result.url).not.toBeNull();
		expect(result.insecureWarning).toBe(true);
	});

	it("n'avertit pas quand un hôte local est joint en https", () => {
		expect(normalizeServerUrl("https://localhost:3000")).toEqual({
			url: "https://localhost:3000",
			error: null,
			insecureWarning: false,
		});
	});

	it("ne prend pas un sous-domaine trompeur pour un hôte local", () => {
		// "localhost.attacker.com" n'est PAS localhost.
		expect(normalizeServerUrl("http://localhost.attacker.com").error).toBe("insecure");
		// ".local" doit être un suffixe de label, pas une sous-chaîne.
		expect(normalizeServerUrl("http://not-local.example.com").error).toBe("insecure");
	});

	it.each(["", "   "])("signale une saisie vide (%p)", (raw) => {
		expect(normalizeServerUrl(raw)).toEqual({
			url: null,
			error: "empty",
			insecureWarning: false,
		});
	});

	it.each(["ftp://sync.example.com", "https://", "h ttp://x", "://"])(
		"signale une URL malformée (%s)",
		(raw) => {
			const result = normalizeServerUrl(raw);
			expect(result.url).toBeNull();
			expect(result.error).toBe("malformed");
		},
	);
});
```

- [ ] **Step 2: Lancer et constater le rouge**

```bash
pnpm test:run src/sync/server-url.test.ts
```
Attendu : ÉCHEC — `Cannot find module './server-url'`.

- [ ] **Step 3: Implémenter**

Créer `src/sync/server-url.ts` :

```ts
export type ServerUrlError = "empty" | "malformed" | "insecure";

export interface ServerUrlResult {
	/** Normalised, without a trailing slash. Null when the input is unusable. */
	url: string | null;
	error: ServerUrlError | null;
	/** True when http:// was tolerated because the host is local (§6.2). */
	insecureWarning: boolean;
}

/**
 * Spec §6.2 demands https, with an explicit carve-out for localhost, 127.0.0.1
 * and .local domains — without it self-hosting on a LAN and development are
 * impractical.
 *
 * Matched on the parsed hostname, never on the raw string: "localhost.attacker.com"
 * contains "localhost" and "not-local.example.com" contains ".local", and a
 * substring test would hand both a plaintext channel.
 */
function isLocalHost(hostname: string): boolean {
	const host = hostname.toLowerCase();
	return (
		host === "localhost" ||
		host === "127.0.0.1" ||
		host === "[::1]" ||
		host === "::1" ||
		host.endsWith(".localhost") ||
		host.endsWith(".local")
	);
}

const FAILURE = { url: null, insecureWarning: false } as const;

export function normalizeServerUrl(raw: string): ServerUrlResult {
	const trimmed = raw.trim();
	if (trimmed === "") return { ...FAILURE, error: "empty" };

	// A bare host is the common case when typing; assume the secure scheme
	// rather than rejecting, so the carve-out below stays the exception.
	const candidate = /^[a-z][a-z0-9+.-]*:\/\//i.test(trimmed)
		? trimmed
		: `https://${trimmed}`;

	let parsed: URL;
	try {
		parsed = new URL(candidate);
	} catch {
		return { ...FAILURE, error: "malformed" };
	}

	if (parsed.protocol !== "https:" && parsed.protocol !== "http:") {
		return { ...FAILURE, error: "malformed" };
	}
	if (parsed.hostname === "") return { ...FAILURE, error: "malformed" };

	const insecure = parsed.protocol === "http:";
	if (insecure && !isLocalHost(parsed.hostname)) {
		return { ...FAILURE, error: "insecure" };
	}

	// Drop query and fragment: a base URL carrying either would corrupt every
	// path the sync client appends to it.
	const path = parsed.pathname.replace(/\/+$/, "");
	return {
		url: `${parsed.protocol}//${parsed.host}${path}`,
		error: null,
		insecureWarning: insecure,
	};
}
```

> `parsed.host` (et non `hostname`) conserve le port. `replace(/\/+$/, "")` plutôt que `replaceAll` : cible ES2020.

- [ ] **Step 4: Vert complet**

```bash
pnpm test:run src/sync/server-url.test.ts
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 715 tests** (les `it.each` comptent une entrée chacun).

- [ ] **Step 5: Commit**

```bash
git add src/sync/server-url.ts src/sync/server-url.test.ts
git commit -m "feat: :closed_lock_with_key: validate the server URL before any credential is typed"
```

- [ ] **Step 6: [SABOTAGE] Vérifier que l'exception locale n'est pas une sous-chaîne**

Dans `src/sync/server-url.ts`, remplacer le corps de `isLocalHost` par une comparaison naïve :

```ts
function isLocalHost(hostname: string): boolean {
	const host = hostname.toLowerCase();
	return host.includes("localhost") || host.includes("127.0.0.1") || host.includes(".local");
}
```

```bash
git diff --stat -- src/sync/server-url.ts
grep -c "includes(" src/sync/server-url.ts
```
Attendu : fichier listé ; le `grep -c` retourne **`3`** (0 avant sabotage).

```bash
pnpm test:run src/sync/server-url.test.ts
```
Attendu : ÉCHEC du test « ne prend pas un sous-domaine trompeur pour un hôte local ».

> **Précision vérifiée — ne pas s'y tromper.** Seule la **première** assertion de ce test tombe : `"localhost.attacker.com".includes("localhost")` est vrai, donc l'URL est acceptée avec `error: null` au lieu de `"insecure"`. La seconde ne bouge pas : `"not-local.example.com".includes(".local")` est **faux** (la sous-chaîne avant `local` est `-`, pas `.`), donc ce cas reste correctement refusé même saboté. Un test qui échoue sur la première assertion est le résultat attendu — ce n'est pas un sabotage à moitié appliqué.

- [ ] **Step 7: Restaurer et re-vérifier**

```bash
git checkout -- src/sync/server-url.ts
pnpm test:run src/sync/server-url.test.ts
```
Attendu : PASS.

---

**Fin de la partie A.** À ce stade le moteur refuse de tourner à froid, la déconnexion est complète, l'inscription ouvre le coffre et les URL sont validées. Aucune de ces tâches n'a rendu la sync visible : `server_url` n'est toujours posé par personne.

---

# PARTIE B — Fondations UI (tâches 5 à 7)

---

### Tâche 5 : Client des appareils

**Files:**
- Create: `src/sync/devices-types.ts`
- Create: `src/sync/devices.ts`
- Test: `src/sync/devices.test.ts`

**Interfaces:**
- Consomme : `AuthorizedHttp` (`src/sync/auth.ts`) — `request<T>(method, path, body?)`.
- Produit : `listDevices(http: AuthorizedHttp): Promise<SyncDevice[]>`, `revokeDevice(http: AuthorizedHttp, id: string): Promise<void>`, type `SyncDevice`. Consommés par la tâche 11.

- [ ] **Step 1: Écrire les tests d'échec**

Créer `src/sync/devices.test.ts` :

```ts
import { describe, expect, it, vi } from "vitest";
import type { AuthorizedHttp } from "./auth";
import { listDevices, revokeDevice } from "./devices";

function fakeHttp(impl: (method: string, path: string) => unknown): AuthorizedHttp {
	return { request: vi.fn(async (method, path) => impl(method, path)) } as unknown as AuthorizedHttp;
}

describe("listDevices", () => {
	it("appelle GET /v1/devices et rend la liste telle quelle", async () => {
		const payload = [
			{
				id: "11111111-1111-4111-8111-111111111111",
				name: "Poste fixe",
				platform: "linux",
				lastSeenAt: "2026-08-26T09:00:00.000Z",
				createdAt: "2026-08-01T09:00:00.000Z",
				current: true,
			},
		];
		const http = fakeHttp(() => payload);
		await expect(listDevices(http)).resolves.toEqual(payload);
		expect(http.request).toHaveBeenCalledWith("GET", "/v1/devices");
	});

	it("tolère un lastSeenAt absent", async () => {
		const http = fakeHttp(() => [
			{
				id: "22222222-2222-4222-8222-222222222222",
				name: "Portable",
				platform: "macos",
				lastSeenAt: null,
				createdAt: "2026-08-02T09:00:00.000Z",
				current: false,
			},
		]);
		const [device] = await listDevices(http);
		expect(device.lastSeenAt).toBeNull();
	});
});

describe("revokeDevice", () => {
	it("appelle DELETE /v1/devices/:id", async () => {
		const http = fakeHttp(() => undefined);
		await revokeDevice(http, "33333333-3333-4333-8333-333333333333");
		expect(http.request).toHaveBeenCalledWith(
			"DELETE",
			"/v1/devices/33333333-3333-4333-8333-333333333333",
		);
	});

	it("encode l'identifiant dans le chemin", async () => {
		const http = fakeHttp(() => undefined);
		await revokeDevice(http, "a b/c");
		expect(http.request).toHaveBeenCalledWith("DELETE", "/v1/devices/a%20b%2Fc");
	});
});
```

- [ ] **Step 2: Lancer et constater le rouge**

```bash
pnpm test:run src/sync/devices.test.ts
```
Attendu : ÉCHEC — `Cannot find module './devices'`.

- [ ] **Step 3: Implémenter**

Créer `src/sync/devices-types.ts` :

```ts
/** One row of GET /v1/devices. `current` is computed server-side from the
 * device id carried by the JWT. */
export interface SyncDevice {
	id: string;
	name: string;
	platform: string;
	lastSeenAt: string | null;
	createdAt: string;
	current: boolean;
}
```

Créer `src/sync/devices.ts` :

```ts
import type { AuthorizedHttp } from "./auth";
import type { SyncDevice } from "./devices-types";

export type { SyncDevice } from "./devices-types";

export function listDevices(http: AuthorizedHttp): Promise<SyncDevice[]> {
	return http.request<SyncDevice[]>("GET", "/v1/devices");
}

/**
 * The server answers 404 — never 403 — when the id is not this account's, so a
 * probe cannot confirm an id exists. Callers surface both the same way.
 */
export async function revokeDevice(
	http: AuthorizedHttp,
	id: string,
): Promise<void> {
	await http.request<void>("DELETE", `/v1/devices/${encodeURIComponent(id)}`);
}
```

> Vérifier que `AuthorizedHttp.request` prend bien un **chemin** et non une URL absolue : `grep -n "async request" -A 8 src/sync/auth.ts`. Si elle attend une URL complète, adapter ces deux fonctions à l'API réelle.

- [ ] **Step 4: Vert complet**

```bash
pnpm test:run src/sync/devices.test.ts
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 719 tests**.

- [ ] **Step 5: Commit**

```bash
git add src/sync/devices-types.ts src/sync/devices.ts src/sync/devices.test.ts
git commit -m "feat: :iphone: add the devices client for listing and revoking"
```

---

### Tâche 6 : Runtime de sync vivant et store de statut

**Files:**
- Create: `src/sync/runtime.ts`
- Create: `src/store/sync.ts`
- Test: `src/sync/runtime.test.ts`
- Test: `src/store/sync.test.ts`

**Interfaces:**
- Consomme : `initSync`, `SyncRuntime` (`src/sync/init.ts`) ; `setRepository` (`src/store/repository.ts`) ; `SyncEngine.onStatus` / `getStatus`.
- Produit :
  - `setSyncContext(ctx: SyncContext): void`, `startSync(): Promise<SyncRuntime | null>`, `stopSync(): Promise<void>`, `getSyncRuntime(): SyncRuntime | null`
  - store zustand `useSyncStore` avec `{ status: SyncStatus | null; lastSyncAt: string | null; attach(engine); detach(); refreshLastSync(db) }`
  - Consommés par les tâches 8 à 14.

Sans ce module, se connecter depuis les réglages n'aurait d'effet qu'au redémarrage suivant : `initSync` n'est appelé qu'une fois, dans `App.tsx`, et son résultat est jeté sauf le repository.

- [ ] **Step 1: Écrire le test d'échec du runtime**

Créer `src/sync/runtime.test.ts` :

```ts
// @vitest-environment node
import { beforeEach, describe, expect, it } from "vitest";
import { ALL_MIGRATIONS } from "@/db/migrations";
import { runMigrations } from "@/db/migrations/run-migrations";
import { SqliteRepository } from "@/db/sqlite-repository";
import { BetterSqliteDriver } from "@/test-harness/BetterSqliteDriver";
import { setSyncState } from "./state";
import { getSyncRuntime, setSyncContext, startSync, stopSync } from "./runtime";

async function makeContext() {
	const driver = new BetterSqliteDriver();
	await runMigrations(driver, ALL_MIGRATIONS);
	const repo = new SqliteRepository(driver);
	setSyncContext({
		db: driver,
		repository: repo,
		fetchImpl: async () => new Response("{}", { status: 200 }),
		isUnlocked: async () => true,
	});
	return { driver, repo };
}

describe("sync runtime", () => {
	beforeEach(async () => {
		await stopSync();
	});

	it("ne démarre rien tant que server_url est absent (§6.1)", async () => {
		await makeContext();
		expect(await startSync()).toBeNull();
		expect(getSyncRuntime()).toBeNull();
	});

	it("démarre une fois la session persistée, et expose le moteur", async () => {
		const { driver } = await makeContext();
		await setSyncState(driver, "server_url", "https://sync.example.com");
		await setSyncState(driver, "refresh_token", "rt-1");

		const runtime = await startSync();
		expect(runtime).not.toBeNull();
		expect(getSyncRuntime()).toBe(runtime);
	});

	it("stopSync arrête le scheduler et oublie le runtime", async () => {
		const { driver } = await makeContext();
		await setSyncState(driver, "server_url", "https://sync.example.com");
		await setSyncState(driver, "refresh_token", "rt-1");
		await startSync();

		await stopSync();
		expect(getSyncRuntime()).toBeNull();
	});

	it("un second startSync ne laisse pas deux schedulers derrière lui", async () => {
		const { driver } = await makeContext();
		await setSyncState(driver, "server_url", "https://sync.example.com");
		await setSyncState(driver, "refresh_token", "rt-1");

		const first = await startSync();
		const second = await startSync();
		expect(second).not.toBe(first);
		expect(getSyncRuntime()).toBe(second);
	});
});
```

- [ ] **Step 2: Lancer et constater le rouge**

```bash
pnpm test:run src/sync/runtime.test.ts
```
Attendu : ÉCHEC — `Cannot find module './runtime'`.

- [ ] **Step 3: Implémenter le runtime**

Créer `src/sync/runtime.ts` :

```ts
import type { DbDriver } from "@/db/driver";
import type { TodoRepository } from "@/db/repository";
import { setRepository } from "@/store/repository";
import type { FetchLike } from "./http";
import { initSync, type SyncRuntime } from "./init";

export interface SyncContext {
	db: DbDriver;
	repository: TodoRepository;
	fetchImpl: FetchLike;
	/** Injected so vitest, which has no Tauri runtime, can drive the vault. */
	isUnlocked?: () => Promise<boolean>;
}

let context: SyncContext | null = null;
let current: SyncRuntime | null = null;

/** Called once from App.tsx, with the driver and repository it just built. */
export function setSyncContext(ctx: SyncContext): void {
	context = ctx;
}

export function getSyncRuntime(): SyncRuntime | null {
	return current;
}

/**
 * Builds the engine from whatever the database now says. Signing in writes
 * server_url and refresh_token, then calls this: without it the engine would
 * only come to life at the next launch, since App.tsx runs initSync once.
 */
export async function startSync(): Promise<SyncRuntime | null> {
	if (!context) throw new Error("Sync context not initialized. Call setSyncContext() first.");
	// Never leave a previous scheduler ticking behind the new one.
	await stopSync();
	const runtime = await initSync(context.db, context.repository, {
		fetchImpl: context.fetchImpl,
		isUnlocked: context.isUnlocked,
	});
	if (!runtime) return null;
	current = runtime;
	// initSync wraps the repository so local writes debounce a sync.
	setRepository(runtime.repository);
	return runtime;
}

export async function stopSync(): Promise<void> {
	if (!current) return;
	current.scheduler.stop();
	current = null;
	// Back to the unwrapped repository: no writes should schedule a sync now.
	if (context) setRepository(context.repository);
}
```

> Confirmer que `SyncScheduler` expose bien `stop()` : `grep -n "stop()" src/sync/scheduler.ts`.

- [ ] **Step 4: Vérifier le vert du runtime**

```bash
pnpm test:run src/sync/runtime.test.ts
```
Attendu : PASS (4 tests).

- [ ] **Step 5: Écrire le test d'échec du store**

Créer `src/store/sync.test.ts` :

```ts
import { beforeEach, describe, expect, it } from "vitest";
import type { SyncEngine } from "@/sync/engine";
import type { SyncStatus } from "@/sync/types";
import { useSyncStore } from "./sync";

/** Minimal stand-in: the store only ever touches getStatus and onStatus. */
function fakeEngine(initial: SyncStatus = "idle") {
	const listeners = new Set<(s: SyncStatus) => void>();
	let status = initial;
	return {
		engine: {
			getStatus: () => status,
			onStatus(listener: (s: SyncStatus) => void) {
				listeners.add(listener);
				return () => listeners.delete(listener);
			},
		} as unknown as SyncEngine,
		emit(next: SyncStatus) {
			status = next;
			for (const listener of listeners) listener(next);
		},
		listenerCount: () => listeners.size,
	};
}

describe("useSyncStore", () => {
	beforeEach(() => {
		useSyncStore.getState().detach();
	});

	it("part d'un statut nul quand la sync n'est pas configurée", () => {
		expect(useSyncStore.getState().status).toBeNull();
	});

	it("adopte le statut courant du moteur dès l'attachement", () => {
		const { engine } = fakeEngine("locked");
		useSyncStore.getState().attach(engine);
		expect(useSyncStore.getState().status).toBe("locked");
	});

	it("suit les transitions émises par le moteur", () => {
		const { engine, emit } = fakeEngine("idle");
		useSyncStore.getState().attach(engine);
		emit("syncing");
		expect(useSyncStore.getState().status).toBe("syncing");
		emit("reauth-required");
		expect(useSyncStore.getState().status).toBe("reauth-required");
	});

	it("se désabonne à detach et repasse à null", () => {
		const { engine, emit, listenerCount } = fakeEngine("idle");
		useSyncStore.getState().attach(engine);
		useSyncStore.getState().detach();
		expect(listenerCount()).toBe(0);
		expect(useSyncStore.getState().status).toBeNull();
		emit("syncing");
		expect(useSyncStore.getState().status).toBeNull();
	});

	it("ne laisse pas fuir l'abonnement au moteur précédent", () => {
		const first = fakeEngine("idle");
		const second = fakeEngine("idle");
		useSyncStore.getState().attach(first.engine);
		useSyncStore.getState().attach(second.engine);
		expect(first.listenerCount()).toBe(0);
		first.emit("protocol-mismatch");
		expect(useSyncStore.getState().status).toBe("idle");
	});
});
```

- [ ] **Step 6: Implémenter le store**

Créer `src/store/sync.ts` :

```ts
import { create } from "zustand";
import type { DbDriver } from "@/db/driver";
import type { SyncEngine } from "@/sync/engine";
import { getSyncState } from "@/sync/state";
import type { SyncStatus } from "@/sync/types";

interface SyncStore {
	/** null means sync is not configured at all — no server_url (§6.1). */
	status: SyncStatus | null;
	lastSyncAt: string | null;
	attach(engine: SyncEngine): void;
	detach(): void;
	refreshLastSync(db: DbDriver): Promise<void>;
}

// Kept outside the store: it is a subscription handle, not rendered state, and
// leaking it would keep a dead engine's listener alive across a reconnect.
let unsubscribe: (() => void) | null = null;

export const useSyncStore = create<SyncStore>((set) => ({
	status: null,
	lastSyncAt: null,

	attach(engine) {
		unsubscribe?.();
		unsubscribe = engine.onStatus((status) => set({ status }));
		set({ status: engine.getStatus() });
	},

	detach() {
		unsubscribe?.();
		unsubscribe = null;
		set({ status: null, lastSyncAt: null });
	},

	async refreshLastSync(db) {
		set({ lastSyncAt: await getSyncState(db, "last_sync_at") });
	},
}));
```

- [ ] **Step 7: Vert complet**

```bash
pnpm test:run src/store/sync.test.ts src/sync/runtime.test.ts
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 728 tests**.

- [ ] **Step 8: Commit**

```bash
git add src/sync/runtime.ts src/sync/runtime.test.ts src/store/sync.ts src/store/sync.test.ts
git commit -m "feat: :electric_plug: hold the live sync engine and expose its status to the UI"
```

- [ ] **Step 9: [SABOTAGE] Vérifier que la fuite d'abonnement est testée**

Dans `src/store/sync.ts`, retirer le désabonnement préalable dans `attach` :

```ts
	attach(engine) {
		unsubscribe = engine.onStatus((status) => set({ status }));
		set({ status: engine.getStatus() });
	},
```

```bash
git diff --stat -- src/store/sync.ts
grep -c "unsubscribe?.()" src/store/sync.ts
```
Attendu : fichier listé ; le `grep -c` retourne **`1`** (2 avant sabotage).

> **Observabilité :** le test « ne laisse pas fuir l'abonnement » est le seul à l'attraper, et rien ne le rattrape ailleurs — `detach` n'est pas appelé entre les deux `attach`. Sans le désabonnement, `first.listenerCount()` vaut 1 et `first.emit("protocol-mismatch")` écrase le statut.

```bash
pnpm test:run src/store/sync.test.ts
```
Attendu : ÉCHEC du test « ne laisse pas fuir l'abonnement au moteur précédent ».

- [ ] **Step 10: Restaurer et re-vérifier**

```bash
git checkout -- src/store/sync.ts
pnpm test:run src/store/sync.test.ts
```
Attendu : PASS.

---

### Tâche 7 : Chaînes i18n (EN + FR)

**Files:**
- Modify: `src/i18n/locales/en.ts`
- Modify: `src/i18n/locales/fr.ts`
- Test: `src/i18n/sync-keys.test.ts` (créer)

**Interfaces:**
- Produit : l'espace `sync.*` complet. Toutes les tâches 8 à 14 s'y réfèrent — **aucune ne doit ajouter de clé**, sauf oubli constaté.

Regrouper toutes les chaînes en une tâche évite que chaque tâche UI ne touche les deux fichiers de locale et n'en fasse un point de conflit permanent.

- [ ] **Step 1: Écrire le test de parité**

Créer `src/i18n/sync-keys.test.ts` :

```ts
import { describe, expect, it } from "vitest";
import en from "./locales/en";
import fr from "./locales/fr";

function flatten(obj: Record<string, unknown>, prefix = ""): string[] {
	const keys: string[] = [];
	for (const [key, value] of Object.entries(obj)) {
		const path = prefix ? `${prefix}.${key}` : key;
		if (value !== null && typeof value === "object") {
			keys.push(...flatten(value as Record<string, unknown>, path));
		} else {
			keys.push(path);
		}
	}
	return keys.sort();
}

describe("clés de synchronisation", () => {
	it("EN et FR exposent exactement les mêmes clés sync.*", () => {
		const enKeys = flatten(en.sync as Record<string, unknown>);
		const frKeys = flatten(fr.sync as Record<string, unknown>);
		expect(frKeys).toEqual(enKeys);
	});

	it("aucune valeur sync n'est vide", () => {
		for (const locale of [en, fr]) {
			for (const key of flatten(locale.sync as Record<string, unknown>)) {
				const value = key
					.split(".")
					.reduce<unknown>((acc, part) => (acc as Record<string, unknown>)[part], locale.sync);
				expect(typeof value, key).toBe("string");
				expect((value as string).trim(), key).not.toBe("");
			}
		}
	});

	it("couvre un libellé par statut moteur", () => {
		for (const status of [
			"idle", "syncing", "locked", "awaitingFirstSync",
			"reauthRequired", "protocolMismatch",
		]) {
			expect(Object.keys(en.sync.status)).toContain(status);
		}
	});
});
```

- [ ] **Step 2: Lancer et constater le rouge**

```bash
pnpm test:run src/i18n/sync-keys.test.ts
```
Attendu : ÉCHEC — `en.sync` est `undefined`.

- [ ] **Step 3: Ajouter le bloc EN**

Dans `src/i18n/locales/en.ts`, ajouter une clé `sync` au premier niveau (à côté de `data`, en respectant les tabulations du fichier) :

```ts
	sync: {
		title: "Sync",
		tab: "Sync",
		intro:
			"Sync your tasks across devices through a server you choose. Everything is encrypted on this device before it leaves it — the server never sees your data.",
		serverUrl: "Server address",
		serverUrlPlaceholder: "https://sync.example.com",
		testConnection: "Test connection",
		testing: "Testing…",
		serverFound: "{{name}} {{version}} — compatible",
		urlError: {
			empty: "Enter a server address.",
			malformed: "This does not look like a valid address.",
			insecure:
				"An unencrypted address (http://) is only allowed for localhost, 127.0.0.1 and .local names.",
		},
		insecureWarning:
			"This connection is not encrypted. Only do this on a network you trust.",
		unreachable: "Could not reach this server. Check the address and your connection.",
		notUsagi: "This address answered, but it is not a usagi server.",
		protocolMismatch:
			"This server speaks protocol version {{server}}, this app speaks {{client}}. Update whichever is older — sync is disabled until they match.",
		signIn: "Sign in",
		signingIn: "Signing in…",
		createAccount: "Create account",
		creatingAccount: "Creating account…",
		registrationClosed: "This server does not accept new accounts. Sign in with an existing one.",
		email: "Email",
		password: "Password",
		inviteToken: "Invite token",
		inviteTokenHint: "Optional — only if the server owner gave you one.",
		signInFailed: "Wrong email or password.",
		registerFailed: "Could not create the account. The invite token may be used or expired.",
		passwordHint:
			"Choose a strong password. It encrypts everything, and it is never sent to the server — nobody can reset it for you.",
		recovery: {
			title: "Your recovery key",
			intro:
				"These 24 words are the only way back into your account if you forget your password. Write them down and keep them somewhere safe. They are shown once and never stored.",
			copy: "Copy",
			copied: "Copied",
			continue: "I have written them down",
			confirmTitle: "Confirm your recovery key",
			confirmIntro: "Type these three words to confirm you saved the key.",
			wordLabel: "Word {{position}}",
			wrongWords: "Those words do not match. Check your copy and try again.",
			back: "Show the key again",
			confirm: "Confirm",
		},
		status: {
			label: "Status",
			idle: "Up to date",
			syncing: "Syncing…",
			locked: "Paused — unlock to resume",
			awaitingFirstSync: "Waiting for your answer",
			reauthRequired: "Sign in again",
			protocolMismatch: "Server incompatible",
		},
		lastSync: "Last sync",
		never: "Never",
		syncNow: "Sync now",
		account: "Account",
		devices: {
			title: "Devices",
			thisDevice: "This device",
			lastSeen: "Last seen {{when}}",
			never: "Never seen",
			revoke: "Revoke",
			revoking: "Revoking…",
			revokeConfirmTitle: "Revoke this device?",
			revokeConfirmMessage:
				"{{name}} will be signed out and will stop syncing. Its local data stays on it.",
			revokeFailed: "Could not revoke this device.",
			loadFailed: "Could not load your devices.",
			empty: "No other device.",
		},
		disconnect: "Disconnect this device",
		disconnecting: "Disconnecting…",
		disconnectConfirmTitle: "Disconnect from this server?",
		disconnectConfirmMessage:
			"Sync stops and this device forgets the account. Your tasks stay on this device, untouched.",
		changeServerHint: "Disconnect first to use a different server.",
		unlock: {
			title: "Unlock sync",
			intro:
				"Your password is needed to decrypt what the server sends. It is never stored, so it is asked again after each restart.",
			submit: "Unlock",
			unlocking: "Unlocking…",
			failed: "Wrong password.",
		},
		banner: {
			locked: "Sync is paused — unlock to resume.",
			reauthRequired: "Sync needs you to sign in again.",
			protocolMismatch: "Sync is disabled: this server is not compatible with this app version.",
			action: "Open settings",
		},
		firstSync: {
			title: "This account already has data",
			intro:
				"This device has tasks, and so does the account. Choose how to combine them — this is asked once.",
			merge: "Merge",
			mergeExplanation:
				"Keep both. Anything present on only one side is kept, and where both changed the same thing, the most recent edit wins.",
			replace: "Replace",
			replaceExplanation:
				"Discard what is on this device and download the account's data instead.",
			replaceWarning:
				"The tasks, projects and tags currently on this device will be deleted. A backup is saved to your computer first.",
			backupSaving: "Saving a backup…",
			backupFailed: "The backup could not be saved, so nothing was replaced. Try again or choose Merge.",
			applying: "Applying…",
			confirm: "Continue",
			failed: "Could not apply your choice. Nothing was changed.",
		},
	},
```

- [ ] **Step 4: Ajouter le bloc FR**

Dans `src/i18n/locales/fr.ts`, ajouter le bloc miroir (**mêmes clés, exactement**) :

```ts
	sync: {
		title: "Synchronisation",
		tab: "Synchronisation",
		intro:
			"Synchronisez vos tâches entre vos appareils via un serveur de votre choix. Tout est chiffré sur cet appareil avant d'en sortir — le serveur ne voit jamais vos données.",
		serverUrl: "Adresse du serveur",
		serverUrlPlaceholder: "https://sync.exemple.com",
		testConnection: "Tester la connexion",
		testing: "Test en cours…",
		serverFound: "{{name}} {{version}} — compatible",
		urlError: {
			empty: "Saisissez une adresse de serveur.",
			malformed: "Cette adresse ne semble pas valide.",
			insecure:
				"Une adresse non chiffrée (http://) n'est autorisée que pour localhost, 127.0.0.1 et les noms en .local.",
		},
		insecureWarning:
			"Cette connexion n'est pas chiffrée. Ne le faites que sur un réseau de confiance.",
		unreachable: "Impossible de joindre ce serveur. Vérifiez l'adresse et votre connexion.",
		notUsagi: "Cette adresse répond, mais ce n'est pas un serveur usagi.",
		protocolMismatch:
			"Ce serveur parle la version {{server}} du protocole, cette application la version {{client}}. Mettez à jour le plus ancien des deux — la synchronisation est désactivée en attendant.",
		signIn: "Se connecter",
		signingIn: "Connexion…",
		createAccount: "Créer un compte",
		creatingAccount: "Création du compte…",
		registrationClosed:
			"Ce serveur n'accepte pas de nouveaux comptes. Connectez-vous avec un compte existant.",
		email: "Adresse e-mail",
		password: "Mot de passe",
		inviteToken: "Jeton d'invitation",
		inviteTokenHint: "Facultatif — seulement si le propriétaire du serveur vous en a fourni un.",
		signInFailed: "Adresse e-mail ou mot de passe incorrect.",
		registerFailed:
			"Impossible de créer le compte. Le jeton d'invitation est peut-être déjà utilisé ou expiré.",
		passwordHint:
			"Choisissez un mot de passe solide. Il chiffre tout, et il n'est jamais transmis au serveur — personne ne peut le réinitialiser à votre place.",
		recovery: {
			title: "Votre clé de récupération",
			intro:
				"Ces 24 mots sont le seul moyen de retrouver votre compte si vous oubliez votre mot de passe. Notez-les et conservez-les en lieu sûr. Ils sont affichés une seule fois et ne sont jamais enregistrés.",
			copy: "Copier",
			copied: "Copié",
			continue: "Je les ai notés",
			confirmTitle: "Confirmez votre clé de récupération",
			confirmIntro: "Saisissez ces trois mots pour confirmer que vous avez bien noté la clé.",
			wordLabel: "Mot {{position}}",
			wrongWords: "Ces mots ne correspondent pas. Vérifiez votre copie et réessayez.",
			back: "Afficher à nouveau la clé",
			confirm: "Confirmer",
		},
		status: {
			label: "État",
			idle: "À jour",
			syncing: "Synchronisation…",
			locked: "En pause — déverrouillez pour reprendre",
			awaitingFirstSync: "En attente de votre réponse",
			reauthRequired: "Reconnexion nécessaire",
			protocolMismatch: "Serveur incompatible",
		},
		lastSync: "Dernière synchronisation",
		never: "Jamais",
		syncNow: "Synchroniser maintenant",
		account: "Compte",
		devices: {
			title: "Appareils",
			thisDevice: "Cet appareil",
			lastSeen: "Vu {{when}}",
			never: "Jamais vu",
			revoke: "Révoquer",
			revoking: "Révocation…",
			revokeConfirmTitle: "Révoquer cet appareil ?",
			revokeConfirmMessage:
				"{{name}} sera déconnecté et cessera de se synchroniser. Ses données locales y restent.",
			revokeFailed: "Impossible de révoquer cet appareil.",
			loadFailed: "Impossible de charger vos appareils.",
			empty: "Aucun autre appareil.",
		},
		disconnect: "Déconnecter cet appareil",
		disconnecting: "Déconnexion…",
		disconnectConfirmTitle: "Se déconnecter de ce serveur ?",
		disconnectConfirmMessage:
			"La synchronisation s'arrête et cet appareil oublie le compte. Vos tâches restent sur cet appareil, intactes.",
		changeServerHint: "Déconnectez-vous d'abord pour utiliser un autre serveur.",
		unlock: {
			title: "Déverrouiller la synchronisation",
			intro:
				"Votre mot de passe est nécessaire pour déchiffrer ce que le serveur envoie. Il n'est jamais conservé, il est donc redemandé après chaque redémarrage.",
			submit: "Déverrouiller",
			unlocking: "Déverrouillage…",
			failed: "Mot de passe incorrect.",
		},
		banner: {
			locked: "La synchronisation est en pause — déverrouillez pour reprendre.",
			reauthRequired: "La synchronisation nécessite une reconnexion.",
			protocolMismatch:
				"Synchronisation désactivée : ce serveur n'est pas compatible avec cette version de l'application.",
			action: "Ouvrir les réglages",
		},
		firstSync: {
			title: "Ce compte contient déjà des données",
			intro:
				"Cet appareil contient des tâches, et le compte aussi. Choisissez comment les combiner — la question n'est posée qu'une fois.",
			merge: "Fusionner",
			mergeExplanation:
				"Tout conserver. Ce qui n'existe que d'un côté est gardé, et lorsque les deux ont modifié la même chose, la modification la plus récente l'emporte.",
			replace: "Remplacer",
			replaceExplanation:
				"Abandonner le contenu de cet appareil et télécharger à la place les données du compte.",
			replaceWarning:
				"Les tâches, projets et tags actuellement sur cet appareil seront supprimés. Une sauvegarde est enregistrée sur votre ordinateur au préalable.",
			backupSaving: "Enregistrement d'une sauvegarde…",
			backupFailed:
				"La sauvegarde n'a pas pu être enregistrée, rien n'a donc été remplacé. Réessayez ou choisissez Fusionner.",
			applying: "Application…",
			confirm: "Continuer",
			failed: "Impossible d'appliquer votre choix. Rien n'a été modifié.",
		},
	},
```

- [ ] **Step 5: Vert complet**

```bash
pnpm test:run src/i18n/sync-keys.test.ts
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 731 tests**.

- [ ] **Step 6: Commit**

```bash
git add src/i18n/locales/en.ts src/i18n/locales/fr.ts src/i18n/sync-keys.test.ts
git commit -m "feat: :globe_with_meridians: add the sync interface strings in English and French"
```

---

# PARTIE C — Écrans (tâches 8 à 14)

**Conventions communes à toutes les tâches UI.** Les respecter évite de les répéter à chaque tâche :

- Fichiers dans `src/components/sync/`. Un composant par fichier, props locales déclarées dans le fichier du composant (CLAUDE.md).
- `import { useTranslation } from "react-i18next";` puis `const { t } = useTranslation();`. **Zéro chaîne en dur.**
- Erreurs en ligne : `<p className="text-xs text-destructive">{message}</p>`. Avertissements avec icône : `<p className="flex items-center gap-1.5 text-xs text-destructive"><AlertTriangle className="h-3.5 w-3.5 shrink-0" />…</p>` (`lucide-react`, déjà utilisé).
- Primitives : `@/components/ui/button`, `input`, `dialog`, `switch`, `badge`. Pas de `Alert`, pas de toasts — ils n'existent pas ici.
- **Chaque composant est une fonction pure de ses props** : toute I/O (réseau, base, coffre) arrive par prop injectée. C'est ce qui les rend testables sans runtime Tauri.
- Tests : `src/components/sync/<Nom>.test.tsx`, environnement par défaut, `import "@/i18n";` en tête, `render` + `userEvent.setup()`, requêtes par rôle et nom accessible. Les libellés existant en EN et FR, matcher avec une regex sur les deux : `screen.getByRole("button", { name: /sign in|se connecter/i })`.

---

### Tâche 8 : Saisie de l'URL et test de connexion (§6.2)

**Files:**
- Create: `src/components/sync/ServerUrlForm.tsx`
- Test: `src/components/sync/ServerUrlForm.test.tsx`

**Interfaces:**
- Consomme : `normalizeServerUrl` (tâche 4) ; `ServerInfo`, `CLIENT_PROTOCOL_VERSION` (`src/sync/types.ts`).
- Produit :
```ts
export interface ServerUrlFormProps {
	/** Injected so tests need no network; the panel passes getServerInfo bound to fetch. */
	probe: (url: string) => Promise<ServerInfo>;
	/** Called with the normalised URL and the server's answer once it checks out. */
	onVerified: (url: string, info: ServerInfo) => void;
	disabled?: boolean;
	disabledHint?: string;
}
export function ServerUrlForm(props: ServerUrlFormProps): JSX.Element;
```
Consommé par la tâche 14.

Le §6.2 veut la vérification **avant toute saisie d'identifiants** : que l'URL pointe vers un usagi-server, et que le protocole soit compatible.

- [ ] **Step 1: Écrire les tests d'échec**

Créer `src/components/sync/ServerUrlForm.test.tsx` :

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import "@/i18n";
import type { ServerInfo } from "@/sync/types";
import { ServerUrlForm } from "./ServerUrlForm";

const OK: ServerInfo = {
	name: "usagi-server",
	version: "1.2.0",
	protocolVersion: 1,
	registrationEnabled: true,
	minClientVersion: "0.1.0",
};

function setup(overrides: Partial<React.ComponentProps<typeof ServerUrlForm>> = {}) {
	const probe = overrides.probe ?? vi.fn(async () => OK);
	const onVerified = overrides.onVerified ?? vi.fn();
	render(<ServerUrlForm probe={probe} onVerified={onVerified} {...overrides} />);
	return { probe, onVerified, user: userEvent.setup() };
}

const urlField = () => screen.getByLabelText(/server address|adresse du serveur/i);
const testButton = () => screen.getByRole("button", { name: /test connection|tester la connexion/i });

describe("ServerUrlForm", () => {
	it("ne sonde pas le réseau tant que l'URL est refusée", async () => {
		const { probe, user } = setup();
		await user.type(urlField(), "http://sync.example.com");
		await user.click(testButton());
		expect(probe).not.toHaveBeenCalled();
		expect(screen.getByText(/only allowed for localhost|autorisée que pour localhost/i)).toBeInTheDocument();
	});

	it("normalise l'URL avant de sonder", async () => {
		const { probe, user } = setup();
		await user.type(urlField(), "sync.example.com/");
		await user.click(testButton());
		expect(probe).toHaveBeenCalledWith("https://sync.example.com");
	});

	it("annonce le serveur trouvé et remonte l'URL normalisée", async () => {
		const onVerified = vi.fn();
		const { user } = setup({ onVerified });
		await user.type(urlField(), "https://sync.example.com");
		await user.click(testButton());
		expect(await screen.findByText(/usagi-server 1\.2\.0/)).toBeInTheDocument();
		expect(onVerified).toHaveBeenCalledWith("https://sync.example.com", OK);
	});

	it("avertit sans bloquer sur un hôte local en http", async () => {
		const onVerified = vi.fn();
		const { probe, user } = setup({ onVerified });
		await user.type(urlField(), "http://localhost:3000");
		await user.click(testButton());
		expect(screen.getByText(/not encrypted|n'est pas chiffrée/i)).toBeInTheDocument();
		expect(probe).toHaveBeenCalledWith("http://localhost:3000");
		expect(onVerified).toHaveBeenCalled();
	});

	it("distingue un serveur injoignable d'une réponse non-usagi", async () => {
		const { user } = setup({ probe: vi.fn(async () => { throw new Error("offline"); }) });
		await user.type(urlField(), "https://sync.example.com");
		await user.click(testButton());
		expect(await screen.findByText(/could not reach|impossible de joindre/i)).toBeInTheDocument();
	});

	it("refuse une réponse qui n'est pas un usagi-server", async () => {
		const onVerified = vi.fn();
		const { user } = setup({
			onVerified,
			probe: vi.fn(async () => ({ hello: "world" }) as unknown as ServerInfo),
		});
		await user.type(urlField(), "https://sync.example.com");
		await user.click(testButton());
		expect(await screen.findByText(/not a usagi server|pas un serveur usagi/i)).toBeInTheDocument();
		expect(onVerified).not.toHaveBeenCalled();
	});

	it("refuse un protocole incompatible en nommant les deux versions", async () => {
		const onVerified = vi.fn();
		const { user } = setup({
			onVerified,
			probe: vi.fn(async () => ({ ...OK, protocolVersion: 2 })),
		});
		await user.type(urlField(), "https://sync.example.com");
		await user.click(testButton());
		expect(await screen.findByText(/protocol version 2|version 2 du protocole/i)).toBeInTheDocument();
		expect(onVerified).not.toHaveBeenCalled();
	});

	it("verrouille la saisie et explique pourquoi quand disabled", () => {
		setup({ disabled: true, disabledHint: "Disconnect first" });
		expect(urlField()).toBeDisabled();
		expect(screen.getByText("Disconnect first")).toBeInTheDocument();
	});
});
```

- [ ] **Step 2: Lancer et constater le rouge**

```bash
pnpm test:run src/components/sync/ServerUrlForm.test.tsx
```
Attendu : ÉCHEC — module introuvable.

- [ ] **Step 3: Implémenter**

Créer `src/components/sync/ServerUrlForm.tsx` :

```tsx
import { AlertTriangle, CheckCircle2 } from "lucide-react";
import { useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { normalizeServerUrl, type ServerUrlError } from "@/sync/server-url";
import { CLIENT_PROTOCOL_VERSION, type ServerInfo } from "@/sync/types";

export interface ServerUrlFormProps {
	probe: (url: string) => Promise<ServerInfo>;
	onVerified: (url: string, info: ServerInfo) => void;
	disabled?: boolean;
	disabledHint?: string;
}

type Probe =
	| { kind: "idle" }
	| { kind: "testing" }
	| { kind: "ok"; info: ServerInfo }
	| { kind: "unreachable" }
	| { kind: "not-usagi" }
	| { kind: "protocol"; server: number };

/** A reachable non-usagi host answers 200 with arbitrary JSON, so the shape is
 * checked rather than trusted — otherwise protocolVersion would be undefined
 * and the mismatch branch would report "version undefined". */
function isServerInfo(value: unknown): value is ServerInfo {
	if (!value || typeof value !== "object") return false;
	const info = value as Partial<ServerInfo>;
	return (
		typeof info.name === "string" &&
		typeof info.version === "string" &&
		typeof info.protocolVersion === "number" &&
		typeof info.registrationEnabled === "boolean"
	);
}

export function ServerUrlForm({
	probe,
	onVerified,
	disabled = false,
	disabledHint,
}: ServerUrlFormProps) {
	const { t } = useTranslation();
	const [raw, setRaw] = useState("");
	const [urlError, setUrlError] = useState<ServerUrlError | null>(null);
	const [insecure, setInsecure] = useState(false);
	const [state, setState] = useState<Probe>({ kind: "idle" });

	async function handleTest() {
		const parsed = normalizeServerUrl(raw);
		setUrlError(parsed.error);
		setInsecure(parsed.insecureWarning);
		setState({ kind: "idle" });
		if (!parsed.url) return;

		setState({ kind: "testing" });
		let info: unknown;
		try {
			info = await probe(parsed.url);
		} catch {
			// §6.2 wants three distinguishable failures; this is "unreachable".
			setState({ kind: "unreachable" });
			return;
		}
		if (!isServerInfo(info)) {
			setState({ kind: "not-usagi" });
			return;
		}
		if (info.protocolVersion !== CLIENT_PROTOCOL_VERSION) {
			setState({ kind: "protocol", server: info.protocolVersion });
			return;
		}
		setState({ kind: "ok", info });
		onVerified(parsed.url, info);
	}

	return (
		<div className="flex flex-col gap-2">
			<label className="text-sm" htmlFor="sync-server-url">
				{t("sync.serverUrl")}
			</label>
			<div className="flex gap-2">
				<Input
					id="sync-server-url"
					value={raw}
					disabled={disabled}
					placeholder={t("sync.serverUrlPlaceholder")}
					onChange={(e) => {
						setRaw(e.target.value);
						setUrlError(null);
						setState({ kind: "idle" });
					}}
				/>
				<Button
					type="button"
					variant="outline"
					disabled={disabled || state.kind === "testing"}
					onClick={handleTest}
				>
					{state.kind === "testing" ? t("sync.testing") : t("sync.testConnection")}
				</Button>
			</div>

			{disabled && disabledHint && (
				<p className="text-xs text-muted-foreground">{disabledHint}</p>
			)}
			{urlError && (
				<p className="text-xs text-destructive">{t(`sync.urlError.${urlError}`)}</p>
			)}
			{insecure && !urlError && (
				<p className="flex items-center gap-1.5 text-xs text-destructive">
					<AlertTriangle className="h-3.5 w-3.5 shrink-0" />
					{t("sync.insecureWarning")}
				</p>
			)}
			{state.kind === "ok" && (
				<p className="flex items-center gap-1.5 text-xs text-muted-foreground">
					<CheckCircle2 className="h-3.5 w-3.5 shrink-0" />
					{t("sync.serverFound", { name: state.info.name, version: state.info.version })}
				</p>
			)}
			{state.kind === "unreachable" && (
				<p className="text-xs text-destructive">{t("sync.unreachable")}</p>
			)}
			{state.kind === "not-usagi" && (
				<p className="text-xs text-destructive">{t("sync.notUsagi")}</p>
			)}
			{state.kind === "protocol" && (
				<p className="text-xs text-destructive">
					{t("sync.protocolMismatch", {
						server: state.server,
						client: CLIENT_PROTOCOL_VERSION,
					})}
				</p>
			)}
		</div>
	);
}
```

- [ ] **Step 4: Vert complet**

```bash
pnpm test:run src/components/sync/ServerUrlForm.test.tsx
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 739 tests**.

- [ ] **Step 5: Commit**

```bash
git add src/components/sync/ServerUrlForm.tsx src/components/sync/ServerUrlForm.test.tsx
git commit -m "feat: :satellite: negotiate the server before any credential is typed"
```

- [ ] **Step 6: [SABOTAGE] Vérifier que la forme de la réponse est contrôlée**

Dans `src/components/sync/ServerUrlForm.tsx`, rendre `isServerInfo` permissive :

```tsx
function isServerInfo(value: unknown): value is ServerInfo {
	return true;
}
```

```bash
git diff --stat -- src/components/sync/ServerUrlForm.tsx
grep -n "function isServerInfo" -A 2 src/components/sync/ServerUrlForm.tsx
```
Attendu : fichier listé ; le `grep` montre le corps réduit à `return true;`.

> **Observabilité :** rien d'autre ne valide la forme — `probe` est injectée et rend l'objet tel quel. Sans le contrôle, `{ hello: "world" }` passe, `protocolVersion` vaut `undefined ≠ 1`, et l'écran affiche l'erreur de protocole au lieu de « pas un serveur usagi ».

```bash
pnpm test:run src/components/sync/ServerUrlForm.test.tsx
```
Attendu : ÉCHEC du test « refuse une réponse qui n'est pas un usagi-server ».

- [ ] **Step 7: Restaurer et re-vérifier**

```bash
git checkout -- src/components/sync/ServerUrlForm.tsx
pnpm test:run src/components/sync/ServerUrlForm.test.tsx
```
Attendu : PASS.

---

### Tâche 9 : Formulaire de connexion

**Files:**
- Create: `src/components/sync/SignInForm.tsx`
- Test: `src/components/sync/SignInForm.test.tsx`

**Interfaces:**
- Produit :
```ts
export interface SignInFormProps {
	onSubmit: (input: { email: string; password: string }) => Promise<void>;
	onSwitchToRegister?: () => void;
}
export function SignInForm(props: SignInFormProps): JSX.Element;
```
Consommé par la tâche 14.

- [ ] **Step 1: Écrire les tests d'échec**

Créer `src/components/sync/SignInForm.test.tsx` :

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import "@/i18n";
import { SignInForm } from "./SignInForm";

const email = () => screen.getByLabelText(/email|adresse e-mail/i);
const password = () => screen.getByLabelText(/password|mot de passe/i);
const submit = () => screen.getByRole("button", { name: /^sign in$|^se connecter$/i });

describe("SignInForm", () => {
	it("n'active la soumission qu'avec les deux champs remplis", async () => {
		const user = userEvent.setup();
		render(<SignInForm onSubmit={vi.fn()} />);
		expect(submit()).toBeDisabled();
		await user.type(email(), "a@example.com");
		expect(submit()).toBeDisabled();
		await user.type(password(), "hunter2hunter2");
		expect(submit()).toBeEnabled();
	});

	it("transmet les identifiants saisis", async () => {
		const user = userEvent.setup();
		const onSubmit = vi.fn(async () => {});
		render(<SignInForm onSubmit={onSubmit} />);
		await user.type(email(), "a@example.com");
		await user.type(password(), "hunter2hunter2");
		await user.click(submit());
		expect(onSubmit).toHaveBeenCalledWith({
			email: "a@example.com",
			password: "hunter2hunter2",
		});
	});

	it("masque le mot de passe", async () => {
		render(<SignInForm onSubmit={vi.fn()} />);
		expect(password()).toHaveAttribute("type", "password");
	});

	it("affiche une erreur explicite quand la connexion échoue", async () => {
		const user = userEvent.setup();
		render(<SignInForm onSubmit={vi.fn(async () => { throw new Error("401"); })} />);
		await user.type(email(), "a@example.com");
		await user.type(password(), "wrong-password");
		await user.click(submit());
		expect(await screen.findByText(/wrong email or password|incorrect/i)).toBeInTheDocument();
	});

	it("ne soumet pas deux fois pendant que la requête est en vol", async () => {
		const user = userEvent.setup();
		let resolve: () => void = () => {};
		const onSubmit = vi.fn(() => new Promise<void>((r) => { resolve = r; }));
		render(<SignInForm onSubmit={onSubmit} />);
		await user.type(email(), "a@example.com");
		await user.type(password(), "hunter2hunter2");
		await user.click(submit());
		expect(submit()).toBeDisabled();
		await user.click(submit());
		expect(onSubmit).toHaveBeenCalledTimes(1);
		resolve();
	});
});
```

- [ ] **Step 2: Lancer et constater le rouge**

```bash
pnpm test:run src/components/sync/SignInForm.test.tsx
```
Attendu : ÉCHEC — module introuvable.

- [ ] **Step 3: Implémenter**

Créer `src/components/sync/SignInForm.tsx` :

```tsx
import { useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export interface SignInFormProps {
	onSubmit: (input: { email: string; password: string }) => Promise<void>;
	onSwitchToRegister?: () => void;
}

export function SignInForm({ onSubmit, onSwitchToRegister }: SignInFormProps) {
	const { t } = useTranslation();
	const [email, setEmail] = useState("");
	const [password, setPassword] = useState("");
	const [busy, setBusy] = useState(false);
	const [failed, setFailed] = useState(false);

	const ready = email.trim() !== "" && password !== "" && !busy;

	async function handleSubmit(e: React.FormEvent) {
		e.preventDefault();
		if (!ready) return;
		setBusy(true);
		setFailed(false);
		try {
			await onSubmit({ email: email.trim(), password });
		} catch {
			// Every sign-in failure reads the same to the user: a 401 and an
			// unreachable server both mean "you are not in", and distinguishing
			// them here would leak which emails exist.
			setFailed(true);
		} finally {
			setBusy(false);
		}
	}

	return (
		<form onSubmit={handleSubmit} className="flex flex-col gap-3">
			<div className="flex flex-col gap-1.5">
				<label className="text-sm" htmlFor="sync-signin-email">
					{t("sync.email")}
				</label>
				<Input
					id="sync-signin-email"
					type="email"
					autoComplete="username"
					value={email}
					onChange={(e) => setEmail(e.target.value)}
				/>
			</div>
			<div className="flex flex-col gap-1.5">
				<label className="text-sm" htmlFor="sync-signin-password">
					{t("sync.password")}
				</label>
				<Input
					id="sync-signin-password"
					type="password"
					autoComplete="current-password"
					value={password}
					onChange={(e) => setPassword(e.target.value)}
				/>
			</div>

			{failed && <p className="text-xs text-destructive">{t("sync.signInFailed")}</p>}

			<div className="flex items-center justify-between gap-2">
				{onSwitchToRegister ? (
					<Button type="button" variant="ghost" size="sm" onClick={onSwitchToRegister}>
						{t("sync.createAccount")}
					</Button>
				) : (
					<span />
				)}
				<Button type="submit" disabled={!ready}>
					{busy ? t("sync.signingIn") : t("sync.signIn")}
				</Button>
			</div>
		</form>
	);
}
```

- [ ] **Step 4: Vert complet**

```bash
pnpm test:run src/components/sync/SignInForm.test.tsx
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 744 tests**.

- [ ] **Step 5: Commit**

```bash
git add src/components/sync/SignInForm.tsx src/components/sync/SignInForm.test.tsx
git commit -m "feat: :key: add the sync sign-in form"
```

---

### Tâche 10 : Inscription et clé de récupération de 24 mots (§2.1)

La tâche la plus délicate : la clé de récupération est du vrai matériel cryptographique et le compte est irrécupérable sans elle.

**Files:**
- Create: `src/components/sync/recovery-confirmation.ts`
- Create: `src/components/sync/RecoveryPhraseStep.tsx`
- Create: `src/components/sync/RegisterForm.tsx`
- Test: `src/components/sync/recovery-confirmation.test.ts`
- Test: `src/components/sync/RecoveryPhraseStep.test.tsx`
- Test: `src/components/sync/RegisterForm.test.tsx`

**Interfaces:**
- Produit :
```ts
// recovery-confirmation.ts
export function pickConfirmationPositions(
	wordCount: number,
	howMany: number,
	random?: () => number,
): number[];   // positions 1-indexées, croissantes, distinctes

// RecoveryPhraseStep.tsx
export interface RecoveryPhraseStepProps {
	phrase: string;                    // 24 mots séparés par des espaces
	onConfirmed: () => void;           // l'appelant efface alors la phrase de son état
	random?: () => number;             // injecté pour rendre le tirage déterministe en test
}

// RegisterForm.tsx
export interface RegisterFormProps {
	onSubmit: (input: { email: string; password: string; inviteToken?: string }) => Promise<string>;  // rend recoveryPhrase
	onComplete: () => void;
	onSwitchToSignIn?: () => void;
	random?: () => number;
}
```
Consommés par la tâche 14.

- [ ] **Step 1: Écrire le test du tirage**

Créer `src/components/sync/recovery-confirmation.test.ts` :

```ts
import { describe, expect, it } from "vitest";
import { pickConfirmationPositions } from "./recovery-confirmation";

describe("pickConfirmationPositions", () => {
	it("rend le nombre demandé de positions distinctes et croissantes", () => {
		const positions = pickConfirmationPositions(24, 3);
		expect(positions).toHaveLength(3);
		expect(new Set(positions).size).toBe(3);
		expect([...positions].sort((a, b) => a - b)).toEqual(positions);
	});

	it("reste dans les bornes 1..wordCount", () => {
		for (let i = 0; i < 200; i++) {
			for (const p of pickConfirmationPositions(24, 3)) {
				expect(p).toBeGreaterThanOrEqual(1);
				expect(p).toBeLessThanOrEqual(24);
			}
		}
	});

	it("est déterministe quand on injecte le générateur", () => {
		const constant = () => 0;
		expect(pickConfirmationPositions(24, 3, constant)).toEqual([1, 2, 3]);
	});

	it("couvre l'ensemble des positions sur de nombreux tirages", () => {
		const seen = new Set<number>();
		for (let i = 0; i < 2000; i++) {
			for (const p of pickConfirmationPositions(24, 3)) seen.add(p);
		}
		// Un tirage biaisé vers le début laisserait la fin inatteignable.
		expect(seen.size).toBe(24);
	});

	it("ne boucle pas si on demande autant de positions que de mots", () => {
		expect(pickConfirmationPositions(3, 3)).toEqual([1, 2, 3]);
	});

	it("plafonne au nombre de mots disponibles", () => {
		expect(pickConfirmationPositions(2, 5)).toEqual([1, 2]);
	});
});
```

- [ ] **Step 2: Constater le rouge, puis implémenter**

```bash
pnpm test:run src/components/sync/recovery-confirmation.test.ts
```
Attendu : ÉCHEC — module introuvable.

Créer `src/components/sync/recovery-confirmation.ts` :

```ts
/**
 * Positions (1-indexed) the user must retype to prove they saved the recovery
 * key. Random rather than fixed: fixed positions would let a user note only
 * those three words and still get through.
 *
 * `random` is injected so tests are deterministic — Math.random is the default.
 */
export function pickConfirmationPositions(
	wordCount: number,
	howMany: number,
	random: () => number = Math.random,
): number[] {
	const target = Math.min(howMany, wordCount);
	const picked = new Set<number>();
	// Draw, then top up in order: a pure rejection loop with a constant `random`
	// (as tests inject) would never terminate.
	while (picked.size < target) {
		const next = Math.floor(random() * wordCount) + 1;
		if (picked.has(next)) {
			for (let candidate = 1; candidate <= wordCount; candidate++) {
				if (!picked.has(candidate)) {
					picked.add(candidate);
					break;
				}
			}
		} else {
			picked.add(next);
		}
	}
	return [...picked].sort((a, b) => a - b);
}
```

```bash
pnpm test:run src/components/sync/recovery-confirmation.test.ts
```
Attendu : PASS (6 tests).

- [ ] **Step 3: Écrire les tests de l'écran de clé**

Créer `src/components/sync/RecoveryPhraseStep.test.tsx` :

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import "@/i18n";
import { RecoveryPhraseStep } from "./RecoveryPhraseStep";

const WORDS = Array.from({ length: 24 }, (_, i) => `word${i + 1}`);
const PHRASE = WORDS.join(" ");

/** Constant generator ⇒ positions 1, 2, 3 (see pickConfirmationPositions). */
const FIXED = () => 0;

describe("RecoveryPhraseStep", () => {
	it("affiche les 24 mots", () => {
		render(<RecoveryPhraseStep phrase={PHRASE} onConfirmed={vi.fn()} random={FIXED} />);
		for (const word of WORDS) {
			expect(screen.getByText(word)).toBeInTheDocument();
		}
	});

	it("ne confirme pas tant que l'utilisateur n'a pas déclaré avoir noté la clé", async () => {
		const onConfirmed = vi.fn();
		const user = userEvent.setup();
		render(<RecoveryPhraseStep phrase={PHRASE} onConfirmed={onConfirmed} random={FIXED} />);
		expect(screen.queryByLabelText(/word 1|mot 1/i)).not.toBeInTheDocument();
		await user.click(screen.getByRole("button", { name: /written them down|je les ai notés/i }));
		expect(onConfirmed).not.toHaveBeenCalled();
		expect(screen.getByLabelText(/word 1|mot 1/i)).toBeInTheDocument();
	});

	it("refuse une ressaisie erronée et ne confirme pas", async () => {
		const onConfirmed = vi.fn();
		const user = userEvent.setup();
		render(<RecoveryPhraseStep phrase={PHRASE} onConfirmed={onConfirmed} random={FIXED} />);
		await user.click(screen.getByRole("button", { name: /written them down|je les ai notés/i }));
		await user.type(screen.getByLabelText(/word 1|mot 1/i), "word1");
		await user.type(screen.getByLabelText(/word 2|mot 2/i), "nope");
		await user.type(screen.getByLabelText(/word 3|mot 3/i), "word3");
		await user.click(screen.getByRole("button", { name: /^confirm$|^confirmer$/i }));
		expect(onConfirmed).not.toHaveBeenCalled();
		expect(screen.getByText(/do not match|ne correspondent pas/i)).toBeInTheDocument();
	});

	it("confirme sur une ressaisie exacte, en tolérant casse et espaces", async () => {
		const onConfirmed = vi.fn();
		const user = userEvent.setup();
		render(<RecoveryPhraseStep phrase={PHRASE} onConfirmed={onConfirmed} random={FIXED} />);
		await user.click(screen.getByRole("button", { name: /written them down|je les ai notés/i }));
		await user.type(screen.getByLabelText(/word 1|mot 1/i), " WORD1 ");
		await user.type(screen.getByLabelText(/word 2|mot 2/i), "word2");
		await user.type(screen.getByLabelText(/word 3|mot 3/i), "Word3");
		await user.click(screen.getByRole("button", { name: /^confirm$|^confirmer$/i }));
		expect(onConfirmed).toHaveBeenCalledTimes(1);
	});

	it("permet de revoir la clé avant de confirmer", async () => {
		const user = userEvent.setup();
		render(<RecoveryPhraseStep phrase={PHRASE} onConfirmed={vi.fn()} random={FIXED} />);
		await user.click(screen.getByRole("button", { name: /written them down|je les ai notés/i }));
		await user.click(screen.getByRole("button", { name: /show the key again|afficher à nouveau/i }));
		expect(screen.getByText("word24")).toBeInTheDocument();
	});
});
```

- [ ] **Step 4: Implémenter l'écran de clé**

Créer `src/components/sync/RecoveryPhraseStep.tsx` :

```tsx
import { useMemo, useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { pickConfirmationPositions } from "./recovery-confirmation";

export interface RecoveryPhraseStepProps {
	phrase: string;
	onConfirmed: () => void;
	random?: () => number;
}

const WORDS_TO_CONFIRM = 3;

export function RecoveryPhraseStep({
	phrase,
	onConfirmed,
	random,
}: RecoveryPhraseStepProps) {
	const { t } = useTranslation();
	const words = useMemo(() => phrase.trim().split(/\s+/), [phrase]);
	// Drawn once per mount: re-drawing on every keystroke would move the target
	// while the user types into it.
	const positions = useMemo(
		() => pickConfirmationPositions(words.length, WORDS_TO_CONFIRM, random),
		[words.length, random],
	);

	const [confirming, setConfirming] = useState(false);
	const [answers, setAnswers] = useState<Record<number, string>>({});
	const [wrong, setWrong] = useState(false);
	const [copied, setCopied] = useState(false);

	async function handleCopy() {
		try {
			await navigator.clipboard.writeText(phrase);
			setCopied(true);
		} catch {
			// Clipboard access can be refused; the words are on screen anyway.
		}
	}

	function handleConfirm() {
		const ok = positions.every(
			(position) =>
				(answers[position] ?? "").trim().toLowerCase() ===
				words[position - 1].toLowerCase(),
		);
		if (!ok) {
			setWrong(true);
			return;
		}
		onConfirmed();
	}

	if (!confirming) {
		return (
			<div className="flex flex-col gap-3">
				<p className="text-sm font-medium">{t("sync.recovery.title")}</p>
				<p className="text-xs text-muted-foreground">{t("sync.recovery.intro")}</p>
				<ol className="grid grid-cols-3 gap-x-4 gap-y-1 rounded-md border border-border p-3 text-xs">
					{words.map((word, index) => (
						<li key={`${index}-${word}`} className="flex gap-2">
							<span className="w-5 shrink-0 text-right text-muted-foreground">
								{index + 1}
							</span>
							<span className="font-mono">{word}</span>
						</li>
					))}
				</ol>
				<div className="flex justify-between gap-2">
					<Button type="button" variant="outline" size="sm" onClick={handleCopy}>
						{copied ? t("sync.recovery.copied") : t("sync.recovery.copy")}
					</Button>
					<Button type="button" onClick={() => setConfirming(true)}>
						{t("sync.recovery.continue")}
					</Button>
				</div>
			</div>
		);
	}

	return (
		<div className="flex flex-col gap-3">
			<p className="text-sm font-medium">{t("sync.recovery.confirmTitle")}</p>
			<p className="text-xs text-muted-foreground">{t("sync.recovery.confirmIntro")}</p>
			{positions.map((position) => (
				<div key={position} className="flex flex-col gap-1.5">
					<label className="text-sm" htmlFor={`recovery-word-${position}`}>
						{t("sync.recovery.wordLabel", { position })}
					</label>
					<Input
						id={`recovery-word-${position}`}
						autoComplete="off"
						value={answers[position] ?? ""}
						onChange={(e) => {
							setAnswers((prev) => ({ ...prev, [position]: e.target.value }));
							setWrong(false);
						}}
					/>
				</div>
			))}
			{wrong && <p className="text-xs text-destructive">{t("sync.recovery.wrongWords")}</p>}
			<div className="flex justify-between gap-2">
				<Button
					type="button"
					variant="ghost"
					size="sm"
					onClick={() => {
						setConfirming(false);
						setWrong(false);
					}}
				>
					{t("sync.recovery.back")}
				</Button>
				<Button type="button" onClick={handleConfirm}>
					{t("sync.recovery.confirm")}
				</Button>
			</div>
		</div>
	);
}
```

```bash
pnpm test:run src/components/sync/RecoveryPhraseStep.test.tsx
```
Attendu : PASS (5 tests).

- [ ] **Step 5: Écrire les tests du formulaire d'inscription**

Créer `src/components/sync/RegisterForm.test.tsx` :

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import "@/i18n";
import { RegisterForm } from "./RegisterForm";

const PHRASE = Array.from({ length: 24 }, (_, i) => `word${i + 1}`).join(" ");
const FIXED = () => 0;

const email = () => screen.getByLabelText(/email|adresse e-mail/i);
const password = () => screen.getByLabelText(/^password$|^mot de passe$/i);
const submit = () => screen.getByRole("button", { name: /^create account$|^créer un compte$/i });

describe("RegisterForm", () => {
	it("crée le compte puis montre la clé de récupération", async () => {
		const user = userEvent.setup();
		const onSubmit = vi.fn(async () => PHRASE);
		render(<RegisterForm onSubmit={onSubmit} onComplete={vi.fn()} random={FIXED} />);
		await user.type(email(), "a@example.com");
		await user.type(password(), "correct horse battery staple");
		await user.click(submit());
		expect(onSubmit).toHaveBeenCalledWith({
			email: "a@example.com",
			password: "correct horse battery staple",
		});
		expect(await screen.findByText("word24")).toBeInTheDocument();
	});

	it("transmet le jeton d'invitation quand il est renseigné", async () => {
		const user = userEvent.setup();
		const onSubmit = vi.fn(async () => PHRASE);
		render(<RegisterForm onSubmit={onSubmit} onComplete={vi.fn()} random={FIXED} />);
		await user.type(email(), "a@example.com");
		await user.type(password(), "correct horse battery staple");
		await user.type(screen.getByLabelText(/invite token|jeton d'invitation/i), "inv-123");
		await user.click(submit());
		expect(onSubmit).toHaveBeenCalledWith({
			email: "a@example.com",
			password: "correct horse battery staple",
			inviteToken: "inv-123",
		});
	});

	it("ne termine qu'après confirmation de la clé", async () => {
		const user = userEvent.setup();
		const onComplete = vi.fn();
		render(
			<RegisterForm onSubmit={vi.fn(async () => PHRASE)} onComplete={onComplete} random={FIXED} />,
		);
		await user.type(email(), "a@example.com");
		await user.type(password(), "correct horse battery staple");
		await user.click(submit());
		await screen.findByText("word24");
		expect(onComplete).not.toHaveBeenCalled();

		await user.click(screen.getByRole("button", { name: /written them down|je les ai notés/i }));
		await user.type(screen.getByLabelText(/word 1|mot 1/i), "word1");
		await user.type(screen.getByLabelText(/word 2|mot 2/i), "word2");
		await user.type(screen.getByLabelText(/word 3|mot 3/i), "word3");
		await user.click(screen.getByRole("button", { name: /^confirm$|^confirmer$/i }));
		expect(onComplete).toHaveBeenCalledTimes(1);
	});

	it("efface la clé de l'écran une fois confirmée", async () => {
		const user = userEvent.setup();
		render(
			<RegisterForm onSubmit={vi.fn(async () => PHRASE)} onComplete={vi.fn()} random={FIXED} />,
		);
		await user.type(email(), "a@example.com");
		await user.type(password(), "correct horse battery staple");
		await user.click(submit());
		await screen.findByText("word24");
		await user.click(screen.getByRole("button", { name: /written them down|je les ai notés/i }));
		await user.type(screen.getByLabelText(/word 1|mot 1/i), "word1");
		await user.type(screen.getByLabelText(/word 2|mot 2/i), "word2");
		await user.type(screen.getByLabelText(/word 3|mot 3/i), "word3");
		await user.click(screen.getByRole("button", { name: /^confirm$|^confirmer$/i }));
		// La phrase ne doit plus être nulle part dans le DOM.
		expect(screen.queryByText("word24")).not.toBeInTheDocument();
	});

	it("affiche une erreur et ne montre aucune clé si l'inscription échoue", async () => {
		const user = userEvent.setup();
		render(
			<RegisterForm
				onSubmit={vi.fn(async () => { throw new Error("400"); })}
				onComplete={vi.fn()}
				random={FIXED}
			/>,
		);
		await user.type(email(), "a@example.com");
		await user.type(password(), "correct horse battery staple");
		await user.click(submit());
		expect(await screen.findByText(/could not create|impossible de créer/i)).toBeInTheDocument();
		expect(screen.queryByText("word24")).not.toBeInTheDocument();
	});
});
```

- [ ] **Step 6: Implémenter le formulaire d'inscription**

Créer `src/components/sync/RegisterForm.tsx` :

```tsx
import { useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { RecoveryPhraseStep } from "./RecoveryPhraseStep";

export interface RegisterFormProps {
	/** Resolves with the 24-word recovery phrase returned by register(). */
	onSubmit: (input: {
		email: string;
		password: string;
		inviteToken?: string;
	}) => Promise<string>;
	onComplete: () => void;
	onSwitchToSignIn?: () => void;
	random?: () => number;
}

export function RegisterForm({
	onSubmit,
	onComplete,
	onSwitchToSignIn,
	random,
}: RegisterFormProps) {
	const { t } = useTranslation();
	const [email, setEmail] = useState("");
	const [password, setPassword] = useState("");
	const [inviteToken, setInviteToken] = useState("");
	const [busy, setBusy] = useState(false);
	const [failed, setFailed] = useState(false);
	// Real key material: it lives here only between registering and confirming,
	// and is dropped the moment the user confirms. Never persisted, never logged.
	const [phrase, setPhrase] = useState<string | null>(null);

	const ready = email.trim() !== "" && password !== "" && !busy;

	async function handleSubmit(e: React.FormEvent) {
		e.preventDefault();
		if (!ready) return;
		setBusy(true);
		setFailed(false);
		try {
			const recoveryPhrase = await onSubmit({
				email: email.trim(),
				password,
				...(inviteToken.trim() ? { inviteToken: inviteToken.trim() } : {}),
			});
			setPhrase(recoveryPhrase);
		} catch {
			setFailed(true);
		} finally {
			setBusy(false);
		}
	}

	if (phrase !== null) {
		return (
			<RecoveryPhraseStep
				phrase={phrase}
				random={random}
				onConfirmed={() => {
					setPhrase(null);
					setPassword("");
					onComplete();
				}}
			/>
		);
	}

	return (
		<form onSubmit={handleSubmit} className="flex flex-col gap-3">
			<div className="flex flex-col gap-1.5">
				<label className="text-sm" htmlFor="sync-register-email">
					{t("sync.email")}
				</label>
				<Input
					id="sync-register-email"
					type="email"
					autoComplete="username"
					value={email}
					onChange={(e) => setEmail(e.target.value)}
				/>
			</div>
			<div className="flex flex-col gap-1.5">
				<label className="text-sm" htmlFor="sync-register-password">
					{t("sync.password")}
				</label>
				<Input
					id="sync-register-password"
					type="password"
					autoComplete="new-password"
					value={password}
					onChange={(e) => setPassword(e.target.value)}
				/>
				<p className="text-xs text-muted-foreground">{t("sync.passwordHint")}</p>
			</div>
			<div className="flex flex-col gap-1.5">
				<label className="text-sm" htmlFor="sync-register-invite">
					{t("sync.inviteToken")}
				</label>
				<Input
					id="sync-register-invite"
					autoComplete="off"
					value={inviteToken}
					onChange={(e) => setInviteToken(e.target.value)}
				/>
				<p className="text-xs text-muted-foreground">{t("sync.inviteTokenHint")}</p>
			</div>

			{failed && <p className="text-xs text-destructive">{t("sync.registerFailed")}</p>}

			<div className="flex items-center justify-between gap-2">
				{onSwitchToSignIn ? (
					<Button type="button" variant="ghost" size="sm" onClick={onSwitchToSignIn}>
						{t("sync.signIn")}
					</Button>
				) : (
					<span />
				)}
				<Button type="submit" disabled={!ready}>
					{busy ? t("sync.creatingAccount") : t("sync.createAccount")}
				</Button>
			</div>
		</form>
	);
}
```

- [ ] **Step 7: Vert complet**

```bash
pnpm test:run src/components/sync/
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 760 tests**.

- [ ] **Step 8: Commit**

```bash
git add src/components/sync/recovery-confirmation.ts src/components/sync/recovery-confirmation.test.ts src/components/sync/RecoveryPhraseStep.tsx src/components/sync/RecoveryPhraseStep.test.tsx src/components/sync/RegisterForm.tsx src/components/sync/RegisterForm.test.tsx
git commit -m "feat: :memo: register an account and confirm the 24-word recovery key"
```

- [ ] **Step 9: [SABOTAGE] Vérifier que la ressaisie est réellement contrôlée**

Dans `src/components/sync/RecoveryPhraseStep.tsx`, rendre la vérification permissive :

```tsx
	function handleConfirm() {
		const ok = true;
		if (!ok) {
			setWrong(true);
			return;
		}
		onConfirmed();
	}
```

```bash
git diff --stat -- src/components/sync/RecoveryPhraseStep.tsx
grep -n "const ok = true" src/components/sync/RecoveryPhraseStep.tsx
```
Attendu : fichier listé ; le `grep` trouve `const ok = true;`.

> **Observabilité :** aucune autre couche ne compare la ressaisie — `onConfirmed` est une prop, et `RegisterForm` lui fait confiance. Sans la comparaison, le test « refuse une ressaisie erronée » voit `onConfirmed` appelé une fois au lieu de zéro.

```bash
pnpm test:run src/components/sync/RecoveryPhraseStep.test.tsx
```
Attendu : ÉCHEC du test « refuse une ressaisie erronée et ne confirme pas ».

- [ ] **Step 10: Restaurer et re-vérifier**

```bash
git checkout -- src/components/sync/RecoveryPhraseStep.tsx
pnpm test:run src/components/sync/RecoveryPhraseStep.test.tsx
```
Attendu : PASS.

---

### Tâche 11 : Panneau connecté — état, appareils, déconnexion (§6.3, §6.5)

**Files:**
- Create: `src/components/sync/DeviceList.tsx`
- Create: `src/components/sync/ConnectedPanel.tsx`
- Test: `src/components/sync/DeviceList.test.tsx`
- Test: `src/components/sync/ConnectedPanel.test.tsx`

**Interfaces:**
- Consomme : `SyncDevice` (tâche 5) ; `SyncStatus` (tâche 1).
- Produit :
```ts
export interface DeviceListProps {
	load: () => Promise<SyncDevice[]>;
	revoke: (id: string) => Promise<void>;
}
export function DeviceList(props: DeviceListProps): JSX.Element;

export interface ConnectedPanelProps {
	accountEmail: string;
	serverUrl: string;
	status: SyncStatus;
	lastSyncAt: string | null;
	onSyncNow: () => Promise<void>;
	onDisconnect: () => Promise<void>;
	onUnlock: () => void;
	devices: DeviceListProps;
}
export function ConnectedPanel(props: ConnectedPanelProps): JSX.Element;
```
Consommés par la tâche 14.

- [ ] **Step 1: Écrire les tests de la liste d'appareils**

Créer `src/components/sync/DeviceList.test.tsx` :

```tsx
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import "@/i18n";
import type { SyncDevice } from "@/sync/devices-types";
import { DeviceList } from "./DeviceList";

const CURRENT: SyncDevice = {
	id: "11111111-1111-4111-8111-111111111111",
	name: "Poste fixe",
	platform: "linux",
	lastSeenAt: "2026-08-26T09:00:00.000Z",
	createdAt: "2026-08-01T09:00:00.000Z",
	current: true,
};
const OTHER: SyncDevice = {
	id: "22222222-2222-4222-8222-222222222222",
	name: "Portable",
	platform: "macos",
	lastSeenAt: null,
	createdAt: "2026-08-02T09:00:00.000Z",
	current: false,
};

describe("DeviceList", () => {
	it("liste les appareils et marque l'appareil courant", async () => {
		render(<DeviceList load={vi.fn(async () => [CURRENT, OTHER])} revoke={vi.fn()} />);
		expect(await screen.findByText("Poste fixe")).toBeInTheDocument();
		expect(screen.getByText("Portable")).toBeInTheDocument();
		expect(screen.getByText(/this device|cet appareil/i)).toBeInTheDocument();
	});

	it("n'offre pas de révoquer l'appareil courant", async () => {
		render(<DeviceList load={vi.fn(async () => [CURRENT, OTHER])} revoke={vi.fn()} />);
		await screen.findByText("Poste fixe");
		// Un seul bouton Révoquer : celui de l'autre appareil.
		expect(screen.getAllByRole("button", { name: /revoke|révoquer/i })).toHaveLength(1);
	});

	it("ne révoque qu'après confirmation, et recharge ensuite", async () => {
		const user = userEvent.setup();
		const revoke = vi.fn(async () => {});
		const load = vi.fn(async () => [CURRENT, OTHER]);
		render(<DeviceList load={load} revoke={revoke} />);
		await screen.findByText("Portable");

		await user.click(screen.getByRole("button", { name: /revoke|révoquer/i }));
		expect(revoke).not.toHaveBeenCalled();
		expect(screen.getByText(/revoke this device\?|révoquer cet appareil \?/i)).toBeInTheDocument();

		await user.click(screen.getByRole("button", { name: /^revoke$|^révoquer$/i, hidden: false }));
		await waitFor(() => expect(revoke).toHaveBeenCalledWith(OTHER.id));
		await waitFor(() => expect(load).toHaveBeenCalledTimes(2));
	});

	it("signale un échec de chargement", async () => {
		render(<DeviceList load={vi.fn(async () => { throw new Error("500"); })} revoke={vi.fn()} />);
		expect(await screen.findByText(/could not load|impossible de charger/i)).toBeInTheDocument();
	});

	it("signale un échec de révocation sans faire disparaître l'appareil", async () => {
		const user = userEvent.setup();
		render(
			<DeviceList
				load={vi.fn(async () => [CURRENT, OTHER])}
				revoke={vi.fn(async () => { throw new Error("404"); })}
			/>,
		);
		await screen.findByText("Portable");
		await user.click(screen.getByRole("button", { name: /revoke|révoquer/i }));
		await user.click(screen.getByRole("button", { name: /^revoke$|^révoquer$/i }));
		expect(await screen.findByText(/could not revoke|impossible de révoquer/i)).toBeInTheDocument();
		expect(screen.getByText("Portable")).toBeInTheDocument();
	});
});
```

- [ ] **Step 2: Implémenter la liste**

Créer `src/components/sync/DeviceList.tsx` :

```tsx
import { useCallback, useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import {
	Dialog,
	DialogContent,
	DialogHeader,
	DialogTitle,
} from "@/components/ui/dialog";
import type { SyncDevice } from "@/sync/devices-types";

export interface DeviceListProps {
	load: () => Promise<SyncDevice[]>;
	revoke: (id: string) => Promise<void>;
}

export function DeviceList({ load, revoke }: DeviceListProps) {
	const { t, i18n } = useTranslation();
	const [devices, setDevices] = useState<SyncDevice[]>([]);
	const [loadFailed, setLoadFailed] = useState(false);
	const [pending, setPending] = useState<SyncDevice | null>(null);
	const [revoking, setRevoking] = useState(false);
	const [revokeFailed, setRevokeFailed] = useState(false);

	const refresh = useCallback(async () => {
		try {
			setDevices(await load());
			setLoadFailed(false);
		} catch {
			setLoadFailed(true);
		}
	}, [load]);

	useEffect(() => {
		void refresh();
	}, [refresh]);

	async function handleRevoke() {
		if (!pending) return;
		setRevoking(true);
		setRevokeFailed(false);
		try {
			await revoke(pending.id);
			setPending(null);
			await refresh();
		} catch {
			// A 404 means "not this account's device" — the server never answers
			// 403, so a failure reads the same either way.
			setRevokeFailed(true);
		} finally {
			setRevoking(false);
		}
	}

	function lastSeen(device: SyncDevice): string {
		if (!device.lastSeenAt) return t("sync.devices.never");
		return t("sync.devices.lastSeen", {
			when: new Date(device.lastSeenAt).toLocaleString(i18n.language),
		});
	}

	const others = devices.filter((d) => !d.current);

	return (
		<div className="flex flex-col gap-2">
			<p className="text-sm font-medium">{t("sync.devices.title")}</p>
			{loadFailed && (
				<p className="text-xs text-destructive">{t("sync.devices.loadFailed")}</p>
			)}
			{!loadFailed && devices.length > 0 && others.length === 0 && (
				<p className="text-xs text-muted-foreground">{t("sync.devices.empty")}</p>
			)}
			<ul className="flex flex-col gap-1">
				{devices.map((device) => (
					<li
						key={device.id}
						className="flex items-center justify-between gap-2 rounded-md border border-border px-3 py-2"
					>
						<div className="min-w-0">
							<p className="truncate text-sm">
								{device.name}
								{device.current && (
									<span className="ml-2 text-xs text-muted-foreground">
										{t("sync.devices.thisDevice")}
									</span>
								)}
							</p>
							<p className="text-xs text-muted-foreground">
								{device.platform} — {lastSeen(device)}
							</p>
						</div>
						{!device.current && (
							<Button
								type="button"
								variant="outline"
								size="sm"
								onClick={() => {
									setPending(device);
									setRevokeFailed(false);
								}}
							>
								{t("sync.devices.revoke")}
							</Button>
						)}
					</li>
				))}
			</ul>

			<Dialog open={pending !== null} onOpenChange={(open) => !open && setPending(null)}>
				<DialogContent>
					<DialogHeader>
						<DialogTitle>{t("sync.devices.revokeConfirmTitle")}</DialogTitle>
					</DialogHeader>
					<p className="text-sm text-muted-foreground">
						{t("sync.devices.revokeConfirmMessage", { name: pending?.name ?? "" })}
					</p>
					{revokeFailed && (
						<p className="text-xs text-destructive">{t("sync.devices.revokeFailed")}</p>
					)}
					<div className="flex justify-end gap-2">
						<Button type="button" variant="outline" onClick={() => setPending(null)}>
							{t("common.cancel")}
						</Button>
						<Button
							type="button"
							variant="destructive"
							disabled={revoking}
							onClick={handleRevoke}
						>
							{revoking ? t("sync.devices.revoking") : t("sync.devices.revoke")}
						</Button>
					</div>
				</DialogContent>
			</Dialog>
		</div>
	);
}
```

> Vérifier la signature réelle de `Dialog` / `DialogContent` dans [`src/components/ui/dialog.tsx`](../../../src/components/ui/dialog.tsx) — notamment si `open`/`onOpenChange` sont les bons noms de props sous `@base-ui/react`. **Adapter à l'API réelle.** Un exemple contrôlé existe dans [`ImportConfirmDialog.tsx`](../../../src/components/layout/ImportConfirmDialog.tsx) : le copier.

```bash
pnpm test:run src/components/sync/DeviceList.test.tsx
```
Attendu : PASS (5 tests).

- [ ] **Step 3: Écrire les tests du panneau connecté**

Créer `src/components/sync/ConnectedPanel.test.tsx` :

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import "@/i18n";
import { ConnectedPanel } from "./ConnectedPanel";

function setup(overrides: Partial<React.ComponentProps<typeof ConnectedPanel>> = {}) {
	const props = {
		accountEmail: "a@example.com",
		serverUrl: "https://sync.example.com",
		status: "idle" as const,
		lastSyncAt: "2026-08-26T09:00:00.000Z",
		onSyncNow: vi.fn(async () => {}),
		onDisconnect: vi.fn(async () => {}),
		onUnlock: vi.fn(),
		devices: { load: vi.fn(async () => []), revoke: vi.fn(async () => {}) },
		...overrides,
	};
	render(<ConnectedPanel {...props} />);
	return { props, user: userEvent.setup() };
}

describe("ConnectedPanel", () => {
	it("montre le compte, le serveur et l'état", () => {
		setup();
		expect(screen.getByText("a@example.com")).toBeInTheDocument();
		expect(screen.getByText("https://sync.example.com")).toBeInTheDocument();
		expect(screen.getByText(/up to date|à jour/i)).toBeInTheDocument();
	});

	it("indique « jamais » quand aucune sync n'a eu lieu", () => {
		setup({ lastSyncAt: null });
		expect(screen.getByText(/^never$|^jamais$/i)).toBeInTheDocument();
	});

	it("déclenche une synchronisation manuelle", async () => {
		const { props, user } = setup();
		await user.click(screen.getByRole("button", { name: /sync now|synchroniser maintenant/i }));
		expect(props.onSyncNow).toHaveBeenCalledTimes(1);
	});

	it("propose de déverrouiller, et pas de synchroniser, quand le coffre est fermé", async () => {
		const { props, user } = setup({ status: "locked" });
		expect(screen.getByText(/paused|en pause/i)).toBeInTheDocument();
		await user.click(screen.getByRole("button", { name: /^unlock$|^déverrouiller$/i }));
		expect(props.onUnlock).toHaveBeenCalledTimes(1);
		expect(
			screen.queryByRole("button", { name: /sync now|synchroniser maintenant/i }),
		).not.toBeInTheDocument();
	});

	it("annonce la reconnexion nécessaire", () => {
		setup({ status: "reauth-required" });
		expect(screen.getByText(/sign in again|reconnexion/i)).toBeInTheDocument();
	});

	it("ne déconnecte qu'après confirmation", async () => {
		const { props, user } = setup();
		await user.click(screen.getByRole("button", { name: /disconnect this device|déconnecter cet appareil/i }));
		expect(props.onDisconnect).not.toHaveBeenCalled();
		expect(screen.getByText(/disconnect from this server\?|se déconnecter de ce serveur \?/i)).toBeInTheDocument();

		await user.click(screen.getByRole("button", { name: /^disconnect$|^déconnecter$/i }));
		expect(props.onDisconnect).toHaveBeenCalledTimes(1);
	});

	it("rassure sur le sort des données locales avant de déconnecter", async () => {
		const { user } = setup();
		await user.click(screen.getByRole("button", { name: /disconnect this device|déconnecter cet appareil/i }));
		expect(
			screen.getByText(/stay on this device|restent sur cet appareil/i),
		).toBeInTheDocument();
	});
});
```

- [ ] **Step 4: Implémenter le panneau connecté**

Créer `src/components/sync/ConnectedPanel.tsx` :

```tsx
import { useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import {
	Dialog,
	DialogContent,
	DialogHeader,
	DialogTitle,
} from "@/components/ui/dialog";
import type { SyncStatus } from "@/sync/types";
import { DeviceList, type DeviceListProps } from "./DeviceList";

export interface ConnectedPanelProps {
	accountEmail: string;
	serverUrl: string;
	status: SyncStatus;
	lastSyncAt: string | null;
	onSyncNow: () => Promise<void>;
	onDisconnect: () => Promise<void>;
	onUnlock: () => void;
	devices: DeviceListProps;
}

/** SyncStatus is kebab-case, the i18n keys are camelCase. */
const STATUS_KEY: Record<SyncStatus, string> = {
	idle: "idle",
	syncing: "syncing",
	locked: "locked",
	"awaiting-first-sync": "awaitingFirstSync",
	"reauth-required": "reauthRequired",
	"protocol-mismatch": "protocolMismatch",
};

export function ConnectedPanel({
	accountEmail,
	serverUrl,
	status,
	lastSyncAt,
	onSyncNow,
	onDisconnect,
	onUnlock,
	devices,
}: ConnectedPanelProps) {
	const { t, i18n } = useTranslation();
	const [confirming, setConfirming] = useState(false);
	const [busy, setBusy] = useState(false);
	const [syncing, setSyncing] = useState(false);

	async function handleDisconnect() {
		setBusy(true);
		try {
			await onDisconnect();
			setConfirming(false);
		} finally {
			setBusy(false);
		}
	}

	async function handleSyncNow() {
		setSyncing(true);
		try {
			await onSyncNow();
		} finally {
			setSyncing(false);
		}
	}

	return (
		<div className="flex flex-col gap-4 py-4">
			<div className="flex flex-col gap-1">
				<p className="text-xs text-muted-foreground">{t("sync.account")}</p>
				<p className="text-sm">{accountEmail}</p>
				<p className="text-xs text-muted-foreground">{serverUrl}</p>
			</div>

			<div className="flex flex-col gap-1">
				<p className="text-xs text-muted-foreground">{t("sync.status.label")}</p>
				<p className="text-sm">{t(`sync.status.${STATUS_KEY[status]}`)}</p>
				<p className="text-xs text-muted-foreground">
					{t("sync.lastSync")} —{" "}
					{lastSyncAt ? new Date(lastSyncAt).toLocaleString(i18n.language) : t("sync.never")}
				</p>
			</div>

			<div className="flex gap-2">
				{status === "locked" ? (
					// Syncing is pointless while the vault is shut: the engine would
					// return straight away. Offer the action that actually unblocks.
					<Button type="button" onClick={onUnlock}>
						{t("sync.unlock.submit")}
					</Button>
				) : (
					<Button
						type="button"
						variant="outline"
						disabled={syncing || status === "syncing"}
						onClick={handleSyncNow}
					>
						{t("sync.syncNow")}
					</Button>
				)}
			</div>

			<DeviceList {...devices} />

			<div className="flex flex-col gap-2 border-t border-border pt-4">
				<p className="text-xs text-muted-foreground">{t("sync.changeServerHint")}</p>
				<div>
					<Button type="button" variant="destructive" onClick={() => setConfirming(true)}>
						{t("sync.disconnect")}
					</Button>
				</div>
			</div>

			<Dialog open={confirming} onOpenChange={(open) => !open && setConfirming(false)}>
				<DialogContent>
					<DialogHeader>
						<DialogTitle>{t("sync.disconnectConfirmTitle")}</DialogTitle>
					</DialogHeader>
					<p className="text-sm text-muted-foreground">
						{t("sync.disconnectConfirmMessage")}
					</p>
					<div className="flex justify-end gap-2">
						<Button type="button" variant="outline" onClick={() => setConfirming(false)}>
							{t("common.cancel")}
						</Button>
						<Button
							type="button"
							variant="destructive"
							disabled={busy}
							onClick={handleDisconnect}
						>
							{busy ? t("sync.disconnecting") : t("sync.disconnect").split(" ")[0]}
						</Button>
					</div>
				</DialogContent>
			</Dialog>
		</div>
	);
}
```

> **Attention au libellé du bouton de confirmation.** `t("sync.disconnect").split(" ")[0]` donnerait « Disconnect » / « Déconnecter » par accident de découpage — fragile et intraduisible. **Ajouter deux clés dédiées** `sync.disconnectShort` (EN `"Disconnect"`, FR `"Déconnecter"`) à la tâche 7 et les utiliser ici. Faire de même si le libellé court de révocation pose le même problème. Mettre à jour `src/i18n/sync-keys.test.ts` n'est pas nécessaire — il vérifie la parité, pas la liste.

- [ ] **Step 5: Vert complet**

```bash
pnpm test:run src/components/sync/
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 772 tests**.

- [ ] **Step 6: Commit**

```bash
git add src/components/sync/DeviceList.tsx src/components/sync/DeviceList.test.tsx src/components/sync/ConnectedPanel.tsx src/components/sync/ConnectedPanel.test.tsx src/i18n/locales/en.ts src/i18n/locales/fr.ts
git commit -m "feat: :desktop_computer: show sync state, devices and disconnection"
```

- [ ] **Step 7: [SABOTAGE] Vérifier que la confirmation de révocation est testée**

Dans `src/components/sync/DeviceList.tsx`, révoquer directement au clic du bouton de la ligne :

```tsx
							<Button
								type="button"
								variant="outline"
								size="sm"
								onClick={() => void revoke(device.id)}
							>
```

```bash
git diff --stat -- src/components/sync/DeviceList.tsx
grep -n "onClick={() => void revoke(device.id)}" src/components/sync/DeviceList.tsx
```
Attendu : fichier listé ; le `grep` trouve la ligne.

> **Observabilité :** rien n'interpose de confirmation ailleurs — le composant est autonome. Le test « ne révoque qu'après confirmation » verra `revoke` appelé avant l'assertion `not.toHaveBeenCalled()`.

```bash
pnpm test:run src/components/sync/DeviceList.test.tsx
```
Attendu : ÉCHEC du test « ne révoque qu'après confirmation, et recharge ensuite ».

- [ ] **Step 8: Restaurer et re-vérifier**

```bash
git checkout -- src/components/sync/DeviceList.tsx
pnpm test:run src/components/sync/DeviceList.test.tsx
```
Attendu : PASS.

---

### Tâche 12 : Déverrouillage à la demande et bandeau d'état (§7)

**Files:**
- Create: `src/components/sync/UnlockDialog.tsx`
- Create: `src/components/sync/SyncStatusBanner.tsx`
- Test: `src/components/sync/UnlockDialog.test.tsx`
- Test: `src/components/sync/SyncStatusBanner.test.tsx`

**Interfaces:**
- Consomme : `useSyncStore` (tâche 6).
- Produit :
```ts
export interface UnlockDialogProps {
	open: boolean;
	onOpenChange: (open: boolean) => void;
	/** Rejects when the password is wrong. Resolves once the vault is open. */
	onUnlock: (password: string) => Promise<void>;
}
export function UnlockDialog(props: UnlockDialogProps): JSX.Element;

export interface SyncStatusBannerProps {
	onOpenSettings: () => void;
}
export function SyncStatusBanner(props: SyncStatusBannerProps): JSX.Element | null;
```
Consommés par la tâche 14.

Le déverrouillage refait `prelogin` puis `beginUnlock`/`completeUnlock` : le sel du compte n'est pas persisté localement, donc il faut le redemander au serveur. Hors ligne, le déverrouillage n'est pas possible — le message doit le dire honnêtement plutôt que d'accuser le mot de passe.

- [ ] **Step 1: Écrire les tests du dialogue**

Créer `src/components/sync/UnlockDialog.test.tsx` :

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import "@/i18n";
import { UnlockDialog } from "./UnlockDialog";

const password = () => screen.getByLabelText(/password|mot de passe/i);
const submit = () => screen.getByRole("button", { name: /^unlock$|^déverrouiller$/i });

describe("UnlockDialog", () => {
	it("n'active le bouton qu'avec un mot de passe saisi", async () => {
		const user = userEvent.setup();
		render(<UnlockDialog open onOpenChange={vi.fn()} onUnlock={vi.fn()} />);
		expect(submit()).toBeDisabled();
		await user.type(password(), "hunter2");
		expect(submit()).toBeEnabled();
	});

	it("transmet le mot de passe et referme au succès", async () => {
		const user = userEvent.setup();
		const onUnlock = vi.fn(async () => {});
		const onOpenChange = vi.fn();
		render(<UnlockDialog open onOpenChange={onOpenChange} onUnlock={onUnlock} />);
		await user.type(password(), "hunter2");
		await user.click(submit());
		expect(onUnlock).toHaveBeenCalledWith("hunter2");
		expect(onOpenChange).toHaveBeenCalledWith(false);
	});

	it("signale un mot de passe erroné sans refermer", async () => {
		const user = userEvent.setup();
		const onOpenChange = vi.fn();
		render(
			<UnlockDialog
				open
				onOpenChange={onOpenChange}
				onUnlock={vi.fn(async () => { throw new Error("bad password"); })}
			/>,
		);
		await user.type(password(), "wrong");
		await user.click(submit());
		expect(await screen.findByText(/wrong password|mot de passe incorrect/i)).toBeInTheDocument();
		expect(onOpenChange).not.toHaveBeenCalledWith(false);
	});

	it("n'expose jamais le mot de passe en clair", async () => {
		render(<UnlockDialog open onOpenChange={vi.fn()} onUnlock={vi.fn()} />);
		expect(password()).toHaveAttribute("type", "password");
	});

	it("oublie le mot de passe saisi après un déverrouillage réussi", async () => {
		const user = userEvent.setup();
		render(<UnlockDialog open onOpenChange={vi.fn()} onUnlock={vi.fn(async () => {})} />);
		await user.type(password(), "hunter2");
		await user.click(submit());
		expect(password()).toHaveValue("");
	});
});
```

- [ ] **Step 2: Implémenter le dialogue**

Créer `src/components/sync/UnlockDialog.tsx` :

```tsx
import { useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import {
	Dialog,
	DialogContent,
	DialogHeader,
	DialogTitle,
} from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";

export interface UnlockDialogProps {
	open: boolean;
	onOpenChange: (open: boolean) => void;
	onUnlock: (password: string) => Promise<void>;
}

export function UnlockDialog({ open, onOpenChange, onUnlock }: UnlockDialogProps) {
	const { t } = useTranslation();
	const [password, setPassword] = useState("");
	const [busy, setBusy] = useState(false);
	const [failed, setFailed] = useState(false);

	async function handleSubmit(e: React.FormEvent) {
		e.preventDefault();
		if (password === "" || busy) return;
		setBusy(true);
		setFailed(false);
		try {
			await onUnlock(password);
			// Drop it as soon as the vault is open: it derives the KEK.
			setPassword("");
			onOpenChange(false);
		} catch {
			setFailed(true);
		} finally {
			setBusy(false);
		}
	}

	return (
		<Dialog open={open} onOpenChange={onOpenChange}>
			<DialogContent>
				<DialogHeader>
					<DialogTitle>{t("sync.unlock.title")}</DialogTitle>
				</DialogHeader>
				<form onSubmit={handleSubmit} className="flex flex-col gap-3">
					<p className="text-xs text-muted-foreground">{t("sync.unlock.intro")}</p>
					<div className="flex flex-col gap-1.5">
						<label className="text-sm" htmlFor="sync-unlock-password">
							{t("sync.password")}
						</label>
						<Input
							id="sync-unlock-password"
							type="password"
							autoComplete="current-password"
							value={password}
							onChange={(e) => {
								setPassword(e.target.value);
								setFailed(false);
							}}
						/>
					</div>
					{failed && <p className="text-xs text-destructive">{t("sync.unlock.failed")}</p>}
					<div className="flex justify-end gap-2">
						<Button type="button" variant="outline" onClick={() => onOpenChange(false)}>
							{t("common.cancel")}
						</Button>
						<Button type="submit" disabled={password === "" || busy}>
							{busy ? t("sync.unlock.unlocking") : t("sync.unlock.submit")}
						</Button>
					</div>
				</form>
			</DialogContent>
		</Dialog>
	);
}
```

```bash
pnpm test:run src/components/sync/UnlockDialog.test.tsx
```
Attendu : PASS (5 tests).

- [ ] **Step 3: Écrire les tests du bandeau**

Créer `src/components/sync/SyncStatusBanner.test.tsx` :

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { beforeEach, describe, expect, it, vi } from "vitest";
import "@/i18n";
import { useSyncStore } from "@/store/sync";
import type { SyncStatus } from "@/sync/types";
import { SyncStatusBanner } from "./SyncStatusBanner";

function setStatus(status: SyncStatus | null) {
	useSyncStore.setState({ status });
}

describe("SyncStatusBanner", () => {
	beforeEach(() => setStatus(null));

	it("ne rend rien quand la sync n'est pas configurée", () => {
		const { container } = render(<SyncStatusBanner onOpenSettings={vi.fn()} />);
		expect(container).toBeEmptyDOMElement();
	});

	it.each(["idle", "syncing", "awaiting-first-sync"] as const)(
		"ne rend rien sur un statut sain (%s)",
		(status) => {
			setStatus(status);
			const { container } = render(<SyncStatusBanner onOpenSettings={vi.fn()} />);
			expect(container).toBeEmptyDOMElement();
		},
	);

	it.each([
		["locked", /paused|en pause/i],
		["reauth-required", /sign in again|reconnexion/i],
		["protocol-mismatch", /not compatible|pas compatible/i],
	] as const)("annonce le statut %s", (status, pattern) => {
		setStatus(status);
		render(<SyncStatusBanner onOpenSettings={vi.fn()} />);
		expect(screen.getByText(pattern)).toBeInTheDocument();
	});

	it("ouvre les réglages depuis le bandeau", async () => {
		const user = userEvent.setup();
		const onOpenSettings = vi.fn();
		setStatus("locked");
		render(<SyncStatusBanner onOpenSettings={onOpenSettings} />);
		await user.click(screen.getByRole("button", { name: /open settings|ouvrir les réglages/i }));
		expect(onOpenSettings).toHaveBeenCalledTimes(1);
	});
});
```

- [ ] **Step 4: Implémenter le bandeau**

Créer `src/components/sync/SyncStatusBanner.tsx` :

```tsx
import { AlertTriangle } from "lucide-react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import { useSyncStore } from "@/store/sync";
import type { SyncStatus } from "@/sync/types";

export interface SyncStatusBannerProps {
	onOpenSettings: () => void;
}

/** §7: only the states the user has to act on. idle, syncing and
 * awaiting-first-sync are handled elsewhere (the panel, the first-sync dialog)
 * and a banner for them would be noise. */
const ACTIONABLE: Partial<Record<SyncStatus, string>> = {
	locked: "sync.banner.locked",
	"reauth-required": "sync.banner.reauthRequired",
	"protocol-mismatch": "sync.banner.protocolMismatch",
};

export function SyncStatusBanner({ onOpenSettings }: SyncStatusBannerProps) {
	const { t } = useTranslation();
	const status = useSyncStore((s) => s.status);

	const messageKey = status ? ACTIONABLE[status] : undefined;
	if (!messageKey) return null;

	return (
		<div className="flex items-center justify-between gap-3 border-b border-border bg-muted/50 px-4 py-2">
			<p className="flex items-center gap-1.5 text-xs text-destructive">
				<AlertTriangle className="h-3.5 w-3.5 shrink-0" />
				{t(messageKey)}
			</p>
			<Button type="button" variant="outline" size="sm" onClick={onOpenSettings}>
				{t("sync.banner.action")}
			</Button>
		</div>
	);
}
```

- [ ] **Step 5: Vert complet**

```bash
pnpm test:run src/components/sync/
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 785 tests**.

- [ ] **Step 6: Commit**

```bash
git add src/components/sync/UnlockDialog.tsx src/components/sync/UnlockDialog.test.tsx src/components/sync/SyncStatusBanner.tsx src/components/sync/SyncStatusBanner.test.tsx
git commit -m "feat: :unlock: let the user unlock sync and surface actionable states"
```

---

### Tâche 13 : Dialogue de première synchronisation et sauvegarde automatique (§6.4)

L'écran le plus destructeur du plan : « Remplacer » efface les tables locales. Le §6.4 exige qu'une sauvegarde JSON soit écrite **avant**.

**Files:**
- Create: `src/components/sync/FirstSyncDialog.tsx`
- Test: `src/components/sync/FirstSyncDialog.test.tsx`

**Interfaces:**
- Consomme : `SyncEngine.resolveFirstSync("merge" | "replace")` (4c).
- Produit :
```ts
export interface FirstSyncDialogProps {
	open: boolean;
	/** Writes the automatic JSON backup. Rejecting MUST abort a replace. */
	backup: () => Promise<void>;
	resolve: (choice: "merge" | "replace") => Promise<void>;
	onResolved: () => void;
}
export function FirstSyncDialog(props: FirstSyncDialogProps): JSX.Element;
```
Consommé par la tâche 14.

- [ ] **Step 1: Écrire les tests d'échec**

Créer `src/components/sync/FirstSyncDialog.test.tsx` :

```tsx
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import "@/i18n";
import { FirstSyncDialog } from "./FirstSyncDialog";

function setup(overrides: Partial<React.ComponentProps<typeof FirstSyncDialog>> = {}) {
	const props = {
		open: true,
		backup: vi.fn(async () => {}),
		resolve: vi.fn(async () => {}),
		onResolved: vi.fn(),
		...overrides,
	};
	render(<FirstSyncDialog {...props} />);
	return { props, user: userEvent.setup() };
}

const chooseMerge = () => screen.getByRole("button", { name: /^merge$|^fusionner$/i });
const chooseReplace = () => screen.getByRole("button", { name: /^replace$|^remplacer$/i });
const confirm = () => screen.getByRole("button", { name: /^continue$|^continuer$/i });

describe("FirstSyncDialog", () => {
	it("n'applique rien tant que le choix n'est pas confirmé", async () => {
		const { props, user } = setup();
		await user.click(chooseMerge());
		expect(props.resolve).not.toHaveBeenCalled();
	});

	it("fusionne sans écrire de sauvegarde", async () => {
		const { props, user } = setup();
		await user.click(chooseMerge());
		await user.click(confirm());
		await waitFor(() => expect(props.resolve).toHaveBeenCalledWith("merge"));
		expect(props.backup).not.toHaveBeenCalled();
		expect(props.onResolved).toHaveBeenCalledTimes(1);
	});

	it("écrit la sauvegarde AVANT de remplacer", async () => {
		const order: string[] = [];
		const { props, user } = setup({
			backup: vi.fn(async () => { order.push("backup"); }),
			resolve: vi.fn(async () => { order.push("resolve"); }),
		});
		await user.click(chooseReplace());
		await user.click(confirm());
		await waitFor(() => expect(props.resolve).toHaveBeenCalledWith("replace"));
		expect(order).toEqual(["backup", "resolve"]);
	});

	it("n'efface RIEN si la sauvegarde échoue", async () => {
		const { props, user } = setup({
			backup: vi.fn(async () => { throw new Error("disk full"); }),
		});
		await user.click(chooseReplace());
		await user.click(confirm());
		expect(await screen.findByText(/backup could not be saved|sauvegarde n'a pas pu/i)).toBeInTheDocument();
		expect(props.resolve).not.toHaveBeenCalled();
		expect(props.onResolved).not.toHaveBeenCalled();
	});

	it("avertit explicitement de la destruction avant de remplacer", async () => {
		const { user } = setup();
		await user.click(chooseReplace());
		expect(screen.getByText(/will be deleted|seront supprimés/i)).toBeInTheDocument();
		expect(screen.getByText(/backup is saved|sauvegarde est enregistrée/i)).toBeInTheDocument();
	});

	it("signale un échec d'application sans prétendre avoir réussi", async () => {
		const { props, user } = setup({
			resolve: vi.fn(async () => { throw new Error("engine failed"); }),
		});
		await user.click(chooseMerge());
		await user.click(confirm());
		expect(await screen.findByText(/could not apply|impossible d'appliquer/i)).toBeInTheDocument();
		expect(props.onResolved).not.toHaveBeenCalled();
	});

	it("ne peut pas être fermé sans répondre", () => {
		setup();
		// §6.4: le moteur reste suspendu tant que la question n'a pas de réponse,
		// donc pas de bouton Annuler.
		expect(screen.queryByRole("button", { name: /cancel|annuler/i })).not.toBeInTheDocument();
	});
});
```

- [ ] **Step 2: Constater le rouge**

```bash
pnpm test:run src/components/sync/FirstSyncDialog.test.tsx
```
Attendu : ÉCHEC — module introuvable.

- [ ] **Step 3: Implémenter**

Créer `src/components/sync/FirstSyncDialog.tsx` :

```tsx
import { AlertTriangle } from "lucide-react";
import { useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import {
	Dialog,
	DialogContent,
	DialogHeader,
	DialogTitle,
} from "@/components/ui/dialog";
import { cn } from "@/lib/utils";

export interface FirstSyncDialogProps {
	open: boolean;
	backup: () => Promise<void>;
	resolve: (choice: "merge" | "replace") => Promise<void>;
	onResolved: () => void;
}

type Phase = "choosing" | "backing-up" | "applying";

export function FirstSyncDialog({
	open,
	backup,
	resolve,
	onResolved,
}: FirstSyncDialogProps) {
	const { t } = useTranslation();
	const [choice, setChoice] = useState<"merge" | "replace">("merge");
	const [phase, setPhase] = useState<Phase>("choosing");
	const [backupFailed, setBackupFailed] = useState(false);
	const [applyFailed, setApplyFailed] = useState(false);

	async function handleConfirm() {
		setBackupFailed(false);
		setApplyFailed(false);

		if (choice === "replace") {
			setPhase("backing-up");
			try {
				await backup();
			} catch {
				// §6.4: the backup is what makes "replace" recoverable. Without it
				// the destructive branch must not run at all.
				setBackupFailed(true);
				setPhase("choosing");
				return;
			}
		}

		setPhase("applying");
		try {
			await resolve(choice);
		} catch {
			setApplyFailed(true);
			setPhase("choosing");
			return;
		}
		onResolved();
	}

	const busy = phase !== "choosing";

	return (
		// No onOpenChange: the engine stays suspended until this is answered, so
		// there is no dismissing it.
		<Dialog open={open}>
			<DialogContent>
				<DialogHeader>
					<DialogTitle>{t("sync.firstSync.title")}</DialogTitle>
				</DialogHeader>
				<p className="text-sm text-muted-foreground">{t("sync.firstSync.intro")}</p>

				<div className="flex gap-2">
					{(["merge", "replace"] as const).map((option) => (
						<Button
							key={option}
							type="button"
							variant={choice === option ? "default" : "outline"}
							disabled={busy}
							className={cn("flex-1")}
							onClick={() => setChoice(option)}
						>
							{t(`sync.firstSync.${option}`)}
						</Button>
					))}
				</div>

				<p className="text-xs text-muted-foreground">
					{t(`sync.firstSync.${choice}Explanation`)}
				</p>

				{choice === "replace" && (
					<p className="flex items-center gap-1.5 text-xs text-destructive">
						<AlertTriangle className="h-3.5 w-3.5 shrink-0" />
						{t("sync.firstSync.replaceWarning")}
					</p>
				)}

				{backupFailed && (
					<p className="text-xs text-destructive">{t("sync.firstSync.backupFailed")}</p>
				)}
				{applyFailed && (
					<p className="text-xs text-destructive">{t("sync.firstSync.failed")}</p>
				)}

				<div className="flex justify-end">
					<Button type="button" disabled={busy} onClick={handleConfirm}>
						{phase === "backing-up"
							? t("sync.firstSync.backupSaving")
							: phase === "applying"
								? t("sync.firstSync.applying")
								: t("sync.firstSync.confirm")}
					</Button>
				</div>
			</DialogContent>
		</Dialog>
	);
}
```

- [ ] **Step 4: Vert complet**

```bash
pnpm test:run src/components/sync/FirstSyncDialog.test.tsx
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 792 tests**.

- [ ] **Step 5: Commit**

```bash
git add src/components/sync/FirstSyncDialog.tsx src/components/sync/FirstSyncDialog.test.tsx
git commit -m "feat: :bomb: ask before the first sync and back up before replacing"
```

- [ ] **Step 6: [SABOTAGE] Vérifier que l'échec de sauvegarde bloque le remplacement**

Dans `src/components/sync/FirstSyncDialog.tsx`, avaler l'échec de sauvegarde au lieu d'abandonner :

```tsx
			try {
				await backup();
			} catch {
				setBackupFailed(true);
			}
```

(c'est-à-dire retirer les deux lignes `setPhase("choosing");` et `return;`)

```bash
git diff --stat -- src/components/sync/FirstSyncDialog.tsx
grep -c "return;" src/components/sync/FirstSyncDialog.tsx
```
Attendu : fichier listé ; le `grep -c` retourne **`1`** (2 avant sabotage).

> **Observabilité :** `resolve` est une prop injectée, rien ne la protège en amont. Sans le `return`, le test « n'efface RIEN si la sauvegarde échoue » verra `resolve` appelé avec `"replace"`, c'est-à-dire l'effacement des tables locales sans sauvegarde — exactement le scénario que le §6.4 interdit.

```bash
pnpm test:run src/components/sync/FirstSyncDialog.test.tsx
```
Attendu : ÉCHEC du test « n'efface RIEN si la sauvegarde échoue ».

- [ ] **Step 7: Restaurer et re-vérifier**

```bash
git checkout -- src/components/sync/FirstSyncDialog.tsx
pnpm test:run src/components/sync/FirstSyncDialog.test.tsx
```
Attendu : PASS.

---

### Tâche 14 : Orchestration et branchement dans l'application

La seule tâche qui pose réellement `server_url` — et donc la seule qui rend la sync vivante.

**Files:**
- Create: `src/components/sync/SyncPanel.tsx`
- Test: `src/components/sync/SyncPanel.test.tsx`
- Modify: `src/components/layout/SettingsDialog.tsx` (lignes 1189-1243)
- Modify: `src/components/layout/AppShell.tsx` (bloc de rendu, lignes 97-112)
- Modify: `src/App.tsx` (lignes 115-139)

**Interfaces:**
- Consomme : tout ce qui précède.
- Produit : `SyncPanel` monté dans l'onglet « Synchronisation », `SyncStatusBanner` monté dans `AppShell`, contexte de sync posé au démarrage.

- [ ] **Step 1: Écrire les tests d'orchestration**

Créer `src/components/sync/SyncPanel.test.tsx` :

```tsx
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { beforeEach, describe, expect, it, vi } from "vitest";
import "@/i18n";
import { useSyncStore } from "@/store/sync";
import type { ServerInfo } from "@/sync/types";
import { SyncPanel, type SyncPanelDeps } from "./SyncPanel";

const OPEN_SERVER: ServerInfo = {
	name: "usagi-server",
	version: "1.2.0",
	protocolVersion: 1,
	registrationEnabled: true,
	minClientVersion: "0.1.0",
};
const CLOSED_SERVER: ServerInfo = { ...OPEN_SERVER, registrationEnabled: false };

function makeDeps(overrides: Partial<SyncPanelDeps> = {}): SyncPanelDeps {
	return {
		loadSession: vi.fn(async () => null),
		probe: vi.fn(async () => OPEN_SERVER),
		signIn: vi.fn(async () => {}),
		register: vi.fn(async () => "w1 w2 w3"),
		signOut: vi.fn(async () => {}),
		unlock: vi.fn(async () => {}),
		syncNow: vi.fn(async () => {}),
		listDevices: vi.fn(async () => []),
		revokeDevice: vi.fn(async () => {}),
		...overrides,
	};
}

const urlField = () => screen.getByLabelText(/server address|adresse du serveur/i);
const testButton = () => screen.getByRole("button", { name: /test connection|tester la connexion/i });

describe("SyncPanel", () => {
	beforeEach(() => {
		useSyncStore.setState({ status: null, lastSyncAt: null });
	});

	it("démarre sur la saisie d'URL quand aucun compte n'est connecté", async () => {
		render(<SyncPanel deps={makeDeps()} />);
		expect(await screen.findByLabelText(/server address|adresse du serveur/i)).toBeInTheDocument();
		expect(screen.queryByLabelText(/^password$|^mot de passe$/i)).not.toBeInTheDocument();
	});

	it("n'offre les identifiants qu'après un serveur vérifié", async () => {
		const user = userEvent.setup();
		render(<SyncPanel deps={makeDeps()} />);
		await user.type(await screen.findByLabelText(/server address|adresse du serveur/i), "https://sync.example.com");
		await user.click(testButton());
		expect(await screen.findByLabelText(/^password$|^mot de passe$/i)).toBeInTheDocument();
	});

	it("cache la création de compte quand le serveur la refuse", async () => {
		const user = userEvent.setup();
		render(<SyncPanel deps={makeDeps({ probe: vi.fn(async () => CLOSED_SERVER) })} />);
		await user.type(await screen.findByLabelText(/server address|adresse du serveur/i), "https://sync.example.com");
		await user.click(testButton());
		await screen.findByLabelText(/^password$|^mot de passe$/i);
		expect(screen.queryByRole("button", { name: /^create account$|^créer un compte$/i })).not.toBeInTheDocument();
		expect(screen.getByText(/does not accept new accounts|n'accepte pas de nouveaux comptes/i)).toBeInTheDocument();
	});

	it("connecte avec l'URL vérifiée et bascule sur le panneau connecté", async () => {
		const user = userEvent.setup();
		const deps = makeDeps();
		render(<SyncPanel deps={deps} />);
		await user.type(await screen.findByLabelText(/server address|adresse du serveur/i), "https://sync.example.com");
		await user.click(testButton());
		await user.type(await screen.findByLabelText(/email|adresse e-mail/i), "a@example.com");
		await user.type(screen.getByLabelText(/^password$|^mot de passe$/i), "hunter2hunter2");
		await user.click(screen.getByRole("button", { name: /^sign in$|^se connecter$/i }));

		await waitFor(() =>
			expect(deps.signIn).toHaveBeenCalledWith({
				serverUrl: "https://sync.example.com",
				email: "a@example.com",
				password: "hunter2hunter2",
			}),
		);
		expect(await screen.findByText("a@example.com")).toBeInTheDocument();
	});

	it("affiche directement le panneau connecté si une session existe", async () => {
		const deps = makeDeps({
			loadSession: vi.fn(async () => ({
				accountEmail: "b@example.com",
				serverUrl: "https://sync.example.com",
			})),
		});
		useSyncStore.setState({ status: "idle" });
		render(<SyncPanel deps={deps} />);
		expect(await screen.findByText("b@example.com")).toBeInTheDocument();
		expect(screen.queryByLabelText(/server address|adresse du serveur/i)).not.toBeInTheDocument();
	});

	it("revient à la saisie d'URL après déconnexion", async () => {
		const user = userEvent.setup();
		const deps = makeDeps({
			loadSession: vi.fn(async () => ({
				accountEmail: "b@example.com",
				serverUrl: "https://sync.example.com",
			})),
		});
		useSyncStore.setState({ status: "idle" });
		render(<SyncPanel deps={deps} />);
		await screen.findByText("b@example.com");
		await user.click(screen.getByRole("button", { name: /disconnect this device|déconnecter cet appareil/i }));
		await user.click(screen.getByRole("button", { name: /^disconnect$|^déconnecter$/i }));
		expect(await screen.findByLabelText(/server address|adresse du serveur/i)).toBeInTheDocument();
		expect(deps.signOut).toHaveBeenCalledTimes(1);
	});
});
```

- [ ] **Step 2: Implémenter `SyncPanel`**

Créer `src/components/sync/SyncPanel.tsx` :

```tsx
import { useCallback, useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import { useSyncStore } from "@/store/sync";
import type { SyncDevice } from "@/sync/devices-types";
import type { ServerInfo } from "@/sync/types";
import { ConnectedPanel } from "./ConnectedPanel";
import { RegisterForm } from "./RegisterForm";
import { ServerUrlForm } from "./ServerUrlForm";
import { SignInForm } from "./SignInForm";
import { UnlockDialog } from "./UnlockDialog";

export interface SyncSession {
	accountEmail: string;
	serverUrl: string;
}

/** Every side effect the panel needs, injected so it renders under vitest
 * without a Tauri runtime, a database or a network. */
export interface SyncPanelDeps {
	loadSession: () => Promise<SyncSession | null>;
	probe: (url: string) => Promise<ServerInfo>;
	signIn: (input: { serverUrl: string; email: string; password: string }) => Promise<void>;
	register: (input: {
		serverUrl: string;
		email: string;
		password: string;
		inviteToken?: string;
	}) => Promise<string>;
	signOut: () => Promise<void>;
	unlock: (password: string) => Promise<void>;
	syncNow: () => Promise<void>;
	listDevices: () => Promise<SyncDevice[]>;
	revokeDevice: (id: string) => Promise<void>;
}

type Screen =
	| { kind: "loading" }
	| { kind: "server" }
	| { kind: "credentials"; url: string; info: ServerInfo; mode: "sign-in" | "register" }
	| { kind: "connected"; session: SyncSession };

export function SyncPanel({ deps }: { deps: SyncPanelDeps }) {
	const { t } = useTranslation();
	const status = useSyncStore((s) => s.status);
	const lastSyncAt = useSyncStore((s) => s.lastSyncAt);
	const [screen, setScreen] = useState<Screen>({ kind: "loading" });
	const [unlocking, setUnlocking] = useState(false);

	useEffect(() => {
		let cancelled = false;
		void deps.loadSession().then((session) => {
			if (cancelled) return;
			setScreen(session ? { kind: "connected", session } : { kind: "server" });
		});
		return () => {
			cancelled = true;
		};
	}, [deps]);

	const handleDisconnect = useCallback(async () => {
		await deps.signOut();
		setScreen({ kind: "server" });
	}, [deps]);

	if (screen.kind === "loading") return <div className="py-4" />;

	if (screen.kind === "connected") {
		return (
			<>
				<ConnectedPanel
					accountEmail={screen.session.accountEmail}
					serverUrl={screen.session.serverUrl}
					status={status ?? "idle"}
					lastSyncAt={lastSyncAt}
					onSyncNow={deps.syncNow}
					onDisconnect={handleDisconnect}
					onUnlock={() => setUnlocking(true)}
					devices={{ load: deps.listDevices, revoke: deps.revokeDevice }}
				/>
				<UnlockDialog
					open={unlocking}
					onOpenChange={setUnlocking}
					onUnlock={deps.unlock}
				/>
			</>
		);
	}

	return (
		<div className="flex flex-col gap-4 py-4">
			<p className="text-xs text-muted-foreground">{t("sync.intro")}</p>

			<ServerUrlForm
				probe={deps.probe}
				onVerified={(url, info) =>
					setScreen({ kind: "credentials", url, info, mode: "sign-in" })
				}
			/>

			{screen.kind === "credentials" && (
				<div className="border-t border-border pt-4">
					{screen.mode === "sign-in" ? (
						<>
							<SignInForm
								onSubmit={async ({ email, password }) => {
									await deps.signIn({ serverUrl: screen.url, email, password });
									setScreen({
										kind: "connected",
										session: { accountEmail: email, serverUrl: screen.url },
									});
								}}
								onSwitchToRegister={
									screen.info.registrationEnabled
										? () => setScreen({ ...screen, mode: "register" })
										: undefined
								}
							/>
							{!screen.info.registrationEnabled && (
								<p className="mt-2 text-xs text-muted-foreground">
									{t("sync.registrationClosed")}
								</p>
							)}
						</>
					) : (
						<RegisterForm
							onSubmit={({ email, password, inviteToken }) =>
								deps.register({ serverUrl: screen.url, email, password, inviteToken })
							}
							onComplete={() =>
								setScreen({
									kind: "connected",
									// The email is the one just typed into RegisterForm; it is
									// re-read from sync_state on the next mount.
									session: { accountEmail: "", serverUrl: screen.url },
								})
							}
							onSwitchToSignIn={() => setScreen({ ...screen, mode: "sign-in" })}
						/>
					)}
				</div>
			)}
		</div>
	);
}
```

> **Le trou du `accountEmail: ""` après inscription.** Le rendre correct : faire remonter l'email par `RegisterForm.onComplete(email)` (changer la signature en `onComplete: (email: string) => void` et l'appeler avec `email.trim()`), ou rappeler `deps.loadSession()` après `onComplete`. **Choisir la seconde** — elle relit `account_email` depuis `sync_state`, que `persistSession` vient d'écrire, donc l'affichage vient de la source de vérité et non d'un état React parallèle. Adapter le test « connecte avec l'URL vérifiée » si besoin.

- [ ] **Step 3: Câbler les dépendances réelles et brancher l'onglet**

Créer les dépendances de production dans `src/components/sync/SyncPanel.tsx` (ou un `sync-panel-deps.ts` voisin si le fichier grossit) :

```tsx
// Le module qui fabrique SyncPanelDeps depuis le vrai monde :
// - loadSession   : getSyncState(db, "account_email") + "server_url"
// - probe         : (url) => getServerInfo(httpFetch as FetchLike, url)
// - signIn        : signIn({ db, fetchImpl, baseUrl: serverUrl, vault: tauriVault }, {...,
//                     deviceName: await hostname-ish, devicePlatform: platform() })
//                   puis await startSync() et useSyncStore.attach(runtime.engine)
// - register      : register({ db, fetchImpl, baseUrl: serverUrl, vault: tauriVault }, {...})
//                   puis await startSync() et attach ; retourne recoveryPhrase
// - signOut       : signOut({ db, fetchImpl, baseUrl }) puis await stopSync()
//                   puis useSyncStore.getState().detach()
// - unlock        : prelogin(email) → beginUnlock(password, salt) → GET /v1/keys
//                   → completeUnlock(wrappedDek, userId) → getSyncRuntime()?.engine.syncNow()
// - syncNow       : getSyncRuntime()?.engine.syncNow() puis refreshLastSync(db)
// - listDevices   : listDevices(new AuthorizedHttp({ db, fetchImpl, baseUrl }))
// - revokeDevice  : revokeDevice(new AuthorizedHttp({ db, fetchImpl, baseUrl }), id)
```

> **`deviceName` / `devicePlatform`.** Vérifier ce qui est disponible : `grep -rn "@tauri-apps/plugin-os\|platform()" src/ src-tauri/Cargo.toml`. Si `@tauri-apps/plugin-os` n'est pas installé, **ne pas l'ajouter** : utiliser `navigator.platform`/`navigator.userAgent` pour `devicePlatform` et une valeur par défaut traduite pour `deviceName`. Le serveur accepte `deviceName` ≤ 128 et `devicePlatform` ≤ 64 caractères, sans autre contrainte.

> **`unlock` a besoin de l'email.** Il est dans `sync_state` sous `account_email` — le lire, ne pas le redemander.

Dans `src/components/layout/SettingsDialog.tsx`, trois modifications chirurgicales :

```tsx
	const [activeTab, setActiveTab] = useState<
		"general" | "customization" | "notifications" | "sync" | "data" | "changelog"
	>("general");
```

Ajouter l'entrée dans le tableau des onglets, entre `notifications` et `data`, **et** dans l'assertion de type qui le suit :

```tsx
								["notifications", t("settings.notifications")],
								["sync", t("sync.tab")],
								["data", t("data.title")],
```
```tsx
							] as [
								(
									| "general"
									| "customization"
									| "notifications"
									| "sync"
									| "data"
									| "changelog"
								),
								string,
							][]
```

Et la ligne de rendu :

```tsx
					{activeTab === "sync" && <SyncPanel deps={productionSyncDeps()} />}
```

Dans `src/components/layout/AppShell.tsx`, envelopper le bloc principal pour loger le bandeau :

```tsx
			<div className="relative z-10 flex h-full w-full flex-col overflow-hidden">
				<SyncStatusBanner onOpenSettings={openSettings} />
				<div className="flex flex-1 overflow-hidden">
					<Sidebar />
					<MainPanel selectedProjectId={selectedProjectId} />
					{showDetail && (
						<>
							<ResizeHandle
								onMouseDown={onMouseDown}
								onDoubleClick={onDoubleClick}
								onKeyDown={onKeyDown}
								isDragging={isDragging}
								ariaLabel={t("common.resizePanel")}
							/>
							<TaskDetail width={width} />
						</>
					)}
				</div>
			</div>
```

> `openSettings` n'existe pas : `SettingsDialog` est ouvert par un `DialogTrigger` dans la `Sidebar`. **Le plus simple** : ajouter un champ `settingsOpen` + `openSettings()` à `src/store/ui.ts` (qui gère déjà la navigation), faire consommer `open`/`onOpenChange` par `SettingsDialog`, et laisser la `Sidebar` appeler `openSettings()`. Vérifier d'abord si `Sidebar` utilise réellement `DialogTrigger` (`grep -n "SettingsDialog" -A 4 src/components/layout/Sidebar.tsx`) et suivre le mécanisme en place.

Dans `src/App.tsx`, poser le contexte avant `initSync` et attacher le store après :

```tsx
				const db = Database.get("sqlite:usagi.db");
				const driver = adaptDatabase(db);
				await runMigrations(driver, ALL_MIGRATIONS);
				await backfillSortKeys(driver);
				const repository = createRepository(db);
				setRepository(repository);
				// The panel reconnects and disconnects without a restart, so the
				// runtime needs to be able to rebuild the engine later.
				setSyncContext({
					db: driver,
					repository,
					fetchImpl: httpFetch as FetchLike,
				});
				const syncRuntime = await startSync();
				if (syncRuntime) {
					useSyncStore.getState().attach(syncRuntime.engine);
					await useSyncStore.getState().refreshLastSync(driver);
				}
				setReady(true);
```

> `startSync()` remplace l'appel direct à `initSync` **et** fait déjà `setRepository(runtime.repository)`. Vérifier que `createRepository(db)` est bien la forme réelle (`grep -n "createRepository" src/db/index.ts`) et l'adapter si elle diffère.

Le dialogue de première sync se monte là où le statut est observé — le plus simple est dans `AppShell`, à côté du bandeau :

```tsx
	const status = useSyncStore((s) => s.status);
	// …
	{status === "awaiting-first-sync" && (
		<FirstSyncDialog
			open
			backup={writeAutomaticBackup}
			resolve={(choice) => getSyncRuntime()?.engine.resolveFirstSync(choice) ?? Promise.resolve()}
			onResolved={() => {}}
		/>
	)}
```

avec la sauvegarde automatique, calquée sur `handleExport` de `SettingsDialog.tsx:958-972` mais **sans dialogue de fichier** (§6.4 : elle est automatique) :

```tsx
async function writeAutomaticBackup(): Promise<void> {
	const data = await exportData(getRepository(), {
		activeTasks: true,
		completedTasks: true,
		archivedTasks: true,
		projects: true,
		tags: true,
	});
	const stamp = new Date().toISOString().slice(0, 19).replace(/:/g, "-");
	const dir = await appDataDir();
	await writeTextFile(
		await join(dir, `bunly-before-replace-${stamp}.json`),
		JSON.stringify(data, null, 2),
	);
}
```

> `appDataDir` et `join` viennent de `@tauri-apps/api/path`, `writeTextFile` de `@tauri-apps/plugin-fs` (déjà importé dans `SettingsDialog.tsx`). Vérifier que `@tauri-apps/api/path` est disponible : `grep -rn "@tauri-apps/api/path" src/`. Si les permissions FS ne couvrent pas `appDataDir`, ajouter le scope nécessaire à `src-tauri/capabilities/desktop.json` — et le mentionner dans le message de commit. **Ne rien écrire dans un chemin arbitraire.**

- [ ] **Step 4: Vert complet**

```bash
pnpm test:run src/components/sync/SyncPanel.test.tsx
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : PASS ; **≥ 798 tests**.

- [ ] **Step 5: Vérifier la non-régression §8.2 « sync désactivée »**

Le test qui garantit qu'aucune requête n'est émise sans `server_url` existe déjà. Le localiser et le relancer explicitement — c'est lui qui protège l'utilisateur purement local du branchement qu'on vient de faire :

```bash
grep -rn "server_url" src/sync/init.test.ts | head
pnpm test:run src/sync/init.test.ts
```
Attendu : PASS. Si `startSync` a introduit un chemin qui construit le moteur sans `server_url`, ce test doit le voir — sinon, **l'étendre** pour couvrir `startSync` et non seulement `initSync`.

- [ ] **Step 6: Commit**

```bash
git add src/components/sync/SyncPanel.tsx src/components/sync/SyncPanel.test.tsx src/components/layout/SettingsDialog.tsx src/components/layout/AppShell.tsx src/App.tsx src/store/ui.ts
git commit -m "feat: :sparkles: wire the synchronisation settings into the app"
```

---

### Tâche 15 : Changelog et clôture

**Files:**
- Modify: `src/assets/changelog.json`

- [ ] **Step 1: Ajouter l'entrée bilingue**

Dans `src/assets/changelog.json`, section `"version": "Unreleased"`, ajouter en **tête** du tableau `features` :

```json
					{
						"en": "Sync is here. Connect the app to a usagi server — your own or someone else's — and your tasks, projects and tags follow you across devices. Everything is encrypted on your device before it leaves it, so the server never sees your data. Set it up under Settings › Sync.",
						"fr": "La synchronisation arrive. Connectez l'application à un serveur usagi — le vôtre ou celui d'un tiers — et vos tâches, projets et tags vous suivent d'un appareil à l'autre. Tout est chiffré sur votre appareil avant d'en sortir : le serveur ne voit jamais vos données. Rendez-vous dans Réglages › Synchronisation."
					},
					{
						"en": "Creating a sync account gives you a 24-word recovery key. It is shown once and never stored — it is the only way back in if you forget your password, so you are asked to confirm you wrote it down.",
						"fr": "La création d'un compte de synchronisation fournit une clé de récupération de 24 mots. Elle est affichée une seule fois et n'est jamais enregistrée — c'est le seul moyen de retrouver votre compte si vous oubliez votre mot de passe, d'où la confirmation demandée."
					},
					{
						"en": "When you connect a device that already has tasks to an account that also has some, the app asks whether to merge them or replace what is on this device — and writes an automatic backup before replacing anything.",
						"fr": "Lorsqu'un appareil contenant déjà des tâches est connecté à un compte qui en contient aussi, l'application demande s'il faut les fusionner ou remplacer le contenu de cet appareil — et enregistre une sauvegarde automatique avant tout remplacement."
					},
```

> **Pas d'entrée pour les tâches 1 à 4.** Elles corrigent un moteur que personne n'avait encore activé : il n'y a rien à annoncer à l'utilisateur. La règle CLAUDE.md vise le changement *visible*.

- [ ] **Step 2: Vérifier que le changelog reste valide**

```bash
node -e "JSON.parse(require('fs').readFileSync('src/assets/changelog.json','utf8')); console.log('ok')"
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```
Attendu : `ok`, puis vert complet.

- [ ] **Step 3: Rituel de fin de tâche CLAUDE.md**

```bash
nvm use 22.22.2
rm -rf ~/.npm/_npx
```
Puis lancer le skill `react-doctor`. **Ne corriger que les diagnostics apparus à cause de ce travail** — les problèmes préexistants sans rapport ne sont pas dans le périmètre.

```bash
pnpm run lint:fix
```

- [ ] **Step 4: Vert final et commit**

```bash
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
git add -A
git commit -m "docs: :memo: announce synchronisation in the changelog"
```

- [ ] **Step 5: Vérification manuelle avant d'ouvrir la PR**

Les tests ne peuvent pas couvrir le runtime Tauri réel (crypto Rust, pool sqlx, plugin-http). Dérouler à la main, avec un `usagi-server` local (`docker compose up` dans `usagi-server`, `ALLOW_REGISTRATION=true`) :

1. App vierge, onglet Synchronisation, saisir `http://localhost:3000` → l'avertissement « non chiffrée » s'affiche, le serveur est reconnu.
2. Créer un compte → les 24 mots s'affichent → confirmer 3 mots → le panneau connecté apparaît, statut « À jour ».
3. Créer une tâche → attendre ~2 s → vérifier côté serveur qu'un enregistrement est arrivé.
4. **Fermer et rouvrir l'app** → le bandeau « Sync en pause » doit s'afficher, **rien ne doit partir en quarantaine** :
   ```bash
   sqlite3 ~/.config/<app>/usagi.db "SELECT COUNT(*) FROM sync_quarantine;"
   ```
   Attendu : `0`. **C'est la vérification la plus importante du plan** — c'est le bug que la tâche 1 corrige, et le seul chemin où le vrai `TauriRecordCipher` est en jeu.
5. Déverrouiller → statut « À jour », la tâche créée en 3 est toujours là.
6. Second appareil (autre profil) → même compte, données locales non vides → le dialogue §6.4 doit apparaître ; choisir « Remplacer » → vérifier qu'un fichier `bunly-before-replace-*.json` a bien été écrit avant l'effacement.
7. Réglages → révoquer le second appareil → sur celui-ci, la sync passe en « Reconnexion nécessaire » et le bandeau s'affiche.
8. Déconnecter → les tâches locales sont **intactes**, `sync_state` est vide sauf `device_id` :
   ```bash
   sqlite3 ~/.config/<app>/usagi.db "SELECT key FROM sync_state;"
   ```
   Attendu : `device_id` seul.

---

## Hors périmètre (volontairement)

- **Compteur et écran de quarantaine.** La table `sync_quarantine` est prête et `retryQuarantine()` est public, mais l'affichage n'est pas retenu pour cette itération. Après la tâche 1, un blob quarantiné devient un événement rare (corruption réelle) plutôt qu'un effet de bord du démarrage — l'urgence qui justifiait le compteur a disparu avec la cause.
- **Avertissement d'import §9.4.** Déjà livré ([`ImportConfirmDialog.tsx`](../../../src/components/layout/ImportConfirmDialog.tsx)).
- **Récupération par clé de 24 mots dans l'UI.** `unlockWithRecovery` existe côté Rust et TS, mais le parcours « j'ai oublié mon mot de passe » (saisie des 24 mots, puis ré-enveloppement de la DEK sous un nouveau mot de passe via `prepareKeyRotation` et `PUT /v1/keys`) est un écran à part entière, avec ses propres pièges. À planifier séparément.
- **Changement de mot de passe.** Même raison : `prepareKeyRotation` est prêt, l'UI ne l'est pas.
- **Émission de jetons d'invitation depuis le client.** La CLI serveur (`src/cli/issue-invite.ts`) est le chemin prévu ; le client se contente de consommer un jeton.
- Tout le « hors périmètre v1 » du spec : partage entre comptes, temps réel, sync des réglages, multi-comptes, application web.

---

## Auto-revue

**Couverture du spec.**

| Exigence | Tâche |
|---|---|
| §2.1 clé de 24 mots affichée une fois, confirmation obligatoire, jamais persistée | 10 |
| §6.2 négociation `server-info`, trois erreurs distinctes | 8 |
| §6.2 https imposé, exceptions localhost/127.0.0.1/`.local`, avertissement | 4, 8 |
| §6.2 formulaire d'inscription selon `registrationEnabled`, jeton d'invitation | 10, 14 |
| §6.3 section « Synchronisation » | 14 |
| §6.3 état, dernière sync, liste des appareils, déconnecter cet appareil | 11 |
| §6.4 dialogue fusionner/remplacer, outbox vidée avant push | 13 (moteur : 4c) |
| §6.4 sauvegarde JSON automatique avant « Remplacer » | 13, 14 |
| §6.5 déconnexion efface curseur/outbox/identité, pas le SQLite | 2 |
| §6.5 curseur propre au serveur, un seul serveur à la fois | 2, 11 (URL verrouillée) |
| §7 reconnexion requise, protocole incompatible visibles | 12 |
| §7 coffre fermé après redémarrage (trou comblé) | 1, 12 |

**Types et signatures.** `SyncStatus` gagne `"locked"` en tâche 1 et est consommé sous ce nom exact en 11 (`STATUS_KEY`) et 12 (`ACTIONABLE`). `resolveFirstSync` prend `"merge" | "replace"` partout. `register` retourne `recoveryPhrase`, propagé comme `string` par `RegisterFormProps.onSubmit`. `SyncDevice` est défini une fois en tâche 5 et importé depuis `devices-types` en 11.

**Deux points laissés explicitement ouverts, avec leur résolution indiquée dans la tâche** plutôt que masqués : le libellé court du bouton de déconnexion (tâche 11, Step 4 — ajouter `sync.disconnectShort`) et l'email affiché après inscription (tâche 14, Step 2 — relire `loadSession()`). Les deux sont signalés avec la correction à appliquer, pas laissés en « TODO ».

**Risque résiduel connu.** Aucun test automatisé ne franchit la frontière IPC : `TauriRecordCipher`, le pool sqlx et `plugin-http` ne tournent pas sous vitest. C'est ce que couvre la vérification manuelle de la tâche 15, Step 5 — en particulier le point 4, qui est le seul endroit où le vrai chiffreur rencontre un coffre fermé.

