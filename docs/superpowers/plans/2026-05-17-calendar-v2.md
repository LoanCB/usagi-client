# Calendar View — Améliorations v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter 5 améliorations à la vue calendrier : tooltip au hover en vue semaine, fix navigation tâche complétée passée, création par clic droit, panneau détail du jour latéral avec resize, et date picker dans le header.

**Architecture:** `DayDetailPanel` est un nouveau composant latéral affiché à droite de la grille, réutilisant `useResizable` et `ResizeHandle` comme le `TaskDetail` dans AppShell. `handleTaskClick` est simplifié pour ne plus naviguer hors du calendrier — `TaskDetail` s'affiche désormais en mode calendrier en supprimant le guard dans AppShell. `ContextMenu` wraps chaque case de jour. `CalendarHeader` reçoit un Popover avec `Calendar` (react-day-picker) pour la navigation rapide.

**Tech Stack:** React 19, TypeScript, date-fns v4, Tailwind v4, lucide-react, Zustand, Vitest + RTL, @base-ui/react

---

## File Map

| Fichier | Rôle |
|---|---|
| `src/i18n/locales/en.ts` | 3 nouvelles clés `calendar.*` |
| `src/i18n/locales/fr.ts` | Traductions FR des mêmes clés |
| `src/components/layout/AppShell.tsx` | Retirer `selectedProjectId !== "calendar"` de `showDetail` |
| `src/components/calendar/WeekView.tsx` | Tooltip + ContextMenu |
| `src/components/calendar/MonthView.tsx` | ContextMenu |
| `src/components/calendar/DayDetailPanel.tsx` | Nouveau composant (panneau latéral détail jour) |
| `src/components/calendar/CalendarHeader.tsx` | Popover date picker + prop `onDateChange` |
| `src/components/calendar/CalendarView.tsx` | `useResizable`, layout, `handleTaskClick`, `onDateChange`, `onCreateForDay` |

---

## Task 1: i18n — Nouvelles clés calendrier

**Files:**
- Modify: `src/i18n/locales/en.ts`
- Modify: `src/i18n/locales/fr.ts`

- [ ] **Step 1: Ajouter les 3 clés dans `src/i18n/locales/en.ts`**

Remplacer le bloc `calendar` existant :

```ts
// Avant
calendar: {
  month: "Month",
  week: "Week",
},

// Après
calendar: {
  month: "Month",
  week: "Week",
  noTasks: "No tasks for this day",
  newTask: "New task for this day",
  closeDay: "Close day detail",
},
```

- [ ] **Step 2: Ajouter les 3 clés dans `src/i18n/locales/fr.ts`**

Remplacer le bloc `calendar` existant :

```ts
// Avant
calendar: {
  month: "Mois",
  week: "Semaine",
},

// Après
calendar: {
  month: "Mois",
  week: "Semaine",
  noTasks: "Aucune tâche pour ce jour",
  newTask: "Nouvelle tâche pour ce jour",
  closeDay: "Fermer le détail du jour",
},
```

- [ ] **Step 3: Vérifier la cohérence TypeScript**

```bash
pnpm exec tsc --noEmit
```

Expected: No errors. (`fr.ts` est typé comme `typeof en`, TS vérifie la cohérence des clés.)

- [ ] **Step 4: Commit**

```bash
git add src/i18n/locales/en.ts src/i18n/locales/fr.ts
git commit -m "feat: add calendar i18n keys for day detail panel"
```

---

## Task 2: AppShell — autoriser TaskDetail en vue calendrier

**Files:**
- Modify: `src/components/layout/AppShell.tsx`

- [ ] **Step 1: Modifier la condition `showDetail`**

Localiser dans `src/components/layout/AppShell.tsx` (lignes ~29-32) :

```ts
// Avant
const showDetail =
  selectedTaskId &&
  selectedProjectId !== "tags" &&
  selectedProjectId !== "calendar";

// Après
const showDetail =
  selectedTaskId &&
  selectedProjectId !== "tags";
```

- [ ] **Step 2: Vérifier TypeScript**

```bash
pnpm exec tsc --noEmit
```

Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/components/layout/AppShell.tsx
git commit -m "fix: allow TaskDetail panel to show in calendar view"
```

---

## Task 3: WeekView — tooltip au hover

**Files:**
- Modify: `src/components/calendar/WeekView.tsx`

- [ ] **Step 1: Ajouter `title={task.title}` aux boutons de tâches**

Dans `WeekView.tsx`, localiser les deux `<button>` dans la section task chips.

Pour les tâches `due` (lignes ~76-88), ajouter `title={task.title}` :

```tsx
// Avant
<button
  key={task.id}
  type="button"
  onClick={(e) => {
    e.stopPropagation();
    onTaskClick(task);
  }}
  className="text-xs truncate px-1.5 py-1 rounded bg-orange-500/20 text-orange-400 hover:bg-orange-500/30 text-left w-full transition-colors"
>

// Après
<button
  key={task.id}
  type="button"
  title={task.title}
  onClick={(e) => {
    e.stopPropagation();
    onTaskClick(task);
  }}
  className="text-xs truncate px-1.5 py-1 rounded bg-orange-500/20 text-orange-400 hover:bg-orange-500/30 text-left w-full transition-colors"
>
```

Pour les tâches `completed` (lignes ~89-101), ajouter `title={task.title}` de même :

```tsx
// Avant
<button
  key={task.id}
  type="button"
  onClick={(e) => {
    e.stopPropagation();
    onTaskClick(task);
  }}
  className="text-xs truncate px-1.5 py-1 rounded bg-green-500/20 text-green-400 hover:bg-green-500/30 text-left w-full transition-colors line-through opacity-70"
>

// Après
<button
  key={task.id}
  type="button"
  title={task.title}
  onClick={(e) => {
    e.stopPropagation();
    onTaskClick(task);
  }}
  className="text-xs truncate px-1.5 py-1 rounded bg-green-500/20 text-green-400 hover:bg-green-500/30 text-left w-full transition-colors line-through opacity-70"
>
```

- [ ] **Step 2: Vérifier TypeScript**

```bash
pnpm exec tsc --noEmit
```

Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/components/calendar/WeekView.tsx
git commit -m "feat: add task title tooltip on hover in week view"
```

---

## Task 4: DayDetailPanel — nouveau composant (TDD)

**Files:**
- Create: `src/components/calendar/DayDetailPanel.test.tsx`
- Create: `src/components/calendar/DayDetailPanel.tsx`

- [ ] **Step 1: Créer le fichier de tests `src/components/calendar/DayDetailPanel.test.tsx`**

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import "@/i18n";
import { DayDetailPanel } from "@/components/calendar/DayDetailPanel";
import * as repositoryModule from "@/store/repository";
import { useTaskStore } from "@/store/tasks";
import type { Task } from "@/types";

// biome-ignore lint/suspicious/noExplicitAny: partial mock
const mockRepository = {} as any;
const mockCreateTask = vi.fn().mockResolvedValue({});

const base: Task = {
  id: "t1",
  title: "Design homepage",
  description: null,
  projectId: null,
  priority: "none",
  dueDate: "2026-05-17",
  completedAt: null,
  tags: [],
  sortOrder: 0,
  createdAt: "2026-05-01T10:00:00.000Z",
  updatedAt: "2026-05-01T10:00:00.000Z",
};

beforeEach(() => {
  vi.clearAllMocks();
  vi.spyOn(repositoryModule, "getRepository").mockReturnValue(mockRepository);
  useTaskStore.setState({
    tasks: [],
    loading: false,
    createTask: mockCreateTask,
    loadTasks: vi.fn(),
    updateTask: vi.fn(),
    completeTask: vi.fn(),
    uncompleteTask: vi.fn(),
    deleteTask: vi.fn(),
    reorderTasks: vi.fn(),
  });
});

describe("DayDetailPanel", () => {
  it("shows noTasks message when entry is undefined", () => {
    render(
      <DayDetailPanel
        day="2026-05-17"
        entry={undefined}
        width={280}
        onClose={vi.fn()}
        onTaskClick={vi.fn()}
      />,
    );
    expect(
      screen.getByText(/no tasks for this day/i),
    ).toBeInTheDocument();
  });

  it("renders due tasks", () => {
    render(
      <DayDetailPanel
        day="2026-05-17"
        entry={{ due: [base], completed: [] }}
        width={280}
        onClose={vi.fn()}
        onTaskClick={vi.fn()}
      />,
    );
    expect(screen.getByText("Design homepage")).toBeInTheDocument();
  });

  it("renders completed tasks with line-through class", () => {
    const completed: Task = {
      ...base,
      completedAt: "2026-05-17T10:00:00.000Z",
    };
    render(
      <DayDetailPanel
        day="2026-05-17"
        entry={{ due: [], completed: [completed] }}
        width={280}
        onClose={vi.fn()}
        onTaskClick={vi.fn()}
      />,
    );
    expect(screen.getByText("Design homepage")).toHaveClass("line-through");
  });

  it("calls onTaskClick when a due task is clicked", async () => {
    const user = userEvent.setup();
    const onTaskClick = vi.fn();
    render(
      <DayDetailPanel
        day="2026-05-17"
        entry={{ due: [base], completed: [] }}
        width={280}
        onClose={vi.fn()}
        onTaskClick={onTaskClick}
      />,
    );
    await user.click(screen.getByText("Design homepage"));
    expect(onTaskClick).toHaveBeenCalledWith(base);
  });

  it("calls onClose when the close button is clicked", async () => {
    const user = userEvent.setup();
    const onClose = vi.fn();
    render(
      <DayDetailPanel
        day="2026-05-17"
        entry={undefined}
        width={280}
        onClose={onClose}
        onTaskClick={vi.fn()}
      />,
    );
    await user.click(
      screen.getByRole("button", { name: /close day detail/i }),
    );
    expect(onClose).toHaveBeenCalledOnce();
  });
});
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

```bash
pnpm test:run src/components/calendar/DayDetailPanel.test.tsx
```

Expected: FAIL — module `@/components/calendar/DayDetailPanel` not found.

- [ ] **Step 3: Créer `src/components/calendar/DayDetailPanel.tsx`**

```tsx
import { format } from "date-fns";
import { enUS, fr } from "date-fns/locale";
import { X } from "lucide-react";
import { useTranslation } from "react-i18next";
import { QuickAddTask } from "@/components/tasks/QuickAddTask";
import type { Task } from "@/types";

interface DayDetailPanelProps {
  readonly day: string;
  readonly entry: { due: Task[]; completed: Task[] } | undefined;
  readonly width: number;
  readonly onClose: () => void;
  readonly onTaskClick: (task: Task) => void;
}

export function DayDetailPanel({
  day,
  entry,
  width,
  onClose,
  onTaskClick,
}: DayDetailPanelProps) {
  const { t, i18n } = useTranslation();
  const locale = i18n.language === "fr" ? fr : enUS;

  const date = new Date(`${day}T00:00:00`);
  const due = entry?.due ?? [];
  const completed = entry?.completed ?? [];
  const hasAny = due.length > 0 || completed.length > 0;

  return (
    <div
      className="flex flex-col shrink-0 border-l border-border/40 overflow-hidden"
      style={{ width }}
    >
      <div className="glass-header px-4 py-3 shrink-0 flex items-center justify-between">
        <span className="text-sm font-semibold capitalize">
          {format(date, "EEEE d MMMM yyyy", { locale })}
        </span>
        <button
          type="button"
          aria-label={t("calendar.closeDay")}
          onClick={onClose}
          className="text-muted-foreground hover:text-foreground transition-colors"
        >
          <X className="h-4 w-4" />
        </button>
      </div>

      <div className="flex flex-col flex-1 overflow-y-auto px-3 py-2 gap-1">
        {!hasAny && (
          <p className="text-xs text-muted-foreground/60 text-center py-4">
            {t("calendar.noTasks")}
          </p>
        )}
        {due.map((task) => (
          <button
            key={task.id}
            type="button"
            onClick={() => onTaskClick(task)}
            className="text-xs text-left px-2 py-1.5 rounded bg-orange-500/20 text-orange-400 hover:bg-orange-500/30 transition-colors w-full"
          >
            {task.title}
          </button>
        ))}
        {completed.map((task) => (
          <button
            key={task.id}
            type="button"
            onClick={() => onTaskClick(task)}
            className="text-xs text-left px-2 py-1.5 rounded bg-green-500/20 text-green-400 hover:bg-green-500/30 transition-colors w-full line-through opacity-70"
          >
            {task.title}
          </button>
        ))}
      </div>

      <div className="shrink-0 border-t border-border/40 py-1">
        <QuickAddTask projectId={null} dueDate={day} />
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Lancer les tests pour vérifier qu'ils passent**

```bash
pnpm test:run src/components/calendar/DayDetailPanel.test.tsx
```

Expected: All 5 PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/calendar/DayDetailPanel.tsx src/components/calendar/DayDetailPanel.test.tsx
git commit -m "feat: add DayDetailPanel component for calendar day detail"
```

---

## Task 5: MonthView — ContextMenu par case

**Files:**
- Modify: `src/components/calendar/MonthView.tsx`

- [ ] **Step 1: Ajouter les imports ContextMenu et mettre à jour l'interface**

En haut de `MonthView.tsx`, ajouter les imports :

```tsx
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuTrigger,
} from "@/components/ui/context-menu";
```

Mettre à jour l'import `useTranslation` pour extraire `t` :

```tsx
// Avant
const { i18n } = useTranslation();

// Après
const { t, i18n } = useTranslation();
```

Ajouter `onCreateForDay` à l'interface :

```tsx
// Avant
interface MonthViewProps {
  readonly currentDate: Date;
  readonly grouped: Map<string, { due: Task[]; completed: Task[] }>;
  readonly selectedDay: string | null;
  readonly onDayClick: (date: string) => void;
  readonly onTaskClick: (task: Task) => void;
}

// Après
interface MonthViewProps {
  readonly currentDate: Date;
  readonly grouped: Map<string, { due: Task[]; completed: Task[] }>;
  readonly selectedDay: string | null;
  readonly onDayClick: (date: string) => void;
  readonly onTaskClick: (task: Task) => void;
  readonly onCreateForDay: (date: string) => void;
}
```

Ajouter `onCreateForDay` dans la déstructuration des props :

```tsx
// Avant
export function MonthView({
  currentDate,
  grouped,
  selectedDay,
  onDayClick,
  onTaskClick,
}: MonthViewProps) {

// Après
export function MonthView({
  currentDate,
  grouped,
  selectedDay,
  onDayClick,
  onTaskClick,
  onCreateForDay,
}: MonthViewProps) {
```

- [ ] **Step 2: Envelopper chaque case dans ContextMenu**

Dans la grille des jours (section `{days.map((day) => { ... })}`), remplacer le `<div key={iso} onClick={...}>` par un `ContextMenu` + `ContextMenuTrigger` :

```tsx
// Avant
return (
  <div
    key={iso}
    onClick={() => onDayClick(iso)}
    className={cn(
      "min-h-[90px] border border-border/20 p-1.5 cursor-pointer transition-colors hover:bg-foreground/5",
      !sameMonth && "opacity-40",
      today && "bg-primary/10 border-primary/30",
      selected && "ring-2 ring-inset ring-primary",
    )}
  >
    {/* ... contenu existant ... */}
  </div>
);

// Après
return (
  <ContextMenu key={iso}>
    <ContextMenuTrigger
      onClick={() => onDayClick(iso)}
      className={cn(
        "min-h-[90px] border border-border/20 p-1.5 cursor-pointer transition-colors hover:bg-foreground/5",
        !sameMonth && "opacity-40",
        today && "bg-primary/10 border-primary/30",
        selected && "ring-2 ring-inset ring-primary",
      )}
    >
      {/* ... contenu existant inchangé ... */}
    </ContextMenuTrigger>
    <ContextMenuContent>
      <ContextMenuItem onClick={() => onCreateForDay(iso)}>
        {t("calendar.newTask")}
      </ContextMenuItem>
    </ContextMenuContent>
  </ContextMenu>
);
```

Le contenu interne (numéro du jour + boutons de tâches) reste identique — seul le conteneur change.

- [ ] **Step 3: Vérifier TypeScript**

```bash
pnpm exec tsc --noEmit
```

Expected: No errors.

- [ ] **Step 4: Commit**

```bash
git add src/components/calendar/MonthView.tsx
git commit -m "feat: add right-click context menu on month view day cells"
```

---

## Task 6: WeekView — ContextMenu par colonne

**Files:**
- Modify: `src/components/calendar/WeekView.tsx`

- [ ] **Step 1: Ajouter les imports ContextMenu et mettre à jour l'interface**

En haut de `WeekView.tsx`, ajouter les imports :

```tsx
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuTrigger,
} from "@/components/ui/context-menu";
```

Mettre à jour `useTranslation` pour inclure `t` :

```tsx
// Avant
const { i18n } = useTranslation();

// Après
const { t, i18n } = useTranslation();
```

Ajouter `onCreateForDay` à l'interface et la déstructuration :

```tsx
// Avant
interface WeekViewProps {
  readonly currentDate: Date;
  readonly grouped: Map<string, { due: Task[]; completed: Task[] }>;
  readonly selectedDay: string | null;
  readonly onDayClick: (date: string) => void;
  readonly onTaskClick: (task: Task) => void;
}

// Après
interface WeekViewProps {
  readonly currentDate: Date;
  readonly grouped: Map<string, { due: Task[]; completed: Task[] }>;
  readonly selectedDay: string | null;
  readonly onDayClick: (date: string) => void;
  readonly onTaskClick: (task: Task) => void;
  readonly onCreateForDay: (date: string) => void;
}
```

Ajouter `onCreateForDay` dans la déstructuration :

```tsx
// Avant
export function WeekView({
  currentDate,
  grouped,
  selectedDay,
  onDayClick,
  onTaskClick,
}: WeekViewProps) {

// Après
export function WeekView({
  currentDate,
  grouped,
  selectedDay,
  onDayClick,
  onTaskClick,
  onCreateForDay,
}: WeekViewProps) {
```

- [ ] **Step 2: Envelopper chaque colonne dans ContextMenu**

Dans `{days.map((day) => { ... })}`, remplacer le `<div key={iso} onClick={...}>` par ContextMenu + ContextMenuTrigger :

```tsx
// Avant
return (
  <div
    key={iso}
    onClick={() => onDayClick(iso)}
    className={cn(
      "flex flex-col flex-1 min-w-0 border border-border/20 cursor-pointer transition-colors hover:bg-foreground/5",
      today && "bg-primary/10 border-primary/30",
      selected && "ring-2 ring-inset ring-primary",
    )}
  >
    {/* ... contenu existant ... */}
  </div>
);

// Après
return (
  <ContextMenu key={iso}>
    <ContextMenuTrigger
      onClick={() => onDayClick(iso)}
      className={cn(
        "flex flex-col flex-1 min-w-0 border border-border/20 cursor-pointer transition-colors hover:bg-foreground/5",
        today && "bg-primary/10 border-primary/30",
        selected && "ring-2 ring-inset ring-primary",
      )}
    >
      {/* ... contenu existant inchangé (day header + task chips) ... */}
    </ContextMenuTrigger>
    <ContextMenuContent>
      <ContextMenuItem onClick={() => onCreateForDay(iso)}>
        {t("calendar.newTask")}
      </ContextMenuItem>
    </ContextMenuContent>
  </ContextMenu>
);
```

- [ ] **Step 3: Vérifier TypeScript**

```bash
pnpm exec tsc --noEmit
```

Expected: No errors.

- [ ] **Step 4: Commit**

```bash
git add src/components/calendar/WeekView.tsx
git commit -m "feat: add right-click context menu on week view day columns"
```

---

## Task 7: CalendarHeader — date picker

**Files:**
- Modify: `src/components/calendar/CalendarHeader.tsx`

- [ ] **Step 1: Mettre à jour les imports**

Remplacer le contenu des imports dans `CalendarHeader.tsx` :

```tsx
import { endOfWeek, format, startOfWeek } from "date-fns";
import { enUS, fr } from "date-fns/locale";
import { CalendarIcon, ChevronLeft, ChevronRight } from "lucide-react";
import { useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "@/components/ui/button";
import { Calendar } from "@/components/ui/calendar";
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover";
import { cn } from "@/lib/utils";
```

- [ ] **Step 2: Mettre à jour l'interface props et la fonction**

Remplacer l'intégralité de `CalendarHeader.tsx` (à partir de l'interface) par :

```tsx
export type CalendarViewMode = "month" | "week";

interface CalendarHeaderProps {
  readonly currentDate: Date;
  readonly viewMode: CalendarViewMode;
  readonly onViewModeChange: (mode: CalendarViewMode) => void;
  readonly onPrev: () => void;
  readonly onNext: () => void;
  readonly onDateChange: (date: Date) => void;
}

export function CalendarHeader({
  currentDate,
  viewMode,
  onViewModeChange,
  onPrev,
  onNext,
  onDateChange,
}: CalendarHeaderProps) {
  const { t, i18n } = useTranslation();
  const locale = i18n.language === "fr" ? fr : enUS;
  const [pickerOpen, setPickerOpen] = useState(false);

  const label =
    viewMode === "month"
      ? format(currentDate, "MMMM yyyy", { locale })
      : (() => {
          const weekStart = startOfWeek(currentDate, { weekStartsOn: 1 });
          const weekEnd = endOfWeek(currentDate, { weekStartsOn: 1 });
          return `${format(weekStart, "d MMM", { locale })} – ${format(weekEnd, "d MMM yyyy", { locale })}`;
        })();

  return (
    <div className="glass-header px-5 pt-5 pb-3 shrink-0 flex items-center justify-between">
      <div className="flex items-center gap-2">
        <h2 className="font-bold text-xl tracking-tight capitalize">{label}</h2>
        <Popover open={pickerOpen} onOpenChange={setPickerOpen}>
          <PopoverTrigger
            aria-label="date picker"
            className="text-muted-foreground hover:text-foreground transition-colors p-1 rounded"
          >
            <CalendarIcon className="h-4 w-4" />
          </PopoverTrigger>
          <PopoverContent side="bottom" align="start" className="w-auto p-0">
            <Calendar
              mode="single"
              selected={currentDate}
              onSelect={(d) => {
                if (d) {
                  onDateChange(d);
                  setPickerOpen(false);
                }
              }}
              captionLayout="dropdown"
              locale={locale}
              startMonth={new Date(2020, 0)}
              endMonth={new Date(2030, 11)}
            />
          </PopoverContent>
        </Popover>
      </div>
      <div className="flex items-center gap-2">
        <div className="flex rounded-lg overflow-hidden border border-border/40">
          <button
            type="button"
            onClick={() => onViewModeChange("month")}
            className={cn(
              "px-3 py-1 text-sm transition-colors",
              viewMode === "month"
                ? "bg-primary text-primary-foreground"
                : "text-muted-foreground hover:text-foreground hover:bg-foreground/5",
            )}
          >
            {t("calendar.month")}
          </button>
          <button
            type="button"
            onClick={() => onViewModeChange("week")}
            className={cn(
              "px-3 py-1 text-sm transition-colors",
              viewMode === "week"
                ? "bg-primary text-primary-foreground"
                : "text-muted-foreground hover:text-foreground hover:bg-foreground/5",
            )}
          >
            {t("calendar.week")}
          </button>
        </div>
        <Button variant="ghost" size="icon" onClick={onPrev} className="h-8 w-8">
          <ChevronLeft className="h-4 w-4" />
        </Button>
        <Button variant="ghost" size="icon" onClick={onNext} className="h-8 w-8">
          <ChevronRight className="h-4 w-4" />
        </Button>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Vérifier TypeScript**

```bash
pnpm exec tsc --noEmit
```

Expected: No errors (CalendarView appellera `onDateChange` manquant → erreur TS attendue et corrigée en Task 8).

- [ ] **Step 4: Commit**

```bash
git add src/components/calendar/CalendarHeader.tsx
git commit -m "feat: add date picker popover to calendar header"
```

---

## Task 8: CalendarView — câblage final

**Files:**
- Modify: `src/components/calendar/CalendarView.tsx`

- [ ] **Step 1: Remplacer l'intégralité de `CalendarView.tsx`**

```tsx
import { addMonths, addWeeks, subMonths, subWeeks } from "date-fns";
import { useMemo, useState, useEffect } from "react";
import { ResizeHandle } from "@/components/layout/ResizeHandle";
import { groupTasksByDate } from "@/lib/calendarUtils";
import { useResizable } from "@/hooks/useResizable";
import { getRepository } from "@/store/repository";
import { useTaskStore } from "@/store/tasks";
import { useUIStore } from "@/store/ui";
import type { Task } from "@/types";
import { CalendarHeader, type CalendarViewMode } from "./CalendarHeader";
import { DayDetailPanel } from "./DayDetailPanel";
import { MonthView } from "./MonthView";
import { WeekView } from "./WeekView";

export function CalendarView() {
  const [viewMode, setViewMode] = useState<CalendarViewMode>("month");
  const [currentDate, setCurrentDate] = useState(() => new Date());
  const [selectedDay, setSelectedDay] = useState<string | null>(null);

  const { loadTasks } = useTaskStore();
  const tasks = useTaskStore((s) => s.tasks);
  const { setSelectedTask } = useUIStore();

  const { width, isDragging, onMouseDown, onDoubleClick } = useResizable({
    storageKey: "calendar-day-panel-width",
    defaultWidth: 280,
    minWidth: 200,
    maxWidth: 480,
  });

  useEffect(() => {
    loadTasks(getRepository(), { allTasks: true });
  }, [loadTasks]);

  const grouped = useMemo(() => groupTasksByDate(tasks), [tasks]);

  function handlePrev() {
    setCurrentDate((d) =>
      viewMode === "month" ? subMonths(d, 1) : subWeeks(d, 1),
    );
    setSelectedDay(null);
  }

  function handleNext() {
    setCurrentDate((d) =>
      viewMode === "month" ? addMonths(d, 1) : addWeeks(d, 1),
    );
    setSelectedDay(null);
  }

  function handleDayClick(date: string) {
    setSelectedDay((prev) => (prev === date ? null : date));
  }

  function handleOpenDay(date: string) {
    setSelectedDay(date);
  }

  function handleTaskClick(task: Task) {
    setSelectedTask(task.id);
  }

  function handleDateChange(date: Date) {
    setCurrentDate(date);
    setSelectedDay(null);
  }

  return (
    <div className="flex flex-col flex-1 min-w-0 overflow-hidden">
      <CalendarHeader
        currentDate={currentDate}
        viewMode={viewMode}
        onViewModeChange={(mode) => {
          setViewMode(mode);
          setSelectedDay(null);
        }}
        onPrev={handlePrev}
        onNext={handleNext}
        onDateChange={handleDateChange}
      />

      <div className="flex flex-1 overflow-hidden min-w-0">
        <div className="flex flex-col flex-1 overflow-hidden min-w-0">
          {viewMode === "month" ? (
            <MonthView
              currentDate={currentDate}
              grouped={grouped}
              selectedDay={selectedDay}
              onDayClick={handleDayClick}
              onTaskClick={handleTaskClick}
              onCreateForDay={handleOpenDay}
            />
          ) : (
            <WeekView
              currentDate={currentDate}
              grouped={grouped}
              selectedDay={selectedDay}
              onDayClick={handleDayClick}
              onTaskClick={handleTaskClick}
              onCreateForDay={handleOpenDay}
            />
          )}
        </div>
        {selectedDay && (
          <>
            <ResizeHandle
              onMouseDown={onMouseDown}
              onDoubleClick={onDoubleClick}
              isDragging={isDragging}
            />
            <DayDetailPanel
              day={selectedDay}
              entry={grouped.get(selectedDay)}
              width={width}
              onClose={() => setSelectedDay(null)}
              onTaskClick={handleTaskClick}
            />
          </>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Vérifier TypeScript**

```bash
pnpm exec tsc --noEmit
```

Expected: No errors.

- [ ] **Step 3: Lancer tous les tests unitaires**

```bash
pnpm test:run
```

Expected: All PASS (including DayDetailPanel tests from Task 4).

- [ ] **Step 4: Commit**

```bash
git add src/components/calendar/CalendarView.tsx
git commit -m "feat: wire DayDetailPanel, resize, date picker and fix task click in CalendarView"
```

---

## Self-Review

**Spec coverage :**

- ✅ Tooltip WeekView — Task 3 (`title={task.title}`)
- ✅ Fix tâche complétée — Task 2 (AppShell `showDetail`) + Task 8 (`handleTaskClick` simplifié)
- ✅ Clic droit créer tâche — Tasks 5 & 6 (ContextMenu) + Task 8 (`onCreateForDay`)
- ✅ Clic gauche → détail du jour — Task 4 (DayDetailPanel) + Task 8 (layout + `handleDayClick`)
- ✅ Date picker — Task 7 (CalendarHeader) + Task 8 (`handleDateChange`)
- ✅ Resize + double-clic reset — Task 8 (`useResizable` + `ResizeHandle`)

**Placeholder scan :** Aucun.

**Type consistency :**
- `onCreateForDay: (date: string) => void` défini dans MonthViewProps (Task 5), WeekViewProps (Task 6) et passé depuis CalendarView (Task 8) — cohérent.
- `DayDetailPanelProps.entry: { due: Task[]; completed: Task[] } | undefined` — cohérent avec `grouped.get(selectedDay)` qui retourne `undefined` si absent.
- `onDateChange: (date: Date) => void` défini dans `CalendarHeaderProps` (Task 7) et implémenté par `handleDateChange` dans CalendarView (Task 8) — cohérent.
- `handleTaskClick` appelle `setSelectedTask(task.id)` — `setSelectedTask` est `(id: string | null) => void` dans `useUIStore` — cohérent.
